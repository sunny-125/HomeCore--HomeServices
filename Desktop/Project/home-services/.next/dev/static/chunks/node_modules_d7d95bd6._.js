(globalThis.TURBOPACK || (globalThis.TURBOPACK = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "State",
    ()=>d,
    "compareArrays",
    ()=>o,
    "compose",
    ()=>r,
    "createSingletonMixin",
    ()=>h,
    "createTemplate",
    ()=>n,
    "debounce",
    ()=>f,
    "decodeJWT",
    ()=>m,
    "isObjEmpty",
    ()=>u,
    "isPlainObject",
    ()=>a,
    "kebabCase",
    ()=>c,
    "pathJoin",
    ()=>s,
    "pluralize",
    ()=>l,
    "withMemCache",
    ()=>i
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
;
function r(...t) {
    return (e)=>t.reduce((t, e)=>e(t), e);
}
const n = (t)=>{
    const e = document.createElement("template");
    return e.innerHTML = t, e;
}, s = (...t)=>t.join("/").replace(/\/+/g, "/"), o = (t, e)=>t.length === e.length && t.every((t, r)=>t === e[r]), i = (t)=>{
    let e, r;
    return (...n)=>(e && o(e, n) || (e = n, r = t(...n)), r);
}, c = (t)=>t.replace(/([a-z])([A-Z])/g, "$1-$2").replace(/[\s_.]+/g, "-").toLowerCase(), u = (t)=>0 === Object.keys(t).length && t.constructor === Object, l = (t)=>(e, ...r)=>e.reduce((e, n, s)=>{
            var o;
            return `${e}${n}${(null === (o = null == r ? void 0 : r[s]) || void 0 === o ? void 0 : o[t > 1 ? 1 : 0]) || ""}`;
        }, ""), f = (t, e = 500)=>{
    let r;
    return function(...n) {
        clearTimeout(r), r = setTimeout(()=>t.apply(this, n), e);
    };
}, a = (t)=>null !== t && "object" == typeof t && Object.getPrototypeOf(t) === Object.prototype, h = (t)=>{
    const e = Symbol(((t)=>{
        const e = t.toString();
        let r = 0;
        for(let t = 0; t < e.length; t++)r = (r << 5) - r + e.charCodeAt(t), r &= r;
        return r.toString(16);
    })(t));
    return (r)=>{
        if (r[e]) return r;
        const n = t(r);
        return n[e] = !0, n;
    };
};
var p, b, g, j;
function O(t, e) {
    const r = Object.getOwnPropertyNames(t), n = Object.getOwnPropertyNames(e);
    if (r.length !== n.length) return !1;
    for(let n = 0; n < r.length; n += 1){
        const s = r[n], o = t[s], i = e[s];
        if (null === o || null === i) {
            if (o !== i) return !1;
        } else if ("object" == typeof o && "object" == typeof i) {
            if (!O(o, i)) return !1;
        } else if (o !== i) return !1;
    }
    return !0;
}
class d {
    constructor(r = {}, { forceUpdate: n = !1 } = {}){
        p.set(this, void 0), b.set(this, {}), g.set(this, 0), j.set(this, !0), this.update = (r)=>{
            const n = "function" == typeof r ? r((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f")) : r, s = Object.assign(Object.assign({}, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f")), n);
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, j, "f") || !O((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f"), s)) {
                const r = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f");
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, p, s, "f"), Object.freeze((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f")), setTimeout(()=>{
                    Object.values((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, b, "f")).forEach((t)=>t(s, r, ((t, e)=>(r)=>t[r] !== e[r])(s, r)));
                }, 0);
            }
        }, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, p, r, "f"), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, j, n, "f");
    }
    get current() {
        return Object.assign({}, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, p, "f"));
    }
    subscribe(r) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, g, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, g, "f") + 1, "f"), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, b, "f")[(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, g, "f")] = r, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, g, "f").toString();
    }
    unsubscribe(e) {
        const r = !!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, b, "f")[e];
        return r && delete (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, b, "f")[e], r;
    }
    unsubscribeAll() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, b, {}, "f"), !0;
    }
}
p = new WeakMap, b = new WeakMap, g = new WeakMap, j = new WeakMap;
const m = (t)=>{
    try {
        const e = t.split(".")[1].replace(/-/g, "+").replace(/_/g, "/"), r = decodeURIComponent(atob(e).split("").map((t)=>"%" + ("00" + t.charCodeAt(0).toString(16)).slice(-2)).join(""));
        return JSON.parse(r);
    } catch (t) {
        return null;
    }
};
;
 //# sourceMappingURL=index.esm.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "loggerMixin",
    ()=>loggerMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
;
;
const logLevels = [
    'error',
    'warn',
    'info',
    'debug'
];
const defaultLogger = {
    error: console.error.bind(console, '[Descope]'),
    warn: console.warn.bind(console, '[Descope]'),
    info: console.info.bind(console, '[Descope]'),
    debug: console.debug.bind(console, '[Descope]')
};
const loggerMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _LoggerMixinClass_instances, _LoggerMixinClass_logger, _LoggerMixinClass_wrapLogger, _a;
    return _a = class LoggerMixinClass extends superclass {
        constructor(){
            super(...arguments);
            _LoggerMixinClass_instances.add(this);
            _LoggerMixinClass_logger.set(this, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _LoggerMixinClass_instances, "m", _LoggerMixinClass_wrapLogger).call(this, defaultLogger));
        }
        set logger(logger) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _LoggerMixinClass_logger, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _LoggerMixinClass_instances, "m", _LoggerMixinClass_wrapLogger).call(this, logger || defaultLogger), "f");
        }
        get logger() {
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _LoggerMixinClass_logger, "f");
        }
        // eslint-disable-next-line class-methods-use-this, @typescript-eslint/no-unused-vars
        onLogEvent(logLevel, data) {}
    }, _LoggerMixinClass_logger = new WeakMap(), _LoggerMixinClass_instances = new WeakSet(), _LoggerMixinClass_wrapLogger = function _LoggerMixinClass_wrapLogger(logger) {
        return logLevels.reduce((acc, logLevel)=>{
            acc[logLevel] = (...args)=>{
                var _b;
                this.onLogEvent(logLevel, args);
                (_b = logger[logLevel]) === null || _b === void 0 ? void 0 : _b.call(logger, ...args);
            };
            return acc;
        }, {});
    }, _a;
});
;
 //# sourceMappingURL=loggerMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "IS_LOCAL_STORAGE",
    ()=>IS_LOCAL_STORAGE
]);
const IS_LOCAL_STORAGE = typeof localStorage !== 'undefined';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ASSETS_FOLDER",
    ()=>ASSETS_FOLDER,
    "BASE_CONTENT_URL",
    ()=>BASE_CONTENT_URL,
    "BASE_CONTENT_URL_FALLBACK",
    ()=>BASE_CONTENT_URL_FALLBACK,
    "OVERRIDE_CONTENT_URL",
    ()=>OVERRIDE_CONTENT_URL
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/constants.js [app-client] (ecmascript)");
;
const BASE_CONTENT_URL_KEY = 'base.content.url';
const BASE_CONTENT_URL = 'https://static.descope.com/pages';
const BASE_CONTENT_URL_FALLBACK = 'https://static2.descope.com/pages';
const OVERRIDE_CONTENT_URL = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IS_LOCAL_STORAGE"] && localStorage.getItem(BASE_CONTENT_URL_KEY) || '';
const ASSETS_FOLDER = 'v2-beta';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/commonValidators.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "missingAttrValidator",
    ()=>missingAttrValidator
]);
const missingAttrValidator = (attrName, value)=>!value && `${attrName} cannot be empty, please make sure to set this attribute`;
;
 //# sourceMappingURL=commonValidators.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "initLifecycleMixin",
    ()=>initLifecycleMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
;
;
const initLifecycleMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _InitLifecycleMixinClass_isInit, _a;
    return _a = class InitLifecycleMixinClass extends superclass {
        constructor(){
            super(...arguments);
            _InitLifecycleMixinClass_isInit.set(this, true);
        }
        connectedCallback() {
            var _b, _c;
            (_b = super.connectedCallback) === null || _b === void 0 ? void 0 : _b.call(this);
            if ((_c = this.shadowRoot) === null || _c === void 0 ? void 0 : _c.isConnected) {
                // the init function is running once, on the first time the component is connected
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _InitLifecycleMixinClass_isInit, "f")) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _InitLifecycleMixinClass_isInit, false, "f");
                    this.init();
                }
            }
        }
        // eslint-disable-next-line class-methods-use-this, @typescript-eslint/no-empty-function
        async init() {}
    }, _InitLifecycleMixinClass_isInit = new WeakMap(), _a;
});
;
 //# sourceMappingURL=initLifecycleMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "attributesObserver",
    ()=>attributesObserver
]);
const attributesObserver = (ele, callback)=>{
    // sync all attrs on init
    Array.from(ele.attributes).forEach((attr)=>callback(attr.name));
    const observer = new MutationObserver((mutationsList)=>{
        mutationsList.forEach((mutation)=>{
            if (mutation.type === 'attributes') {
                callback(mutation.attributeName);
            }
        });
    });
    observer.observe(ele, {
        attributes: true
    });
};
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "observeAttributesMixin",
    ()=>observeAttributesMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/helpers.js [app-client] (ecmascript)");
;
;
;
;
;
const observeAttributesMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _ObserveAttributesMixinClass_observeMappings, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"])(superclass);
    return _a = class ObserveAttributesMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _ObserveAttributesMixinClass_observeMappings.set(this, {});
        }
        async init() {
            var _b;
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["attributesObserver"])(this, (attrName)=>{
                var _b;
                (_b = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ObserveAttributesMixinClass_observeMappings, "f")[attrName]) === null || _b === void 0 ? void 0 : _b.forEach((cb)=>{
                    cb(attrName, this.getAttribute(attrName));
                });
            });
        }
        observeAttribute(attrName, onAttrChange) {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ObserveAttributesMixinClass_observeMappings, "f")[attrName]) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ObserveAttributesMixinClass_observeMappings, "f")[attrName] = [];
            }
            const idx = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ObserveAttributesMixinClass_observeMappings, "f")[attrName].push(onAttrChange);
            return ()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ObserveAttributesMixinClass_observeMappings, "f")[attrName].splice(idx, 1);
        }
        observeAttributes(attrs, cb) {
            const unobserveList = attrs.reduce((acc, attrName)=>{
                acc.push(this.observeAttribute(attrName, cb));
                return acc;
            }, []);
            return ()=>unobserveList.forEach((unobserve)=>unobserve());
        }
    }, _ObserveAttributesMixinClass_observeMappings = new WeakMap(), _a;
});
;
 //# sourceMappingURL=observeAttributesMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createValidateAttributesMixin",
    ()=>createValidateAttributesMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/commonValidators.js [app-client] (ecmascript)");
;
;
;
;
;
;
const createValidateAttributesMixin = (mappings)=>(superclass)=>{
        var _ValidateAttributesMixinClass_instances, _ValidateAttributesMixinClass_handleError, _a;
        const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["observeAttributesMixin"])(superclass);
        const mappingsNames = Object.keys(mappings);
        return _a = class ValidateAttributesMixinClass extends BaseClass {
            constructor(...args){
                super(...args);
                _ValidateAttributesMixinClass_instances.add(this);
                this.observeAttributes(mappingsNames, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ValidateAttributesMixinClass_instances, "m", _ValidateAttributesMixinClass_handleError).bind(this));
            }
            async init() {
                var _b;
                // check attributes initial values
                mappingsNames.forEach((attr)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ValidateAttributesMixinClass_instances, "m", _ValidateAttributesMixinClass_handleError).call(this, attr, this.getAttribute(attr)));
                await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            }
        }, _ValidateAttributesMixinClass_instances = new WeakSet(), _ValidateAttributesMixinClass_handleError = function _ValidateAttributesMixinClass_handleError(attrName, newValue) {
            const onError = mappings[attrName];
            const error = typeof onError === 'function' ? onError(attrName, newValue) : onError;
            if (error) {
                this.logger.error(error);
            }
        }, _a;
    };
createValidateAttributesMixin.missingAttrValidator = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["missingAttrValidator"];
;
 //# sourceMappingURL=createValidateAttributesMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/projectIdMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "projectIdMixin",
    ()=>projectIdMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/commonValidators.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)");
;
;
;
const projectIdMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"])({
        'project-id': __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["missingAttrValidator"]
    }))(superclass);
    return class ProjectIdMixinClass extends BaseClass {
        get projectId() {
            return this.getAttribute('project-id');
        }
    };
});
;
 //# sourceMappingURL=projectIdMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/baseUrlMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "baseUrlMixin",
    ()=>baseUrlMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
;
const baseUrlMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    return class BaseUrlMixinClass extends superclass {
        get baseUrl() {
            return this.getAttribute('base-url') || '';
        }
    };
});
;
 //# sourceMappingURL=baseUrlMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/fetchWithFallbacks.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "fetchWithFallbacks",
    ()=>fetchWithFallbacks
]);
const notLastMsgSuffix = 'Trying the next fallback URL...';
// reties in case on network error
const fetchWithRetry = async (url, init, { logger } = {})=>{
    try {
        return await fetch(url, init);
    } catch (e) {
        // if there is an exception, we want to retry
        // so we can overcome network errors
        logger === null || logger === void 0 ? void 0 : logger.debug(`Network error fetching URL ${url} [${e.message}], retrying...`);
        return fetch(url, init);
    }
};
const fetchWithFallbacks = async (fallbacks, init, { logger, onSuccess } = {})=>{
    const fallbacksArr = Array.isArray(fallbacks) ? fallbacks : [
        fallbacks
    ];
    for(let index = 0; index < fallbacksArr.length; index++){
        const url = fallbacksArr[index];
        const isLast = index === fallbacksArr.length - 1;
        try {
            const res = await fetchWithRetry(url.toString(), init, {
                logger
            });
            if (res.ok) {
                onSuccess === null || onSuccess === void 0 ? void 0 : onSuccess(index);
                logger === null || logger === void 0 ? void 0 : logger.debug(`Successfully fetched URL ${url}`);
                return res;
            }
            const errMsg = `Error fetching URL ${url} [${res.status}]`;
            if (isLast) throw new Error(errMsg);
            logger === null || logger === void 0 ? void 0 : logger.debug(`${errMsg}. ${notLastMsgSuffix}`);
        } catch (e) {
            const errMsg = `Error fetching URL ${url} [${e.message}]`;
            if (isLast) throw new Error(errMsg);
            logger === null || logger === void 0 ? void 0 : logger.debug(`${errMsg}. ${notLastMsgSuffix}`);
        }
    }
};
;
 //# sourceMappingURL=fetchWithFallbacks.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/staticResourcesMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getResourceUrl",
    ()=>getResourceUrl,
    "staticResourcesMixin",
    ()=>staticResourcesMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$projectIdMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/projectIdMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$baseUrlMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/baseUrlMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$fetchWithFallbacks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/fetchWithFallbacks.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
function getResourceUrl({ projectId, filename, assetsFolder = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ASSETS_FOLDER"], baseUrl = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL"] }) {
    const url = new URL(baseUrl);
    url.pathname = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["pathJoin"])(url.pathname, projectId, assetsFolder, filename);
    // we want to keep the baseUrl so we can use it later
    url.baseUrl = baseUrl;
    return url;
}
const staticResourcesMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _StaticResourcesMixinClass_instances, _StaticResourcesMixinClass_lastBaseUrl, _StaticResourcesMixinClass_workingBaseUrl, _StaticResourcesMixinClass_failedUrls, _StaticResourcesMixinClass_getResourceUrls, _StaticResourcesMixinClass_createResourceUrl, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$projectIdMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["projectIdMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$baseUrlMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["baseUrlMixin"])(superclass);
    // the logic should be as following:
    // 1. if there is a local storage override, use it
    // 2. otherwise, if there is a base-static-url attribute, use it
    // 3. otherwise, try to use base-url
    // 4. otherwise, try to use default content url (BASE_CONTENT_URL)
    // 5. otherwise, try to use the fallback content url (BASE_CONTENT_URL_FALLBACK)
    // For steps 3-5: if the url is valid, keep using it, and if not, don't try it anymore
    return _a = class StaticResourcesMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _StaticResourcesMixinClass_instances.add(this);
            _StaticResourcesMixinClass_lastBaseUrl.set(this, void 0);
            _StaticResourcesMixinClass_workingBaseUrl.set(this, void 0);
            _StaticResourcesMixinClass_failedUrls.set(this, new Set());
        }
        async fetchStaticResource(filename, format) {
            const resourceUrls = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_getResourceUrls).call(this, filename);
            // Cache the working URL and mark failed URLs
            const onSuccess = !Array.isArray(resourceUrls) ? null : (index)=>{
                const { baseUrl } = resourceUrls[index];
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _StaticResourcesMixinClass_workingBaseUrl, baseUrl, "f");
                // Mark all URLs before this one as failed
                for(let i = 0; i < index; i++){
                    const failedUrl = resourceUrls[i].baseUrl;
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_failedUrls, "f").add(failedUrl);
                }
            };
            try {
                const res = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$fetchWithFallbacks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fetchWithFallbacks"])(resourceUrls, {
                    cache: 'default'
                }, {
                    logger: this.logger,
                    onSuccess
                });
                return {
                    body: await res[format](),
                    headers: Object.fromEntries(res.headers.entries())
                };
            } catch (e) {
                this.logger.error(e.message);
            }
        }
        get baseStaticUrl() {
            return this.getAttribute('base-static-url') || '';
        }
    }, _StaticResourcesMixinClass_lastBaseUrl = new WeakMap(), _StaticResourcesMixinClass_workingBaseUrl = new WeakMap(), _StaticResourcesMixinClass_failedUrls = new WeakMap(), _StaticResourcesMixinClass_instances = new WeakSet(), _StaticResourcesMixinClass_getResourceUrls = function _StaticResourcesMixinClass_getResourceUrls(filename) {
        const overrideUrl = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["OVERRIDE_CONTENT_URL"] || this.baseStaticUrl;
        // Steps 1-2: Override URL or base-static-url takes precedence
        if (overrideUrl) {
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, overrideUrl);
        }
        // Reset state if base-url changed
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_lastBaseUrl, "f") !== this.baseUrl) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _StaticResourcesMixinClass_lastBaseUrl, this.baseUrl, "f");
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _StaticResourcesMixinClass_workingBaseUrl, undefined, "f");
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_failedUrls, "f").clear();
        }
        // Build URL list based on cached state or fallback chain
        const urls = [];
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_workingBaseUrl, "f")) {
            // Use cached working URL
            urls.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_workingBaseUrl, "f")));
            // Add fallback only for BASE_CONTENT_URL (for resilience)
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_workingBaseUrl, "f") === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL"]) {
                urls.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL_FALLBACK"]));
            }
        } else {
            // Build fallback chain: try URLs that haven't failed yet
            const baseUrlWithPages = this.baseUrl + '/pages';
            if (this.baseUrl && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_failedUrls, "f").has(baseUrlWithPages)) {
                urls.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, baseUrlWithPages));
            }
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_failedUrls, "f").has(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL"])) {
                urls.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL"]));
            }
            urls.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _StaticResourcesMixinClass_instances, "m", _StaticResourcesMixinClass_createResourceUrl).call(this, filename, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_CONTENT_URL_FALLBACK"]));
        }
        return urls.length === 1 ? urls[0] : urls;
    }, _StaticResourcesMixinClass_createResourceUrl = function _StaticResourcesMixinClass_createResourceUrl(filename, baseUrl) {
        return getResourceUrl({
            projectId: this.projectId,
            filename,
            baseUrl
        });
    }, _a;
});
;
 //# sourceMappingURL=staticResourcesMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CONFIG_FILENAME",
    ()=>CONFIG_FILENAME
]);
const CONFIG_FILENAME = 'config.json';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/resetMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "resetMixin",
    ()=>resetMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/commonValidators.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)");
;
;
;
;
const resetMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _ResetMixinClass_callbacks, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"])({
        'project-id': __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$commonValidators$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["missingAttrValidator"]
    }))(superclass);
    return _a = class ResetMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _ResetMixinClass_callbacks.set(this, new Map());
        }
        onReset(sectionId, callback) {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").has(sectionId)) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").set(sectionId, callback);
                return ()=>{
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").delete(sectionId);
                };
            } else {
                throw new Error(`Callback for sectionId ${sectionId} already exists`);
            }
        }
        async reset(...sectionIds) {
            if (sectionIds.length === 0) {
                await Promise.all(Array.from((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").values()).map((callback)=>callback()));
            } else {
                await Promise.all(sectionIds.map((sectionId)=>{
                    var _b;
                    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").has(sectionId)) {
                        throw new Error(`Callback for sectionId ${sectionId} does not exist`);
                    }
                    return (_b = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ResetMixinClass_callbacks, "f").get(sectionId)) === null || _b === void 0 ? void 0 : _b();
                }));
            }
        }
    }, _ResetMixinClass_callbacks = new WeakMap(), _a;
});
;
 //# sourceMappingURL=resetMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/configMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "configMixin",
    ()=>configMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/staticResourcesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$resetMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/resetMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
;
;
;
;
;
;
const configMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _ConfigMixinClass_instances, _ConfigMixinClass_configCacheClear, _ConfigMixinClass__configResource, _ConfigMixinClass_fetchConfig, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["staticResourcesMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$resetMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["resetMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"])(superclass);
    return _a = class ConfigMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _ConfigMixinClass_instances.add(this);
            _ConfigMixinClass__configResource.set(this, void 0);
            _ConfigMixinClass_fetchConfig.set(this, async ()=>{
                try {
                    const { body, headers } = await this.fetchStaticResource(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["CONFIG_FILENAME"], 'json');
                    return {
                        projectConfig: body,
                        executionContext: {
                            geo: headers['x-geo']
                        }
                    };
                } catch (e) {
                    this.logger.error('Cannot fetch config file', 'make sure that your projectId & flowId are correct');
                }
                return undefined;
            });
        }
        async init() {
            await super.init();
            this.onReset('config', (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ConfigMixinClass_instances, "m", _ConfigMixinClass_configCacheClear).bind(this));
        }
        get config() {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ConfigMixinClass__configResource, "f")) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _ConfigMixinClass__configResource, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ConfigMixinClass_fetchConfig, "f").call(this), "f");
            }
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ConfigMixinClass__configResource, "f");
        }
    }, _ConfigMixinClass__configResource = new WeakMap(), _ConfigMixinClass_fetchConfig = new WeakMap(), _ConfigMixinClass_instances = new WeakSet(), _ConfigMixinClass_configCacheClear = function _ConfigMixinClass_configCacheClear() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _ConfigMixinClass__configResource, undefined, "f");
    }, _a;
});
;
 //# sourceMappingURL=configMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/debuggerMixin/debuggerMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "debuggerMixin",
    ()=>debuggerMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
;
;
;
;
const debuggerMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _DebuggerMixinClass_instances, _DebuggerMixinClass_debuggerEle, _DebuggerMixinClass_disableDebugger, _DebuggerMixinClass_enableDebugger, _DebuggerMixinClass_handleDebugMode, _DebuggerMixinClass_updateDebuggerMessages, _a;
    return _a = class DebuggerMixinClass extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"])(superclass) {
        constructor(){
            super(...arguments);
            _DebuggerMixinClass_instances.add(this);
            _DebuggerMixinClass_debuggerEle.set(this, void 0);
            this.attributeChangedCallback = (attrName, oldValue, newValue)=>{
                var _b;
                (_b = super.attributeChangedCallback) === null || _b === void 0 ? void 0 : _b.call(this, attrName, oldValue, newValue);
                if (attrName === 'debug') {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_instances, "m", _DebuggerMixinClass_handleDebugMode).call(this);
                }
            };
        }
        get debug() {
            return this.getAttribute('debug') === 'true';
        }
        onLogEvent(logLevel, args) {
            var _b;
            (_b = super.onLogEvent) === null || _b === void 0 ? void 0 : _b.call(this, logLevel, args);
            if (logLevel === 'error') {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_instances, "m", _DebuggerMixinClass_updateDebuggerMessages).call(this, args[0] || 'Error', args[1]);
            }
        }
        async init() {
            var _b;
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_instances, "m", _DebuggerMixinClass_handleDebugMode).call(this);
        }
    }, _DebuggerMixinClass_debuggerEle = new WeakMap(), _DebuggerMixinClass_instances = new WeakSet(), _DebuggerMixinClass_disableDebugger = function _DebuggerMixinClass_disableDebugger() {
        var _b;
        (_b = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_debuggerEle, "f")) === null || _b === void 0 ? void 0 : _b.remove();
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _DebuggerMixinClass_debuggerEle, null, "f");
    }, _DebuggerMixinClass_enableDebugger = async function _DebuggerMixinClass_enableDebugger() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _DebuggerMixinClass_debuggerEle, document.createElement('descope-debugger'), "f");
        Object.assign((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_debuggerEle, "f").style, {
            position: 'fixed',
            top: '0',
            right: '0',
            height: '100vh',
            width: '100vw',
            pointerEvents: 'none',
            zIndex: 99999
        });
        // we are importing the debugger dynamically so we won't load it when it's not needed
        await __turbopack_context__.A("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/debuggerMixin/debugger-wc.js [app-client] (ecmascript, async loader)");
        document.body.appendChild((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_debuggerEle, "f"));
    }, _DebuggerMixinClass_handleDebugMode = function _DebuggerMixinClass_handleDebugMode() {
        if (this.debug) (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_instances, "m", _DebuggerMixinClass_enableDebugger).call(this);
        else (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_instances, "m", _DebuggerMixinClass_disableDebugger).call(this);
    }, _DebuggerMixinClass_updateDebuggerMessages = function _DebuggerMixinClass_updateDebuggerMessages(title, description) {
        var _b;
        if (title) (_b = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DebuggerMixinClass_debuggerEle, "f")) === null || _b === void 0 ? void 0 : _b.updateData({
            title,
            description
        });
    }, _a;
});
;
 //# sourceMappingURL=debuggerMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BASE_URLS",
    ()=>BASE_URLS
]);
const BASE_URLS = [
    'https://descopecdn.com',
    'https://static.descope.com',
    'https://cdn.jsdelivr.net'
];
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "generateLibUrls",
    ()=>generateLibUrls,
    "injectScriptWithFallbacks",
    ()=>injectScriptWithFallbacks
]);
const getExistingScript = (scriptId)=>{
    return document.querySelector(`script#${scriptId}`);
};
const isScriptLoaded = (script)=>{
    return script.getAttribute('status') === 'loaded';
};
const isScriptError = (script)=>{
    return script.getAttribute('status') === 'error';
};
const hashUrl = (url)=>{
    let hash = 0;
    const urlStr = url.toString();
    for(let i = 0; i < urlStr.length; i++){
        const char = urlStr.charCodeAt(i);
        hash = (hash << 5) - hash + char;
        hash = hash & hash; // Convert to 32-bit integer
    }
    return `${Math.abs(hash).toString()}`;
};
const setupScript = (id)=>{
    const scriptEle = document.createElement('script');
    scriptEle.id = id;
    return scriptEle;
};
const injectScript = (scriptId, url)=>{
    return new Promise((res, rej)=>{
        const scriptEle = setupScript(scriptId);
        scriptEle.onerror = (error)=>{
            scriptEle.setAttribute('status', 'error');
            rej(error);
        };
        scriptEle.onload = ()=>{
            scriptEle.setAttribute('status', 'loaded');
            res(scriptEle);
        };
        scriptEle.src = url.toString();
        document.body.appendChild(scriptEle);
    });
};
const handleExistingScript = (existingScript)=>{
    if (isScriptLoaded(existingScript)) {
        return Promise.resolve(existingScript);
    }
    if (isScriptError(existingScript)) {
        return Promise.reject();
    }
    return new Promise((res, rej)=>{
        existingScript.addEventListener('load', ()=>{
            res(existingScript);
        });
        existingScript.addEventListener('error', (error)=>{
            rej(error);
        });
    });
};
const injectScriptWithFallbacks = async (scriptsData, onError)=>{
    for (const scriptData of scriptsData){
        const { id, url } = scriptData;
        const existingScript = getExistingScript(id);
        if (existingScript) {
            try {
                await handleExistingScript(existingScript);
                return scriptData;
            } catch (e) {
                onError(scriptData, true);
            }
        } else {
            try {
                await injectScript(id, url);
                return scriptData;
            } catch (e) {
                onError(scriptData, false);
            }
        }
    }
    throw new Error('All scripts failed to load');
};
const generateLibUrls = (baseUrls, libName, version, path = '')=>baseUrls.reduce((prev, curr)=>{
        const baseUrl = curr;
        if (!baseUrl) {
            return prev;
        }
        let url;
        try {
            url = new URL(baseUrl);
        } catch (e) {
            throw new Error(`Invalid URL: ${baseUrl}`);
        }
        const isUrlIncludesPath = url.pathname !== '/';
        if (!isUrlIncludesPath) {
            url.pathname = `/npm/${libName}@${version}/${path}`;
        }
        return [
            ...prev,
            {
                url: url,
                id: `npmlib-${libName.replaceAll('@', '').replaceAll('/', '_')}-${hashUrl(url)}`
            }
        ];
    }, []);
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/injectNpmLibMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "injectNpmLibMixin",
    ()=>injectNpmLibMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/helpers.js [app-client] (ecmascript)");
;
;
;
;
const injectNpmLibMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"])(superclass);
    return class InjectNpmLibMixinClass extends BaseClass {
        get baseCdnUrl() {
            return this.getAttribute('base-cdn-url') || '';
        }
        injectNpmLib(libName, version, filePath = '', overrides = []) {
            this.logger.debug(`Injecting npm lib: "${libName}" with version: "${version}"`);
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["injectScriptWithFallbacks"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["generateLibUrls"])([
                ...overrides,
                this.baseCdnUrl,
                ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_URLS"]
            ], libName, version, filePath), (scriptData, existingScript)=>{
                if (existingScript) {
                    this.logger.error(`Existing script cannot be loaded: "${scriptData.url}"`);
                    return;
                }
                this.logger.error(`Cannot load script from URL, Make sure this URL is valid and return the correct script: "${scriptData.url}"`);
            });
        }
    };
});
;
 //# sourceMappingURL=injectNpmLibMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getDescopeUiComponentsList",
    ()=>getDescopeUiComponentsList
]);
const getDescopeUiComponentsList = (template)=>[
        ...Array.from(template.content.querySelectorAll('*')).reduce((acc, el)=>el.localName.startsWith('descope-') ? acc.add(el.localName) : acc, new Set())
    ];
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "JS_FILE_PATH",
    ()=>JS_FILE_PATH,
    "LOCAL_STORAGE_OVERRIDE",
    ()=>LOCAL_STORAGE_OVERRIDE,
    "UI_COMPONENTS_URL_KEY",
    ()=>UI_COMPONENTS_URL_KEY,
    "WEB_COMPONENTS_UI_LIB_NAME",
    ()=>WEB_COMPONENTS_UI_LIB_NAME
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/constants.js [app-client] (ecmascript)");
;
const UI_COMPONENTS_URL_KEY = 'base.ui.components.url';
const LOCAL_STORAGE_OVERRIDE = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IS_LOCAL_STORAGE"] && localStorage.getItem(UI_COMPONENTS_URL_KEY);
const JS_FILE_PATH = 'dist/umd/index.js';
const WEB_COMPONENTS_UI_LIB_NAME = '@descope/web-components-ui';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "descopeUiMixin",
    ()=>descopeUiMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$configMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/configMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$injectNpmLibMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/injectNpmLibMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/helpers.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/constants.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
const descopeUiMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _DescopeUiMixinClass_instances, _DescopeUiMixinClass_getComponentsVersion, _DescopeUiMixinClass_descopeUi, _DescopeUiMixinClass_loadDescopeUiComponent, _DescopeUiMixinClass_getDescopeUi, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$configMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["configMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$injectNpmLibMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["injectNpmLibMixin"])(superclass);
    return _a = class DescopeUiMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _DescopeUiMixinClass_instances.add(this);
            _DescopeUiMixinClass_descopeUi.set(this, void 0);
        }
        get descopeUi() {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DescopeUiMixinClass_descopeUi, "f")) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _DescopeUiMixinClass_descopeUi, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DescopeUiMixinClass_instances, "m", _DescopeUiMixinClass_getDescopeUi).call(this), "f");
            }
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DescopeUiMixinClass_descopeUi, "f");
        }
        async loadDescopeUiComponents(templateOrComponentNames) {
            const descopeUiComponentsList = Array.isArray(templateOrComponentNames) ? templateOrComponentNames : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDescopeUiComponentsList"])(templateOrComponentNames);
            return Promise.all(descopeUiComponentsList.map((componentName)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DescopeUiMixinClass_instances, "m", _DescopeUiMixinClass_loadDescopeUiComponent).call(this, componentName)));
        }
    }, _DescopeUiMixinClass_descopeUi = new WeakMap(), _DescopeUiMixinClass_instances = new WeakSet(), _DescopeUiMixinClass_getComponentsVersion = // eslint-disable-next-line class-methods-use-this
    async function _DescopeUiMixinClass_getComponentsVersion() {
        var _b;
        const config = await this.config;
        const componentsVersion = (_b = config === null || config === void 0 ? void 0 : config.projectConfig) === null || _b === void 0 ? void 0 : _b.componentsVersion;
        if (!componentsVersion) {
            this.logger.error('Could not get components version');
        } else {
            this.logger.debug(`Got component version "${componentsVersion}"`);
        }
        return componentsVersion;
    }, _DescopeUiMixinClass_loadDescopeUiComponent = async function _DescopeUiMixinClass_loadDescopeUiComponent(componentName) {
        const isComponentAlreadyDefined = !!customElements.get(componentName);
        if (isComponentAlreadyDefined) {
            this.logger.debug(`Loading component "${componentName}" is skipped as it is already defined`);
            return undefined;
        }
        const descopeUI = await this.descopeUi;
        if (!descopeUI[componentName]) {
            this.logger.error(`Cannot load UI component "${componentName}"`, `Descope UI does not have a component named "${componentName}", available components are: "${Object.keys(descopeUI).join(', ')}"`);
            return undefined;
        }
        try {
            // eslint-disable-next-line @typescript-eslint/return-await
            return await descopeUI[componentName]();
        } catch (e) {
            // this error is thrown when trying to register a component which is already registered
            // when running 2 flows on the same page, it might happen that the register fn is called twice
            // in case it happens, we are silently ignore the error
            if (e.name === 'NotSupportedError') {
                // eslint-disable-next-line no-console
                console.debug(`Encountered an error while attempting to define the "${componentName}" component, it is likely that this component is already defined`);
            } else {
                throw e;
            }
        }
        return undefined;
    }, _DescopeUiMixinClass_getDescopeUi = async function _DescopeUiMixinClass_getDescopeUi() {
        if (globalThis.DescopeUI) {
            return globalThis.DescopeUI;
        }
        try {
            await this.injectNpmLib(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["WEB_COMPONENTS_UI_LIB_NAME"], await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _DescopeUiMixinClass_instances, "m", _DescopeUiMixinClass_getComponentsVersion).call(this), __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["JS_FILE_PATH"], [
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LOCAL_STORAGE_OVERRIDE"]
            ]);
            this.logger.debug('DescopeUI was loaded');
            return globalThis.DescopeUI;
        } catch (error) {
            this.logger.error(error);
            throw new Error('DescopeUI was not loaded');
        }
    }, _a;
});
;
 //# sourceMappingURL=descopeUiMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cspNonceMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cspNonceMixin",
    ()=>cspNonceMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)");
;
;
;
;
const cspNonceMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _CspNonceMixinClass_instances, _CspNonceMixinClass_setNonce, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["observeAttributesMixin"])(superclass);
    return _a = class CspNonceMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _CspNonceMixinClass_instances.add(this);
        }
        get nonce() {
            return this.getAttribute('nonce') || '';
        }
        async init() {
            var _b;
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            this.observeAttribute('nonce', (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _CspNonceMixinClass_instances, "m", _CspNonceMixinClass_setNonce).bind(this));
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _CspNonceMixinClass_instances, "m", _CspNonceMixinClass_setNonce).call(this);
        }
    }, _CspNonceMixinClass_instances = new WeakSet(), _CspNonceMixinClass_setNonce = function _CspNonceMixinClass_setNonce() {
        if (this.nonce) {
            window.DESCOPE_NONCE = this.nonce;
        }
    }, _a;
});
;
 //# sourceMappingURL=cspNonceMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectStyleMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "injectStyleMixin",
    ()=>injectStyleMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cspNonceMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cspNonceMixin.js [app-client] (ecmascript)");
;
;
// we should mimic the CSSStyleSheet API for the fns we are using
class CSSStyleSheetMock {
    constructor(ref, nonce, { prepend = false } = {}){
        this.styleEle = document.createElement('style');
        this.styleEle.setAttribute('nonce', nonce);
        this.ref = ref;
        if (!this.ref) {
            return;
        }
        if (prepend) {
            this.ref.prepend(this.styleEle);
        } else {
            this.ref.append(this.styleEle);
        }
    }
    replaceSync(cssString) {
        this.styleEle.textContent = cssString;
    }
    get cssRules() {
        var _a;
        return (_a = this.styleEle.sheet) === null || _a === void 0 ? void 0 : _a.cssRules;
    }
}
const injectStyleMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cspNonceMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["cspNonceMixin"])(superclass);
    return class InjectStyleMixinClass extends BaseClass {
        injectStyle(cssString, { prepend = false } = {}) {
            let style;
            try {
                style = new CSSStyleSheet();
            } catch (e) {
                // fallback for browsers that don't support CSSStyleSheet
                style = new CSSStyleSheetMock(this.shadowRoot, this.nonce, {
                    prepend
                });
            }
            if (cssString) {
                style.replaceSync(cssString);
            }
            if (style instanceof CSSStyleSheet) {
                const ref = this.shadowRoot;
                if (ref && 'adoptedStyleSheets' in ref) {
                    const adoptedStyleSheets = [
                        ...ref.adoptedStyleSheets || []
                    ];
                    adoptedStyleSheets[prepend ? 'unshift' : 'push'](style);
                    ref.adoptedStyleSheets = adoptedStyleSheets;
                }
            }
            return style;
        }
    };
});
;
 //# sourceMappingURL=injectStyleMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "initElementMixin",
    ()=>initElementMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectStyleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectStyleMixin.js [app-client] (ecmascript)");
;
;
const CONTENT_ROOT_ID = 'content-root';
const ROOT_ID = 'root';
const initElementMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectStyleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["injectStyleMixin"])(superclass);
    return class InitElementMixinClass extends BaseClass {
        constructor(...rest){
            var _a, _b;
            super(...rest);
            this.attachShadow({
                mode: 'open'
            }).innerHTML = `
          <div id="${ROOT_ID}">
            <div id="${CONTENT_ROOT_ID}"></div>
          </div>
          `;
            this.injectStyle(`
            #${ROOT_ID}, #${CONTENT_ROOT_ID} {
              height: 100%;
            }
            #${ROOT_ID} {
              position: relative;
              height: fit-content;
            }
          `);
            this.contentRootElement = (_a = this.shadowRoot) === null || _a === void 0 ? void 0 : _a.getElementById(CONTENT_ROOT_ID);
            this.rootElement = (_b = this.shadowRoot) === null || _b === void 0 ? void 0 : _b.getElementById(ROOT_ID);
        }
    };
});
;
 //# sourceMappingURL=initElementMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MODAL_ELE_TAG",
    ()=>MODAL_ELE_TAG
]);
const MODAL_ELE_TAG = 'descope-modal';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createModalEle",
    ()=>createModalEle
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/constants.js [app-client] (ecmascript)");
;
const createModalEle = (config = {})=>{
    const modal = document.createElement(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODAL_ELE_TAG"]);
    Object.keys(config).forEach((key)=>{
        modal.setAttribute(key, config[key]);
    });
    return modal;
};
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-component-drivers/dist/index.esm.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AppsListDriver",
    ()=>n,
    "AvatarDriver",
    ()=>o,
    "ButtonDriver",
    ()=>d,
    "DeviceListDriver",
    ()=>N,
    "FlowButtonDriver",
    ()=>C,
    "FlowDriver",
    ()=>u,
    "GenericFlowButtonDriver",
    ()=>a,
    "GridDriver",
    ()=>h,
    "LinkDriver",
    ()=>m,
    "ModalDriver",
    ()=>p,
    "MultiSelectDriver",
    ()=>b,
    "NotificationDriver",
    ()=>f,
    "OutboundAppsListDriver",
    ()=>A,
    "SingleSelectDriver",
    ()=>E,
    "TenantSelectorDriver",
    ()=>I,
    "TextDriver",
    ()=>w,
    "TextFieldDriver",
    ()=>x,
    "UserAttributeDriver",
    ()=>L,
    "UserAuthMethodDriver",
    ()=>k
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
;
;
var i, r;
class s {
    constructor(t, l){
        i.set(this, void 0), this.nodeName = "", (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, i, t, "f"), this.logger = l.logger;
    }
    get asyncEle() {
        return (async (e, t)=>new Promise((l)=>{
                const i = setInterval(()=>{
                    const t = "function" == typeof e ? e() : e;
                    t && (clearInterval(i), l(t));
                }, 100);
                setTimeout(()=>{
                    clearInterval(i), l(null);
                }, t);
            }))((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f"), 1e3);
    }
    get ele() {
        var e, l;
        const r = "function" == typeof (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f") ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f").call(this) : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f");
        return r ? (null == r ? void 0 : r.localName) !== this.nodeName ? (null === (l = this.logger) || void 0 === l || l.debug(`node name do not match, expected "${this.nodeName}", received "${r.localName}" `, Error()), null) : r : (null === (e = this.logger) || void 0 === e || e.debug(`Driver element is not available for ${this.nodeName}`, new Error), null);
    }
    get isExists() {
        return !!this.ele;
    }
}
i = new WeakMap;
class n extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-apps-list";
    }
    set data(e) {
        this.ele && (this.ele.data = e);
    }
    get data() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.data;
    }
    get ele() {
        return super.ele;
    }
}
class o extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-avatar";
    }
    set displayName(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("display-name", e);
    }
    set image(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("img", e);
    }
    get flowId() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("flow-id");
    }
    onClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("click", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("click", e);
        };
    }
}
class d extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-button";
    }
    onClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("click", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("click", e);
        };
    }
    disable() {
        var e;
        null === (e = this.ele) || void 0 === e || e.setAttribute("disabled", "true");
    }
    enable() {
        var e;
        null === (e = this.ele) || void 0 === e || e.removeAttribute("disabled");
    }
}
class u extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-wc";
    }
    set projectId(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("project-id", e);
    }
    set baseUrl(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("base-url", e);
    }
    set flowId(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("flow-id", e);
    }
    set theme(e) {
        var t, l;
        e ? null === (l = this.ele) || void 0 === l || l.setAttribute("theme", e) : null === (t = this.ele) || void 0 === t || t.removeAttribute("theme");
    }
    onSuccess(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("success", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("success", e);
        };
    }
    onPageUpdated(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("page-updated", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("page-updated", e);
        };
    }
}
class a extends d {
    get flowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("flow-id")) || "";
    }
    get enableMode() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("enable-mode");
    }
}
const v = /^descope-grid-([^-]+)-column$/, c = {
    text: class extends s {
        constructor(){
            super(...arguments), this.nodeName = "descope-grid-text-column";
        }
        onSortDirectionChange(e) {
            var t;
            return null === (t = this.ele) || void 0 === t || t.addEventListener("direction-changed", e), ()=>{
                var t;
                return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("selected-items-changed", e);
            };
        }
    },
    custom: class extends s {
        constructor(){
            super(...arguments), this.nodeName = "descope-grid-custom-column";
        }
        onSortDirectionChange(e) {
            var t;
            return null === (t = this.ele) || void 0 === t || t.addEventListener("direction-changed", e), ()=>{
                var t;
                return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("selected-items-changed", e);
            };
        }
    }
};
class h extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-grid", r.set(this, void 0);
    }
    onSelectedItemsChange(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("selected-items-changed", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("selected-items-changed", e);
        };
    }
    get ele() {
        return super.ele;
    }
    get data() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.data;
    }
    set data(e) {
        this.ele && (this.ele.data = e);
    }
    get columns() {
        return this.ele ? Array.from(this.ele.children).reduce((e, t)=>{
            var l;
            const i = null === (l = v.exec(t.localName)) || void 0 === l ? void 0 : l[1], r = c[i];
            return r ? (e.push(new r(t, {
                logger: this.logger
            })), e) : e;
        }, []) : [];
    }
    filterColumns(e) {
        var i, s;
        const n = null === (i = this.ele.columns) || void 0 === i ? void 0 : i.filter(e);
        n && this.ele.columns && ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compareArrays"])(n, this.ele.columns) || (this.ele.columns = n, null === (s = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, r, "f")) || void 0 === s || s.call(this, n)));
    }
    onColumnsChange(t) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, r, t, "f");
    }
    set renderColumn(e) {
        this.ele.renderColumn = e;
    }
}
r = new WeakMap;
class m extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-link";
    }
    get ele() {
        return super.ele;
    }
    get href() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("href");
    }
    set href(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("href", null != e ? e : "");
    }
}
var g;
class p extends s {
    constructor(){
        super(...arguments), g.set(this, void 0), this.nodeName = "descope-modal";
    }
    close() {
        var e, t;
        null === (e = this.ele) || void 0 === e || e.removeAttribute("opened"), null === (t = this.afterClose) || void 0 === t || t.call(this);
    }
    async open() {
        var e, t;
        await (null === (e = this.beforeOpen) || void 0 === e ? void 0 : e.call(this)), null === (t = this.ele) || void 0 === t || t.setAttribute("opened", "true");
    }
    reset() {
        var e;
        this.ele && (this.ele.innerHTML = ""), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, g, "f") && (null === (e = this.ele) || void 0 === e || e.append((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, g, "f").content.cloneNode(!0)));
    }
    setContent(t) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, g, t, "f"), this.reset();
    }
    remove() {
        var e;
        null === (e = this.ele) || void 0 === e || e.remove();
    }
}
g = new WeakMap;
class b extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-multi-select-combo-box";
    }
    async setData(e) {
        var t;
        null === (t = await this.asyncEle) || void 0 === t || t.setAttribute("data", JSON.stringify(e.sort()));
    }
}
class f extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-notification";
    }
    close() {
        var e;
        null === (e = this.ele) || void 0 === e || e.removeAttribute("opened");
    }
    show() {
        var e;
        null === (e = this.ele) || void 0 === e || e.setAttribute("opened", "true");
    }
    setContent(e) {
        this.ele.innerHTML = "", this.ele.appendChild(e.content.cloneNode(!0));
    }
    remove() {
        var e;
        null === (e = this.ele) || void 0 === e || e.remove();
    }
}
class A extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-outbound-apps";
    }
    set data(e) {
        this.ele && (this.ele.data = e);
    }
    get data() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.data;
    }
    get ele() {
        return super.ele;
    }
    get connectFlowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("connect-flow-id")) || "";
    }
    get disconnectFlowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("disconnect-flow-id")) || "";
    }
    onConnectClick(e) {
        var t;
        const l = (t)=>e(t.detail);
        return null === (t = this.ele) || void 0 === t || t.addEventListener("connect-clicked", l), ()=>{
            var e;
            return null === (e = this.ele) || void 0 === e ? void 0 : e.removeEventListener("connect-clicked", l);
        };
    }
    onDisconnectClick(e) {
        var t;
        const l = (t)=>e(t.detail);
        return null === (t = this.ele) || void 0 === t || t.addEventListener("disconnect-clicked", l), ()=>{
            var e;
            return null === (e = this.ele) || void 0 === e ? void 0 : e.removeEventListener("disconnect-clicked", l);
        };
    }
}
class E extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-combo-box";
    }
    onInput(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("input", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("input", e);
        };
    }
    get value() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.value;
    }
    set value(e) {
        this.ele && (this.ele.value = e);
    }
    async setData(e) {
        var t;
        null === (t = await this.asyncEle) || void 0 === t || t.setAttribute("data", JSON.stringify(e.sort()));
    }
    setAllowCustomValue(e) {
        this.ele && this.ele.toggleAttribute("allow-custom-value", e);
    }
}
class w extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-text";
    }
    get ele() {
        return super.ele;
    }
    get text() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.innerText;
    }
    set text(e) {
        this.ele && (this.ele.innerText = e);
    }
}
class x extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-text-field";
    }
    onInput(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("input", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("input", e);
        };
    }
    get value() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.value;
    }
    set value(e) {
        this.ele && (this.ele.value = e);
    }
    disable() {
        var e;
        null === (e = this.ele) || void 0 === e || e.setAttribute("disabled", "true");
    }
    enable() {
        var e;
        null === (e = this.ele) || void 0 === e || e.removeAttribute("disabled");
    }
}
class L extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-user-attribute";
    }
    set value(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("value", e);
    }
    set badgeLabel(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("badge-label", e);
    }
    get label() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("label")) || "";
    }
    get editFlowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("edit-flow-id")) || "";
    }
    get deleteFlowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("delete-flow-id")) || "";
    }
    onEditClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("edit-clicked", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("edit-clicked", e);
        };
    }
    onDeleteClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("delete-clicked", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("delete-clicked", e);
        };
    }
}
class k extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-user-auth-method";
    }
    set fulfilled(e) {
        var t, l;
        e ? null === (t = this.ele) || void 0 === t || t.setAttribute("fulfilled", "true") : null === (l = this.ele) || void 0 === l || l.removeAttribute("fulfilled");
    }
    onUnfulfilledButtonClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("button-clicked", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("button-clicked", e);
        };
    }
    onFulfilledButtonClick(e) {
        var t;
        return null === (t = this.ele) || void 0 === t || t.addEventListener("fulfilled-button-clicked", e), ()=>{
            var t;
            return null === (t = this.ele) || void 0 === t ? void 0 : t.removeEventListener("fulfilled-button-clicked", e);
        };
    }
    get flowId() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("flow-id");
    }
    get fulfilledFlowId() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("fulfilled-flow-id");
    }
}
class N extends s {
    constructor(){
        super(...arguments), this.nodeName = "descope-trusted-devices";
    }
    set displayName(e) {
        var t;
        null === (t = this.ele) || void 0 === t || t.setAttribute("display-name", e);
    }
    get ele() {
        return super.ele;
    }
    get data() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.data;
    }
    set data(e) {
        this.ele && (this.ele.data = e);
    }
    get flowId() {
        var e;
        return null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("flow-id");
    }
    onRemoveDeviceClick(e) {
        var t;
        const l = (t)=>e(t.detail);
        return null === (t = this.ele) || void 0 === t || t.addEventListener("remove-device-clicked", l), ()=>{
            var e;
            return null === (e = this.ele) || void 0 === e ? void 0 : e.removeEventListener("remove-device-clicked", l);
        };
    }
}
class C extends d {
    get flowId() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("flow-id")) || "";
    }
}
class I extends E {
    get action() {
        var e;
        return (null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("data-action")) || "";
    }
    get refreshTimeout() {
        var e;
        return Number(null === (e = this.ele) || void 0 === e ? void 0 : e.getAttribute("data-refresh-timeout")) || 500;
    }
    get shouldReload() {
        return "reload" === this.action;
    }
}
;
 //# sourceMappingURL=index.esm.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/modalMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "modalMixin",
    ()=>modalMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/helpers.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-component-drivers/dist/index.esm.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
const modalMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _ModalMixinClass_ModalDriverWrapper, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initElementMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["descopeUiMixin"])(superclass);
    return _a = class ModalMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _ModalMixinClass_ModalDriverWrapper.set(this, (()=>{
                const loadDescopeUiComponents = this.loadDescopeUiComponents.bind(this);
                return class ModalDriverWrapper extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ModalDriver"] {
                    setContent(template) {
                        loadDescopeUiComponents(template);
                        super.setContent(template);
                    }
                };
            })());
        }
        createModal(config) {
            const baseConfig = {};
            const modal = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createModalEle"])(Object.assign(Object.assign({}, baseConfig), config));
            this.rootElement.append(modal);
            return new ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ModalMixinClass_ModalDriverWrapper, "f"))(modal, {
                logger: this.logger
            });
        }
        async init() {
            var _b;
            this.loadDescopeUiComponents([
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODAL_ELE_TAG"]
            ]);
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
        }
    }, _ModalMixinClass_ModalDriverWrapper = new WeakMap(), _a;
});
;
 //# sourceMappingURL=modalMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "NOTIFICATION_ELE_TAG",
    ()=>NOTIFICATION_ELE_TAG
]);
const NOTIFICATION_ELE_TAG = 'descope-notification';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createNotificationEle",
    ()=>createNotificationEle
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/constants.js [app-client] (ecmascript)");
;
const createNotificationEle = (config = {})=>{
    const notification = document.createElement(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["NOTIFICATION_ELE_TAG"]);
    Object.keys(config).forEach((key)=>{
        notification.setAttribute(key, config[key]);
    });
    return notification;
};
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/notificationsMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "notificationsMixin",
    ()=>notificationsMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/helpers.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-component-drivers/dist/index.esm.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
const notificationsMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _NotificationsMixinClass_NotificationDriverWrapper, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initElementMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["descopeUiMixin"])(superclass);
    return _a = class NotificationsMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _NotificationsMixinClass_NotificationDriverWrapper.set(this, (()=>{
                const loadDescopeUiComponents = this.loadDescopeUiComponents.bind(this);
                return class NotificationDriverWrapper extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["NotificationDriver"] {
                    setContent(templateOrString) {
                        const template = typeof templateOrString === 'string' ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createTemplate"])(templateOrString) : templateOrString;
                        loadDescopeUiComponents(template);
                        super.setContent(template);
                    }
                };
            })());
        }
        createNotification(config) {
            const baseConfig = {};
            const notification = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createNotificationEle"])(Object.assign(Object.assign({}, baseConfig), config));
            this.rootElement.append(notification);
            return new ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _NotificationsMixinClass_NotificationDriverWrapper, "f"))(notification, {
                logger: this.logger
            });
        }
        async init() {
            var _b;
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            this.loadDescopeUiComponents([
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["NOTIFICATION_ELE_TAG"]
            ]);
        }
    }, _NotificationsMixinClass_NotificationDriverWrapper = new WeakMap(), _a;
});
;
 //# sourceMappingURL=notificationsMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_STYLE_ID",
    ()=>DEFAULT_STYLE_ID
]);
// Those files are saved on a new folder to prevent breaking changes
const DEFAULT_STYLE_ID = 'theme';
;
 //# sourceMappingURL=constants.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/helpers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "loadDevTheme",
    ()=>loadDevTheme,
    "loadFont",
    ()=>loadFont
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/constants.js [app-client] (ecmascript)");
;
const loadFont = (url)=>{
    const font = document.createElement('link');
    font.href = url;
    font.rel = 'stylesheet';
    document.head.appendChild(font);
};
const loadDevTheme = async ()=>{
    const componentsUrl = localStorage.getItem(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["UI_COMPONENTS_URL_KEY"]);
    const descopeDevUrl = componentsUrl === null || componentsUrl === void 0 ? void 0 : componentsUrl.replace(/[^\/]+$/, 'DescopeDev.js');
    // eslint-disable-next-line no-console
    console.warn('Trying to load DescopeDev.js from', descopeDevUrl);
    const scriptEle = document.createElement('script');
    scriptEle.src = descopeDevUrl;
    document.body.appendChild(scriptEle);
    await new Promise((resolve, reject)=>{
        scriptEle.onload = resolve;
        scriptEle.onerror = reject;
    });
    if (globalThis.DescopeDev) {
        const { themeToStyle, defaultTheme, darkTheme } = globalThis.DescopeDev;
        return {
            light: themeToStyle(defaultTheme),
            dark: themeToStyle(darkTheme)
        };
    }
};
;
 //# sourceMappingURL=helpers.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/themeMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "themeMixin",
    ()=>themeMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$configMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/configMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/staticResourcesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/helpers.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectStyleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectStyleMixin.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
;
;
const themeValidation = (_, theme)=>(theme || false) && theme !== 'light' && theme !== 'dark' && 'Supported theme values are "light", "dark", or leave empty for using the OS theme';
const themeMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    var _ThemeMixinClass_instances, _ThemeMixinClass_globalStyle, _ThemeMixinClass__themeResource, _ThemeMixinClass_fetchTheme, _ThemeMixinClass_themeResource_get, _ThemeMixinClass_loadGlobalStyle, _ThemeMixinClass_loadComponentsStyle, _ThemeMixinClass_getFontsConfig, _ThemeMixinClass_loadFonts, _ThemeMixinClass_applyTheme, _ThemeMixinClass_onThemeChange, _ThemeMixinClass_loadTheme, _ThemeMixinClass_toggleOsThemeChangeListener, _a;
    const BaseClass = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"])({
        theme: themeValidation
    }), __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["staticResourcesMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["descopeUiMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$configMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["configMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initElementMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["observeAttributesMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectStyleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["injectStyleMixin"])(superclass);
    return _a = class ThemeMixinClass extends BaseClass {
        constructor(){
            super(...arguments);
            _ThemeMixinClass_instances.add(this);
            _ThemeMixinClass_globalStyle.set(this, void 0);
            _ThemeMixinClass__themeResource.set(this, void 0);
            _ThemeMixinClass_onThemeChange.set(this, ()=>{
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadTheme).call(this);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_toggleOsThemeChangeListener, "f").call(this, this.getAttribute('theme') === 'os');
            });
            // add or remove os theme change listener
            _ThemeMixinClass_toggleOsThemeChangeListener.set(this, (listen)=>{
                var _b, _c, _d;
                const method = listen ? 'addEventListener' : 'removeEventListener';
                (_d = (_c = (_b = window.matchMedia) === null || _b === void 0 ? void 0 : _b.call(window, '(prefers-color-scheme: dark)')) === null || _c === void 0 ? void 0 : _c[method]) === null || _d === void 0 ? void 0 : _d.call(_c, 'change', ()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadTheme).call(this));
            });
        }
        get theme() {
            var _b, _c;
            const theme = this.getAttribute('theme');
            if (theme === 'os') {
                const isOsDark = window.matchMedia && ((_c = (_b = window.matchMedia) === null || _b === void 0 ? void 0 : _b.call(window, '(prefers-color-scheme: dark)')) === null || _c === void 0 ? void 0 : _c.matches);
                return isOsDark ? 'dark' : 'light';
            }
            return theme || 'light';
        }
        get styleId() {
            return this.getAttribute('style-id') || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_STYLE_ID"];
        }
        async init() {
            var _b;
            await ((_b = super.init) === null || _b === void 0 ? void 0 : _b.call(this));
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_onThemeChange, "f").call(this);
            await Promise.all([
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadGlobalStyle).call(this),
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadComponentsStyle).call(this)
            ]);
            this.observeAttributes([
                'theme'
            ], (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_onThemeChange, "f"));
            this.observeAttributes([
                'style-id'
            ], ()=>{
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _ThemeMixinClass__themeResource, null, "f");
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadFonts).call(this);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadGlobalStyle).call(this);
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadComponentsStyle).call(this);
            });
        }
    }, _ThemeMixinClass_globalStyle = new WeakMap(), _ThemeMixinClass__themeResource = new WeakMap(), _ThemeMixinClass_onThemeChange = new WeakMap(), _ThemeMixinClass_toggleOsThemeChangeListener = new WeakMap(), _ThemeMixinClass_instances = new WeakSet(), _ThemeMixinClass_fetchTheme = async function _ThemeMixinClass_fetchTheme() {
        try {
            const { body: fetchedTheme } = await this.fetchStaticResource(`${this.styleId}.json`, 'json');
            // In development mode, we sometimes want to override the UI components URL
            // The override components might have a different theme, so we need to merge it with the project theme in order to see the components correctly
            if ("TURBOPACK compile-time truthy", 1) {
                if (localStorage === null || localStorage === void 0 ? void 0 : localStorage.getItem(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["UI_COMPONENTS_URL_KEY"])) {
                    try {
                        this.logger.warn('You are in DEV mode, and UI components override URL was found\ntrying to merge project theme with the default theme of the UI components');
                        const devTheme = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loadDevTheme"])();
                        if (devTheme) {
                            fetchedTheme.light.components = Object.assign(Object.assign({}, fetchedTheme.light.components), devTheme.light.components);
                            fetchedTheme.dark.components = Object.assign(Object.assign({}, fetchedTheme.dark.components), devTheme.dark.components);
                            this.logger.warn('Theme was merged successfully');
                            // eslint-disable-next-line no-console
                            console.log('%cNOTICE! This is not the theme that will be used in production!\n\nMake sure to test it without the override UI components URL!', 'color: black; background-color:yellow; font-size: x-large');
                        }
                    } catch (e) {
                        this.logger.error('Failed to merge UI components theme\n', e);
                    }
                }
            }
            return fetchedTheme;
        } catch (e) {
            this.logger.error('Cannot fetch theme file', 'make sure that your projectId & flowId are correct');
        }
        return undefined;
    }, _ThemeMixinClass_themeResource_get = function _ThemeMixinClass_themeResource_get() {
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass__themeResource, "f")) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _ThemeMixinClass__themeResource, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_fetchTheme).call(this), "f");
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass__themeResource, "f").then((theme)=>this.logger.debug('Fetched theme', theme));
        }
        // eslint-disable-next-line no-underscore-dangle
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass__themeResource, "f");
    }, _ThemeMixinClass_loadGlobalStyle = async function _ThemeMixinClass_loadGlobalStyle() {
        var _b, _c;
        const theme = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "a", _ThemeMixinClass_themeResource_get);
        if (!theme) return;
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_globalStyle, "f")) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, _ThemeMixinClass_globalStyle, this.injectStyle(''), "f");
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_globalStyle, "f").replaceSync((((_b = theme === null || theme === void 0 ? void 0 : theme.light) === null || _b === void 0 ? void 0 : _b.globals) || '') + (((_c = theme === null || theme === void 0 ? void 0 : theme.dark) === null || _c === void 0 ? void 0 : _c.globals) || ''));
    }, _ThemeMixinClass_loadComponentsStyle = async function _ThemeMixinClass_loadComponentsStyle() {
        var _b, _c;
        const theme = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "a", _ThemeMixinClass_themeResource_get);
        if (!theme) return;
        const descopeUi = await this.descopeUi;
        if (descopeUi === null || descopeUi === void 0 ? void 0 : descopeUi.componentsThemeManager) {
            descopeUi.componentsThemeManager.themes = {
                light: (_b = theme === null || theme === void 0 ? void 0 : theme.light) === null || _b === void 0 ? void 0 : _b.components,
                dark: (_c = theme === null || theme === void 0 ? void 0 : theme.dark) === null || _c === void 0 ? void 0 : _c.components
            };
        }
    }, _ThemeMixinClass_getFontsConfig = async function _ThemeMixinClass_getFontsConfig() {
        var _b, _c;
        const { projectConfig } = await this.config || {};
        const newConfig = (_b = projectConfig === null || projectConfig === void 0 ? void 0 : projectConfig.styles) === null || _b === void 0 ? void 0 : _b[this.styleId];
        const oldConfig = projectConfig === null || projectConfig === void 0 ? void 0 : projectConfig.cssTemplate;
        const config = newConfig || oldConfig;
        const fonts = (_c = config === null || config === void 0 ? void 0 : config[this.theme]) === null || _c === void 0 ? void 0 : _c.fonts;
        return fonts;
    }, _ThemeMixinClass_loadFonts = async function _ThemeMixinClass_loadFonts() {
        const fonts = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_getFontsConfig).call(this);
        if (fonts) {
            Object.values(fonts).forEach((font)=>{
                if (font.url) {
                    this.logger.debug(`Loading font from URL "${font.url}"`);
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$helpers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loadFont"])(font.url);
                }
            });
        } else {
            this.logger.debug('No fonts to load');
        }
    }, _ThemeMixinClass_applyTheme = async function _ThemeMixinClass_applyTheme() {
        this.rootElement.setAttribute('data-theme', this.theme);
        const descopeUi = await this.descopeUi;
        if (descopeUi === null || descopeUi === void 0 ? void 0 : descopeUi.componentsThemeManager) {
            descopeUi.componentsThemeManager.currentThemeName = this.theme;
        }
    }, _ThemeMixinClass_loadTheme = function _ThemeMixinClass_loadTheme() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_loadFonts).call(this);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, _ThemeMixinClass_instances, "m", _ThemeMixinClass_applyTheme).call(this);
    }, _a;
});
;
 //# sourceMappingURL=themeMixin.js.map
}),
"[project]/node_modules/@reduxjs/toolkit/node_modules/immer/dist/immer.mjs [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Immer",
    ()=>Immer2,
    "applyPatches",
    ()=>applyPatches,
    "castDraft",
    ()=>castDraft,
    "castImmutable",
    ()=>castImmutable,
    "createDraft",
    ()=>createDraft,
    "current",
    ()=>current,
    "enableArrayMethods",
    ()=>enableArrayMethods,
    "enableMapSet",
    ()=>enableMapSet,
    "enablePatches",
    ()=>enablePatches,
    "finishDraft",
    ()=>finishDraft,
    "freeze",
    ()=>freeze,
    "immerable",
    ()=>DRAFTABLE,
    "isDraft",
    ()=>isDraft,
    "isDraftable",
    ()=>isDraftable,
    "nothing",
    ()=>NOTHING,
    "original",
    ()=>original,
    "produce",
    ()=>produce,
    "produceWithPatches",
    ()=>produceWithPatches,
    "setAutoFreeze",
    ()=>setAutoFreeze,
    "setUseStrictIteration",
    ()=>setUseStrictIteration,
    "setUseStrictShallowCopy",
    ()=>setUseStrictShallowCopy
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [app-client] (ecmascript)");
// src/utils/env.ts
var NOTHING = Symbol.for("immer-nothing");
var DRAFTABLE = Symbol.for("immer-draftable");
var DRAFT_STATE = Symbol.for("immer-state");
// src/utils/errors.ts
var errors = ("TURBOPACK compile-time truthy", 1) ? [
    // All error codes, starting by 0:
    function(plugin) {
        return `The plugin for '${plugin}' has not been loaded into Immer. To enable the plugin, import and call \`enable${plugin}()\` when initializing your application.`;
    },
    function(thing) {
        return `produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '${thing}'`;
    },
    "This object has been frozen and should not be mutated",
    function(data) {
        return "Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? " + data;
    },
    "An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",
    "Immer forbids circular references",
    "The first or second argument to `produce` must be a function",
    "The third argument to `produce` must be a function or undefined",
    "First argument to `createDraft` must be a plain object, an array, or an immerable object",
    "First argument to `finishDraft` must be a draft returned by `createDraft`",
    function(thing) {
        return `'current' expects a draft, got: ${thing}`;
    },
    "Object.defineProperty() cannot be used on an Immer draft",
    "Object.setPrototypeOf() cannot be used on an Immer draft",
    "Immer only supports deleting array indices",
    "Immer only supports setting array indices and the 'length' property",
    function(thing) {
        return `'original' expects a draft, got: ${thing}`;
    }
] : "TURBOPACK unreachable";
function die(error, ...args) {
    if ("TURBOPACK compile-time truthy", 1) {
        const e = errors[error];
        const msg = isFunction(e) ? e.apply(null, args) : e;
        throw new Error(`[Immer] ${msg}`);
    }
    throw new Error(`[Immer] minified error nr: ${error}. Full error at: https://bit.ly/3cXEKWf`);
}
// src/utils/common.ts
var O = Object;
var getPrototypeOf = O.getPrototypeOf;
var CONSTRUCTOR = "constructor";
var PROTOTYPE = "prototype";
var CONFIGURABLE = "configurable";
var ENUMERABLE = "enumerable";
var WRITABLE = "writable";
var VALUE = "value";
var isDraft = (value)=>!!value && !!value[DRAFT_STATE];
function isDraftable(value) {
    if (!value) return false;
    return isPlainObject(value) || isArray(value) || !!value[DRAFTABLE] || !!value[CONSTRUCTOR]?.[DRAFTABLE] || isMap(value) || isSet(value);
}
var objectCtorString = O[PROTOTYPE][CONSTRUCTOR].toString();
var cachedCtorStrings = /* @__PURE__ */ new WeakMap();
function isPlainObject(value) {
    if (!value || !isObjectish(value)) return false;
    const proto = getPrototypeOf(value);
    if (proto === null || proto === O[PROTOTYPE]) return true;
    const Ctor = O.hasOwnProperty.call(proto, CONSTRUCTOR) && proto[CONSTRUCTOR];
    if (Ctor === Object) return true;
    if (!isFunction(Ctor)) return false;
    let ctorString = cachedCtorStrings.get(Ctor);
    if (ctorString === void 0) {
        ctorString = Function.toString.call(Ctor);
        cachedCtorStrings.set(Ctor, ctorString);
    }
    return ctorString === objectCtorString;
}
function original(value) {
    if (!isDraft(value)) die(15, value);
    return value[DRAFT_STATE].base_;
}
function each(obj, iter, strict = true) {
    if (getArchtype(obj) === 0 /* Object */ ) {
        const keys = strict ? Reflect.ownKeys(obj) : O.keys(obj);
        keys.forEach((key)=>{
            iter(key, obj[key], obj);
        });
    } else {
        obj.forEach((entry, index)=>iter(index, entry, obj));
    }
}
function getArchtype(thing) {
    const state = thing[DRAFT_STATE];
    return state ? state.type_ : isArray(thing) ? 1 /* Array */  : isMap(thing) ? 2 /* Map */  : isSet(thing) ? 3 /* Set */  : 0 /* Object */ ;
}
var has = (thing, prop, type = getArchtype(thing))=>type === 2 /* Map */  ? thing.has(prop) : O[PROTOTYPE].hasOwnProperty.call(thing, prop);
var get = (thing, prop, type = getArchtype(thing))=>// @ts-ignore
    type === 2 /* Map */  ? thing.get(prop) : thing[prop];
var set = (thing, propOrOldValue, value, type = getArchtype(thing))=>{
    if (type === 2 /* Map */ ) thing.set(propOrOldValue, value);
    else if (type === 3 /* Set */ ) {
        thing.add(value);
    } else thing[propOrOldValue] = value;
};
function is(x, y) {
    if (x === y) {
        return x !== 0 || 1 / x === 1 / y;
    } else {
        return x !== x && y !== y;
    }
}
var isArray = Array.isArray;
var isMap = (target)=>target instanceof Map;
var isSet = (target)=>target instanceof Set;
var isObjectish = (target)=>typeof target === "object";
var isFunction = (target)=>typeof target === "function";
var isBoolean = (target)=>typeof target === "boolean";
function isArrayIndex(value) {
    const n = +value;
    return Number.isInteger(n) && String(n) === value;
}
var getProxyDraft = (value)=>{
    if (!isObjectish(value)) return null;
    return value?.[DRAFT_STATE];
};
var latest = (state)=>state.copy_ || state.base_;
var getValue = (value)=>{
    const proxyDraft = getProxyDraft(value);
    return proxyDraft ? proxyDraft.copy_ ?? proxyDraft.base_ : value;
};
var getFinalValue = (state)=>state.modified_ ? state.copy_ : state.base_;
function shallowCopy(base, strict) {
    if (isMap(base)) {
        return new Map(base);
    }
    if (isSet(base)) {
        return new Set(base);
    }
    if (isArray(base)) return Array[PROTOTYPE].slice.call(base);
    const isPlain = isPlainObject(base);
    if (strict === true || strict === "class_only" && !isPlain) {
        const descriptors = O.getOwnPropertyDescriptors(base);
        delete descriptors[DRAFT_STATE];
        let keys = Reflect.ownKeys(descriptors);
        for(let i = 0; i < keys.length; i++){
            const key = keys[i];
            const desc = descriptors[key];
            if (desc[WRITABLE] === false) {
                desc[WRITABLE] = true;
                desc[CONFIGURABLE] = true;
            }
            if (desc.get || desc.set) descriptors[key] = {
                [CONFIGURABLE]: true,
                [WRITABLE]: true,
                // could live with !!desc.set as well here...
                [ENUMERABLE]: desc[ENUMERABLE],
                [VALUE]: base[key]
            };
        }
        return O.create(getPrototypeOf(base), descriptors);
    } else {
        const proto = getPrototypeOf(base);
        if (proto !== null && isPlain) {
            return {
                ...base
            };
        }
        const obj = O.create(proto);
        return O.assign(obj, base);
    }
}
function freeze(obj, deep = false) {
    if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj)) return obj;
    if (getArchtype(obj) > 1) {
        O.defineProperties(obj, {
            set: dontMutateMethodOverride,
            add: dontMutateMethodOverride,
            clear: dontMutateMethodOverride,
            delete: dontMutateMethodOverride
        });
    }
    O.freeze(obj);
    if (deep) each(obj, (_key, value)=>{
        freeze(value, true);
    }, false);
    return obj;
}
function dontMutateFrozenCollections() {
    die(2);
}
var dontMutateMethodOverride = {
    [VALUE]: dontMutateFrozenCollections
};
function isFrozen(obj) {
    if (obj === null || !isObjectish(obj)) return true;
    return O.isFrozen(obj);
}
// src/utils/plugins.ts
var PluginMapSet = "MapSet";
var PluginPatches = "Patches";
var PluginArrayMethods = "ArrayMethods";
var plugins = {};
function getPlugin(pluginKey) {
    const plugin = plugins[pluginKey];
    if (!plugin) {
        die(0, pluginKey);
    }
    return plugin;
}
var isPluginLoaded = (pluginKey)=>!!plugins[pluginKey];
function loadPlugin(pluginKey, implementation) {
    if (!plugins[pluginKey]) plugins[pluginKey] = implementation;
}
// src/core/scope.ts
var currentScope;
var getCurrentScope = ()=>currentScope;
var createScope = (parent_, immer_)=>({
        drafts_: [],
        parent_,
        immer_,
        // Whenever the modified draft contains a draft from another scope, we
        // need to prevent auto-freezing so the unowned draft can be finalized.
        canAutoFreeze_: true,
        unfinalizedDrafts_: 0,
        handledSet_: /* @__PURE__ */ new Set(),
        processedForPatches_: /* @__PURE__ */ new Set(),
        mapSetPlugin_: isPluginLoaded(PluginMapSet) ? getPlugin(PluginMapSet) : void 0,
        arrayMethodsPlugin_: isPluginLoaded(PluginArrayMethods) ? getPlugin(PluginArrayMethods) : void 0
    });
function usePatchesInScope(scope, patchListener) {
    if (patchListener) {
        scope.patchPlugin_ = getPlugin(PluginPatches);
        scope.patches_ = [];
        scope.inversePatches_ = [];
        scope.patchListener_ = patchListener;
    }
}
function revokeScope(scope) {
    leaveScope(scope);
    scope.drafts_.forEach(revokeDraft);
    scope.drafts_ = null;
}
function leaveScope(scope) {
    if (scope === currentScope) {
        currentScope = scope.parent_;
    }
}
var enterScope = (immer2)=>currentScope = createScope(currentScope, immer2);
function revokeDraft(draft) {
    const state = draft[DRAFT_STATE];
    if (state.type_ === 0 /* Object */  || state.type_ === 1 /* Array */ ) state.revoke_();
    else state.revoked_ = true;
}
// src/core/finalize.ts
function processResult(result, scope) {
    scope.unfinalizedDrafts_ = scope.drafts_.length;
    const baseDraft = scope.drafts_[0];
    const isReplaced = result !== void 0 && result !== baseDraft;
    if (isReplaced) {
        if (baseDraft[DRAFT_STATE].modified_) {
            revokeScope(scope);
            die(4);
        }
        if (isDraftable(result)) {
            result = finalize(scope, result);
        }
        const { patchPlugin_ } = scope;
        if (patchPlugin_) {
            patchPlugin_.generateReplacementPatches_(baseDraft[DRAFT_STATE].base_, result, scope);
        }
    } else {
        result = finalize(scope, baseDraft);
    }
    maybeFreeze(scope, result, true);
    revokeScope(scope);
    if (scope.patches_) {
        scope.patchListener_(scope.patches_, scope.inversePatches_);
    }
    return result !== NOTHING ? result : void 0;
}
function finalize(rootScope, value) {
    if (isFrozen(value)) return value;
    const state = value[DRAFT_STATE];
    if (!state) {
        const finalValue = handleValue(value, rootScope.handledSet_, rootScope);
        return finalValue;
    }
    if (!isSameScope(state, rootScope)) {
        return value;
    }
    if (!state.modified_) {
        return state.base_;
    }
    if (!state.finalized_) {
        const { callbacks_ } = state;
        if (callbacks_) {
            while(callbacks_.length > 0){
                const callback = callbacks_.pop();
                callback(rootScope);
            }
        }
        generatePatchesAndFinalize(state, rootScope);
    }
    return state.copy_;
}
function maybeFreeze(scope, value, deep = false) {
    if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
        freeze(value, deep);
    }
}
function markStateFinalized(state) {
    state.finalized_ = true;
    state.scope_.unfinalizedDrafts_--;
}
var isSameScope = (state, rootScope)=>state.scope_ === rootScope;
var EMPTY_LOCATIONS_RESULT = [];
function updateDraftInParent(parent, draftValue, finalizedValue, originalKey) {
    const parentCopy = latest(parent);
    const parentType = parent.type_;
    if (originalKey !== void 0) {
        const currentValue = get(parentCopy, originalKey, parentType);
        if (currentValue === draftValue) {
            set(parentCopy, originalKey, finalizedValue, parentType);
            return;
        }
    }
    if (!parent.draftLocations_) {
        const draftLocations = parent.draftLocations_ = /* @__PURE__ */ new Map();
        each(parentCopy, (key, value)=>{
            if (isDraft(value)) {
                const keys = draftLocations.get(value) || [];
                keys.push(key);
                draftLocations.set(value, keys);
            }
        });
    }
    const locations = parent.draftLocations_.get(draftValue) ?? EMPTY_LOCATIONS_RESULT;
    for (const location of locations){
        set(parentCopy, location, finalizedValue, parentType);
    }
}
function registerChildFinalizationCallback(parent, child, key) {
    parent.callbacks_.push(function childCleanup(rootScope) {
        const state = child;
        if (!state || !isSameScope(state, rootScope)) {
            return;
        }
        rootScope.mapSetPlugin_?.fixSetContents(state);
        const finalizedValue = getFinalValue(state);
        updateDraftInParent(parent, state.draft_ ?? state, finalizedValue, key);
        generatePatchesAndFinalize(state, rootScope);
    });
}
function generatePatchesAndFinalize(state, rootScope) {
    const shouldFinalize = state.modified_ && !state.finalized_ && (state.type_ === 3 /* Set */  || state.type_ === 1 /* Array */  && state.allIndicesReassigned_ || (state.assigned_?.size ?? 0) > 0);
    if (shouldFinalize) {
        const { patchPlugin_ } = rootScope;
        if (patchPlugin_) {
            const basePath = patchPlugin_.getPath(state);
            if (basePath) {
                patchPlugin_.generatePatches_(state, basePath, rootScope);
            }
        }
        markStateFinalized(state);
    }
}
function handleCrossReference(target, key, value) {
    const { scope_ } = target;
    if (isDraft(value)) {
        const state = value[DRAFT_STATE];
        if (isSameScope(state, scope_)) {
            state.callbacks_.push(function crossReferenceCleanup() {
                prepareCopy(target);
                const finalizedValue = getFinalValue(state);
                updateDraftInParent(target, value, finalizedValue, key);
            });
        }
    } else if (isDraftable(value)) {
        target.callbacks_.push(function nestedDraftCleanup() {
            const targetCopy = latest(target);
            if (target.type_ === 3 /* Set */ ) {
                if (targetCopy.has(value)) {
                    handleValue(value, scope_.handledSet_, scope_);
                }
            } else {
                if (get(targetCopy, key, target.type_) === value) {
                    if (scope_.drafts_.length > 1 && (target.assigned_.get(key) ?? false) === true && target.copy_) {
                        handleValue(get(target.copy_, key, target.type_), scope_.handledSet_, scope_);
                    }
                }
            }
        });
    }
}
function handleValue(target, handledSet, rootScope) {
    if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
        return target;
    }
    if (isDraft(target) || handledSet.has(target) || !isDraftable(target) || isFrozen(target)) {
        return target;
    }
    handledSet.add(target);
    each(target, (key, value)=>{
        if (isDraft(value)) {
            const state = value[DRAFT_STATE];
            if (isSameScope(state, rootScope)) {
                const updatedValue = getFinalValue(state);
                set(target, key, updatedValue, target.type_);
                markStateFinalized(state);
            }
        } else if (isDraftable(value)) {
            handleValue(value, handledSet, rootScope);
        }
    });
    return target;
}
// src/core/proxy.ts
function createProxyProxy(base, parent) {
    const baseIsArray = isArray(base);
    const state = {
        type_: baseIsArray ? 1 /* Array */  : 0 /* Object */ ,
        // Track which produce call this is associated with.
        scope_: parent ? parent.scope_ : getCurrentScope(),
        // True for both shallow and deep changes.
        modified_: false,
        // Used during finalization.
        finalized_: false,
        // Track which properties have been assigned (true) or deleted (false).
        // actually instantiated in `prepareCopy()`
        assigned_: void 0,
        // The parent draft state.
        parent_: parent,
        // The base state.
        base_: base,
        // The base proxy.
        draft_: null,
        // set below
        // The base copy with any updated values.
        copy_: null,
        // Called by the `produce` function.
        revoke_: null,
        isManual_: false,
        // `callbacks` actually gets assigned in `createProxy`
        callbacks_: void 0
    };
    let target = state;
    let traps = objectTraps;
    if (baseIsArray) {
        target = [
            state
        ];
        traps = arrayTraps;
    }
    const { revoke, proxy } = Proxy.revocable(target, traps);
    state.draft_ = proxy;
    state.revoke_ = revoke;
    return [
        proxy,
        state
    ];
}
var objectTraps = {
    get (state, prop) {
        if (prop === DRAFT_STATE) return state;
        let arrayPlugin = state.scope_.arrayMethodsPlugin_;
        const isArrayWithStringProp = state.type_ === 1 /* Array */  && typeof prop === "string";
        if (isArrayWithStringProp) {
            if (arrayPlugin?.isArrayOperationMethod(prop)) {
                return arrayPlugin.createMethodInterceptor(state, prop);
            }
        }
        const source = latest(state);
        if (!has(source, prop, state.type_)) {
            return readPropFromProto(state, source, prop);
        }
        const value = source[prop];
        if (state.finalized_ || !isDraftable(value)) {
            return value;
        }
        if (isArrayWithStringProp && state.operationMethod && arrayPlugin?.isMutatingArrayMethod(state.operationMethod) && isArrayIndex(prop)) {
            return value;
        }
        if (value === peek(state.base_, prop)) {
            prepareCopy(state);
            const childKey = state.type_ === 1 /* Array */  ? +prop : prop;
            const childDraft = createProxy(state.scope_, value, state, childKey);
            return state.copy_[childKey] = childDraft;
        }
        return value;
    },
    has (state, prop) {
        return prop in latest(state);
    },
    ownKeys (state) {
        return Reflect.ownKeys(latest(state));
    },
    set (state, prop, value) {
        const desc = getDescriptorFromProto(latest(state), prop);
        if (desc?.set) {
            desc.set.call(state.draft_, value);
            return true;
        }
        if (!state.modified_) {
            const current2 = peek(latest(state), prop);
            const currentState = current2?.[DRAFT_STATE];
            if (currentState && currentState.base_ === value) {
                state.copy_[prop] = value;
                state.assigned_.set(prop, false);
                return true;
            }
            if (is(value, current2) && (value !== void 0 || has(state.base_, prop, state.type_))) return true;
            prepareCopy(state);
            markChanged(state);
        }
        if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
        (value !== void 0 || prop in state.copy_) || // special case: NaN
        Number.isNaN(value) && Number.isNaN(state.copy_[prop])) return true;
        state.copy_[prop] = value;
        state.assigned_.set(prop, true);
        handleCrossReference(state, prop, value);
        return true;
    },
    deleteProperty (state, prop) {
        prepareCopy(state);
        if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
            state.assigned_.set(prop, false);
            markChanged(state);
        } else {
            state.assigned_.delete(prop);
        }
        if (state.copy_) {
            delete state.copy_[prop];
        }
        return true;
    },
    // Note: We never coerce `desc.value` into an Immer draft, because we can't make
    // the same guarantee in ES5 mode.
    getOwnPropertyDescriptor (state, prop) {
        const owner = latest(state);
        const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
        if (!desc) return desc;
        return {
            [WRITABLE]: true,
            [CONFIGURABLE]: state.type_ !== 1 /* Array */  || prop !== "length",
            [ENUMERABLE]: desc[ENUMERABLE],
            [VALUE]: owner[prop]
        };
    },
    defineProperty () {
        die(11);
    },
    getPrototypeOf (state) {
        return getPrototypeOf(state.base_);
    },
    setPrototypeOf () {
        die(12);
    }
};
var arrayTraps = {};
for(let key in objectTraps){
    let fn = objectTraps[key];
    arrayTraps[key] = function() {
        const args = arguments;
        args[0] = args[0][0];
        return fn.apply(this, args);
    };
}
arrayTraps.deleteProperty = function(state, prop) {
    if (("TURBOPACK compile-time value", "development") !== "production" && isNaN(parseInt(prop))) die(13);
    return arrayTraps.set.call(this, state, prop, void 0);
};
arrayTraps.set = function(state, prop, value) {
    if (("TURBOPACK compile-time value", "development") !== "production" && prop !== "length" && isNaN(parseInt(prop))) die(14);
    return objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function peek(draft, prop) {
    const state = draft[DRAFT_STATE];
    const source = state ? latest(state) : draft;
    return source[prop];
}
function readPropFromProto(state, source, prop) {
    const desc = getDescriptorFromProto(source, prop);
    return desc ? VALUE in desc ? desc[VALUE] : // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_) : void 0;
}
function getDescriptorFromProto(source, prop) {
    if (!(prop in source)) return void 0;
    let proto = getPrototypeOf(source);
    while(proto){
        const desc = Object.getOwnPropertyDescriptor(proto, prop);
        if (desc) return desc;
        proto = getPrototypeOf(proto);
    }
    return void 0;
}
function markChanged(state) {
    if (!state.modified_) {
        state.modified_ = true;
        if (state.parent_) {
            markChanged(state.parent_);
        }
    }
}
function prepareCopy(state) {
    if (!state.copy_) {
        state.assigned_ = /* @__PURE__ */ new Map();
        state.copy_ = shallowCopy(state.base_, state.scope_.immer_.useStrictShallowCopy_);
    }
}
// src/core/immerClass.ts
var Immer2 = class {
    constructor(config){
        this.autoFreeze_ = true;
        this.useStrictShallowCopy_ = false;
        this.useStrictIteration_ = false;
        /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */ this.produce = (base, recipe, patchListener)=>{
            if (isFunction(base) && !isFunction(recipe)) {
                const defaultBase = recipe;
                recipe = base;
                const self = this;
                return function curriedProduce(base2 = defaultBase, ...args) {
                    return self.produce(base2, (draft)=>recipe.call(this, draft, ...args));
                };
            }
            if (!isFunction(recipe)) die(6);
            if (patchListener !== void 0 && !isFunction(patchListener)) die(7);
            let result;
            if (isDraftable(base)) {
                const scope = enterScope(this);
                const proxy = createProxy(scope, base, void 0);
                let hasError = true;
                try {
                    result = recipe(proxy);
                    hasError = false;
                } finally{
                    if (hasError) revokeScope(scope);
                    else leaveScope(scope);
                }
                usePatchesInScope(scope, patchListener);
                return processResult(result, scope);
            } else if (!base || !isObjectish(base)) {
                result = recipe(base);
                if (result === void 0) result = base;
                if (result === NOTHING) result = void 0;
                if (this.autoFreeze_) freeze(result, true);
                if (patchListener) {
                    const p = [];
                    const ip = [];
                    getPlugin(PluginPatches).generateReplacementPatches_(base, result, {
                        patches_: p,
                        inversePatches_: ip
                    });
                    patchListener(p, ip);
                }
                return result;
            } else die(1, base);
        };
        this.produceWithPatches = (base, recipe)=>{
            if (isFunction(base)) {
                return (state, ...args)=>this.produceWithPatches(state, (draft)=>base(draft, ...args));
            }
            let patches, inversePatches;
            const result = this.produce(base, recipe, (p, ip)=>{
                patches = p;
                inversePatches = ip;
            });
            return [
                result,
                patches,
                inversePatches
            ];
        };
        if (isBoolean(config?.autoFreeze)) this.setAutoFreeze(config.autoFreeze);
        if (isBoolean(config?.useStrictShallowCopy)) this.setUseStrictShallowCopy(config.useStrictShallowCopy);
        if (isBoolean(config?.useStrictIteration)) this.setUseStrictIteration(config.useStrictIteration);
    }
    createDraft(base) {
        if (!isDraftable(base)) die(8);
        if (isDraft(base)) base = current(base);
        const scope = enterScope(this);
        const proxy = createProxy(scope, base, void 0);
        proxy[DRAFT_STATE].isManual_ = true;
        leaveScope(scope);
        return proxy;
    }
    finishDraft(draft, patchListener) {
        const state = draft && draft[DRAFT_STATE];
        if (!state || !state.isManual_) die(9);
        const { scope_: scope } = state;
        usePatchesInScope(scope, patchListener);
        return processResult(void 0, scope);
    }
    /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */ setAutoFreeze(value) {
        this.autoFreeze_ = value;
    }
    /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */ setUseStrictShallowCopy(value) {
        this.useStrictShallowCopy_ = value;
    }
    /**
   * Pass false to use faster iteration that skips non-enumerable properties
   * but still handles symbols for compatibility.
   *
   * By default, strict iteration is enabled (includes all own properties).
   */ setUseStrictIteration(value) {
        this.useStrictIteration_ = value;
    }
    shouldUseStrictIteration() {
        return this.useStrictIteration_;
    }
    applyPatches(base, patches) {
        let i;
        for(i = patches.length - 1; i >= 0; i--){
            const patch = patches[i];
            if (patch.path.length === 0 && patch.op === "replace") {
                base = patch.value;
                break;
            }
        }
        if (i > -1) {
            patches = patches.slice(i + 1);
        }
        const applyPatchesImpl = getPlugin(PluginPatches).applyPatches_;
        if (isDraft(base)) {
            return applyPatchesImpl(base, patches);
        }
        return this.produce(base, (draft)=>applyPatchesImpl(draft, patches));
    }
};
function createProxy(rootScope, value, parent, key) {
    const [draft, state] = isMap(value) ? getPlugin(PluginMapSet).proxyMap_(value, parent) : isSet(value) ? getPlugin(PluginMapSet).proxySet_(value, parent) : createProxyProxy(value, parent);
    const scope = parent?.scope_ ?? getCurrentScope();
    scope.drafts_.push(draft);
    state.callbacks_ = parent?.callbacks_ ?? [];
    state.key_ = key;
    if (parent && key !== void 0) {
        registerChildFinalizationCallback(parent, state, key);
    } else {
        state.callbacks_.push(function rootDraftCleanup(rootScope2) {
            rootScope2.mapSetPlugin_?.fixSetContents(state);
            const { patchPlugin_ } = rootScope2;
            if (state.modified_ && patchPlugin_) {
                patchPlugin_.generatePatches_(state, [], rootScope2);
            }
        });
    }
    return draft;
}
// src/core/current.ts
function current(value) {
    if (!isDraft(value)) die(10, value);
    return currentImpl(value);
}
function currentImpl(value) {
    if (!isDraftable(value) || isFrozen(value)) return value;
    const state = value[DRAFT_STATE];
    let copy;
    let strict = true;
    if (state) {
        if (!state.modified_) return state.base_;
        state.finalized_ = true;
        copy = shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
        strict = state.scope_.immer_.shouldUseStrictIteration();
    } else {
        copy = shallowCopy(value, true);
    }
    each(copy, (key, childValue)=>{
        set(copy, key, currentImpl(childValue));
    }, strict);
    if (state) {
        state.finalized_ = false;
    }
    return copy;
}
// src/plugins/patches.ts
function enablePatches() {
    const errorOffset = 16;
    if ("TURBOPACK compile-time truthy", 1) {
        errors.push('Sets cannot have "replace" patches.', function(op) {
            return "Unsupported patch operation: " + op;
        }, function(path) {
            return "Cannot apply patch, path doesn't resolve: " + path;
        }, "Patching reserved attributes like __proto__, prototype and constructor is not allowed");
    }
    function getPath(state, path = []) {
        if (state.key_ !== void 0) {
            const parentCopy = state.parent_.copy_ ?? state.parent_.base_;
            const proxyDraft = getProxyDraft(get(parentCopy, state.key_));
            const valueAtKey = get(parentCopy, state.key_);
            if (valueAtKey === void 0) {
                return null;
            }
            if (valueAtKey !== state.draft_ && valueAtKey !== state.base_ && valueAtKey !== state.copy_) {
                return null;
            }
            if (proxyDraft != null && proxyDraft.base_ !== state.base_) {
                return null;
            }
            const isSet2 = state.parent_.type_ === 3 /* Set */ ;
            let key;
            if (isSet2) {
                const setParent = state.parent_;
                key = Array.from(setParent.drafts_.keys()).indexOf(state.key_);
            } else {
                key = state.key_;
            }
            if (!(isSet2 && parentCopy.size > key || has(parentCopy, key))) {
                return null;
            }
            path.push(key);
        }
        if (state.parent_) {
            return getPath(state.parent_, path);
        }
        path.reverse();
        try {
            resolvePath(state.copy_, path);
        } catch (e) {
            return null;
        }
        return path;
    }
    function resolvePath(base, path) {
        let current2 = base;
        for(let i = 0; i < path.length - 1; i++){
            const key = path[i];
            current2 = get(current2, key);
            if (!isObjectish(current2) || current2 === null) {
                throw new Error(`Cannot resolve path at '${path.join("/")}'`);
            }
        }
        return current2;
    }
    const REPLACE = "replace";
    const ADD = "add";
    const REMOVE = "remove";
    function generatePatches_(state, basePath, scope) {
        if (state.scope_.processedForPatches_.has(state)) {
            return;
        }
        state.scope_.processedForPatches_.add(state);
        const { patches_, inversePatches_ } = scope;
        switch(state.type_){
            case 0 /* Object */ :
            case 2 /* Map */ :
                return generatePatchesFromAssigned(state, basePath, patches_, inversePatches_);
            case 1 /* Array */ :
                return generateArrayPatches(state, basePath, patches_, inversePatches_);
            case 3 /* Set */ :
                return generateSetPatches(state, basePath, patches_, inversePatches_);
        }
    }
    function generateArrayPatches(state, basePath, patches, inversePatches) {
        let { base_, assigned_ } = state;
        let copy_ = state.copy_;
        if (copy_.length < base_.length) {
            ;
            [base_, copy_] = [
                copy_,
                base_
            ];
            [patches, inversePatches] = [
                inversePatches,
                patches
            ];
        }
        const allReassigned = state.allIndicesReassigned_ === true;
        for(let i = 0; i < base_.length; i++){
            const copiedItem = copy_[i];
            const baseItem = base_[i];
            const isAssigned = allReassigned || assigned_?.get(i.toString());
            if (isAssigned && copiedItem !== baseItem) {
                const childState = copiedItem?.[DRAFT_STATE];
                if (childState && childState.modified_) {
                    continue;
                }
                const path = basePath.concat([
                    i
                ]);
                patches.push({
                    op: REPLACE,
                    path,
                    // Need to maybe clone it, as it can in fact be the original value
                    // due to the base/copy inversion at the start of this function
                    value: clonePatchValueIfNeeded(copiedItem)
                });
                inversePatches.push({
                    op: REPLACE,
                    path,
                    value: clonePatchValueIfNeeded(baseItem)
                });
            }
        }
        for(let i = base_.length; i < copy_.length; i++){
            const path = basePath.concat([
                i
            ]);
            patches.push({
                op: ADD,
                path,
                // Need to maybe clone it, as it can in fact be the original value
                // due to the base/copy inversion at the start of this function
                value: clonePatchValueIfNeeded(copy_[i])
            });
        }
        for(let i = copy_.length - 1; base_.length <= i; --i){
            const path = basePath.concat([
                i
            ]);
            inversePatches.push({
                op: REMOVE,
                path
            });
        }
    }
    function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
        const { base_, copy_, type_ } = state;
        each(state.assigned_, (key, assignedValue)=>{
            const origValue = get(base_, key, type_);
            const value = get(copy_, key, type_);
            const op = !assignedValue ? REMOVE : has(base_, key) ? REPLACE : ADD;
            if (origValue === value && op === REPLACE) return;
            const path = basePath.concat(key);
            patches.push(op === REMOVE ? {
                op,
                path
            } : {
                op,
                path,
                value: clonePatchValueIfNeeded(value)
            });
            inversePatches.push(op === ADD ? {
                op: REMOVE,
                path
            } : op === REMOVE ? {
                op: ADD,
                path,
                value: clonePatchValueIfNeeded(origValue)
            } : {
                op: REPLACE,
                path,
                value: clonePatchValueIfNeeded(origValue)
            });
        });
    }
    function generateSetPatches(state, basePath, patches, inversePatches) {
        let { base_, copy_ } = state;
        let i = 0;
        base_.forEach((value)=>{
            if (!copy_.has(value)) {
                const path = basePath.concat([
                    i
                ]);
                patches.push({
                    op: REMOVE,
                    path,
                    value
                });
                inversePatches.unshift({
                    op: ADD,
                    path,
                    value
                });
            }
            i++;
        });
        i = 0;
        copy_.forEach((value)=>{
            if (!base_.has(value)) {
                const path = basePath.concat([
                    i
                ]);
                patches.push({
                    op: ADD,
                    path,
                    value
                });
                inversePatches.unshift({
                    op: REMOVE,
                    path,
                    value
                });
            }
            i++;
        });
    }
    function generateReplacementPatches_(baseValue, replacement, scope) {
        const { patches_, inversePatches_ } = scope;
        patches_.push({
            op: REPLACE,
            path: [],
            value: replacement === NOTHING ? void 0 : replacement
        });
        inversePatches_.push({
            op: REPLACE,
            path: [],
            value: baseValue
        });
    }
    function applyPatches_(draft, patches) {
        patches.forEach((patch)=>{
            const { path, op } = patch;
            let base = draft;
            for(let i = 0; i < path.length - 1; i++){
                const parentType = getArchtype(base);
                let p = path[i];
                if (typeof p !== "string" && typeof p !== "number") {
                    p = "" + p;
                }
                if ((parentType === 0 /* Object */  || parentType === 1 /* Array */ ) && (p === "__proto__" || p === CONSTRUCTOR)) die(errorOffset + 3);
                if (isFunction(base) && p === PROTOTYPE) die(errorOffset + 3);
                base = get(base, p);
                if (!isObjectish(base)) die(errorOffset + 2, path.join("/"));
            }
            const type = getArchtype(base);
            const value = deepClonePatchValue(patch.value);
            const key = path[path.length - 1];
            switch(op){
                case REPLACE:
                    switch(type){
                        case 2 /* Map */ :
                            return base.set(key, value);
                        case 3 /* Set */ :
                            die(errorOffset);
                        default:
                            return base[key] = value;
                    }
                case ADD:
                    switch(type){
                        case 1 /* Array */ :
                            return key === "-" ? base.push(value) : base.splice(key, 0, value);
                        case 2 /* Map */ :
                            return base.set(key, value);
                        case 3 /* Set */ :
                            return base.add(value);
                        default:
                            return base[key] = value;
                    }
                case REMOVE:
                    switch(type){
                        case 1 /* Array */ :
                            return base.splice(key, 1);
                        case 2 /* Map */ :
                            return base.delete(key);
                        case 3 /* Set */ :
                            return base.delete(patch.value);
                        default:
                            return delete base[key];
                    }
                default:
                    die(errorOffset + 1, op);
            }
        });
        return draft;
    }
    function deepClonePatchValue(obj) {
        if (!isDraftable(obj)) return obj;
        if (isArray(obj)) return obj.map(deepClonePatchValue);
        if (isMap(obj)) return new Map(Array.from(obj.entries()).map(([k, v])=>[
                k,
                deepClonePatchValue(v)
            ]));
        if (isSet(obj)) return new Set(Array.from(obj).map(deepClonePatchValue));
        const cloned = Object.create(getPrototypeOf(obj));
        for(const key in obj)cloned[key] = deepClonePatchValue(obj[key]);
        if (has(obj, DRAFTABLE)) cloned[DRAFTABLE] = obj[DRAFTABLE];
        return cloned;
    }
    function clonePatchValueIfNeeded(obj) {
        if (isDraft(obj)) {
            return deepClonePatchValue(obj);
        } else return obj;
    }
    loadPlugin(PluginPatches, {
        applyPatches_,
        generatePatches_,
        generateReplacementPatches_,
        getPath
    });
}
// src/plugins/mapset.ts
function enableMapSet() {
    class DraftMap extends Map {
        constructor(target, parent){
            super();
            this[DRAFT_STATE] = {
                type_: 2 /* Map */ ,
                parent_: parent,
                scope_: parent ? parent.scope_ : getCurrentScope(),
                modified_: false,
                finalized_: false,
                copy_: void 0,
                assigned_: void 0,
                base_: target,
                draft_: this,
                isManual_: false,
                revoked_: false,
                callbacks_: []
            };
        }
        get size() {
            return latest(this[DRAFT_STATE]).size;
        }
        has(key) {
            return latest(this[DRAFT_STATE]).has(key);
        }
        set(key, value) {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            if (!latest(state).has(key) || latest(state).get(key) !== value) {
                prepareMapCopy(state);
                markChanged(state);
                state.assigned_.set(key, true);
                state.copy_.set(key, value);
                state.assigned_.set(key, true);
                handleCrossReference(state, key, value);
            }
            return this;
        }
        delete(key) {
            if (!this.has(key)) {
                return false;
            }
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            prepareMapCopy(state);
            markChanged(state);
            if (state.base_.has(key)) {
                state.assigned_.set(key, false);
            } else {
                state.assigned_.delete(key);
            }
            state.copy_.delete(key);
            return true;
        }
        clear() {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            if (latest(state).size) {
                prepareMapCopy(state);
                markChanged(state);
                state.assigned_ = /* @__PURE__ */ new Map();
                each(state.base_, (key)=>{
                    state.assigned_.set(key, false);
                });
                state.copy_.clear();
            }
        }
        forEach(cb, thisArg) {
            const state = this[DRAFT_STATE];
            latest(state).forEach((_value, key, _map)=>{
                cb.call(thisArg, this.get(key), key, this);
            });
        }
        get(key) {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            const value = latest(state).get(key);
            if (state.finalized_ || !isDraftable(value)) {
                return value;
            }
            if (value !== state.base_.get(key)) {
                return value;
            }
            const draft = createProxy(state.scope_, value, state, key);
            prepareMapCopy(state);
            state.copy_.set(key, draft);
            return draft;
        }
        keys() {
            return latest(this[DRAFT_STATE]).keys();
        }
        values() {
            const iterator = this.keys();
            return {
                [Symbol.iterator]: ()=>this.values(),
                next: ()=>{
                    const r = iterator.next();
                    if (r.done) return r;
                    const value = this.get(r.value);
                    return {
                        done: false,
                        value
                    };
                }
            };
        }
        entries() {
            const iterator = this.keys();
            return {
                [Symbol.iterator]: ()=>this.entries(),
                next: ()=>{
                    const r = iterator.next();
                    if (r.done) return r;
                    const value = this.get(r.value);
                    return {
                        done: false,
                        value: [
                            r.value,
                            value
                        ]
                    };
                }
            };
        }
        [(DRAFT_STATE, Symbol.iterator)]() {
            return this.entries();
        }
    }
    function proxyMap_(target, parent) {
        const map = new DraftMap(target, parent);
        return [
            map,
            map[DRAFT_STATE]
        ];
    }
    function prepareMapCopy(state) {
        if (!state.copy_) {
            state.assigned_ = /* @__PURE__ */ new Map();
            state.copy_ = new Map(state.base_);
        }
    }
    class DraftSet extends Set {
        constructor(target, parent){
            super();
            this[DRAFT_STATE] = {
                type_: 3 /* Set */ ,
                parent_: parent,
                scope_: parent ? parent.scope_ : getCurrentScope(),
                modified_: false,
                finalized_: false,
                copy_: void 0,
                base_: target,
                draft_: this,
                drafts_: /* @__PURE__ */ new Map(),
                revoked_: false,
                isManual_: false,
                assigned_: void 0,
                callbacks_: []
            };
        }
        get size() {
            return latest(this[DRAFT_STATE]).size;
        }
        has(value) {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            if (!state.copy_) {
                return state.base_.has(value);
            }
            if (state.copy_.has(value)) return true;
            if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value))) return true;
            return false;
        }
        add(value) {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            if (!this.has(value)) {
                prepareSetCopy(state);
                markChanged(state);
                state.copy_.add(value);
                handleCrossReference(state, value, value);
            }
            return this;
        }
        delete(value) {
            if (!this.has(value)) {
                return false;
            }
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            prepareSetCopy(state);
            markChanged(state);
            return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : /* istanbul ignore next */ false);
        }
        clear() {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            if (latest(state).size) {
                prepareSetCopy(state);
                markChanged(state);
                state.copy_.clear();
            }
        }
        values() {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            prepareSetCopy(state);
            return state.copy_.values();
        }
        entries() {
            const state = this[DRAFT_STATE];
            assertUnrevoked(state);
            prepareSetCopy(state);
            return state.copy_.entries();
        }
        keys() {
            return this.values();
        }
        [(DRAFT_STATE, Symbol.iterator)]() {
            return this.values();
        }
        forEach(cb, thisArg) {
            const iterator = this.values();
            let result = iterator.next();
            while(!result.done){
                cb.call(thisArg, result.value, result.value, this);
                result = iterator.next();
            }
        }
    }
    function proxySet_(target, parent) {
        const set2 = new DraftSet(target, parent);
        return [
            set2,
            set2[DRAFT_STATE]
        ];
    }
    function prepareSetCopy(state) {
        if (!state.copy_) {
            state.copy_ = /* @__PURE__ */ new Set();
            state.base_.forEach((value)=>{
                if (isDraftable(value)) {
                    const draft = createProxy(state.scope_, value, state, value);
                    state.drafts_.set(value, draft);
                    state.copy_.add(draft);
                } else {
                    state.copy_.add(value);
                }
            });
        }
    }
    function assertUnrevoked(state) {
        if (state.revoked_) die(3, JSON.stringify(latest(state)));
    }
    function fixSetContents(target) {
        if (target.type_ === 3 /* Set */  && target.copy_) {
            const copy = new Set(target.copy_);
            target.copy_.clear();
            copy.forEach((value)=>{
                target.copy_.add(getValue(value));
            });
        }
    }
    loadPlugin(PluginMapSet, {
        proxyMap_,
        proxySet_,
        fixSetContents
    });
}
// src/plugins/arrayMethods.ts
function enableArrayMethods() {
    const SHIFTING_METHODS = /* @__PURE__ */ new Set([
        "shift",
        "unshift"
    ]);
    const QUEUE_METHODS = /* @__PURE__ */ new Set([
        "push",
        "pop"
    ]);
    const RESULT_RETURNING_METHODS = /* @__PURE__ */ new Set([
        ...QUEUE_METHODS,
        ...SHIFTING_METHODS
    ]);
    const REORDERING_METHODS = /* @__PURE__ */ new Set([
        "reverse",
        "sort"
    ]);
    const MUTATING_METHODS = /* @__PURE__ */ new Set([
        ...RESULT_RETURNING_METHODS,
        ...REORDERING_METHODS,
        "splice"
    ]);
    const FIND_METHODS = /* @__PURE__ */ new Set([
        "find",
        "findLast"
    ]);
    const NON_MUTATING_METHODS = /* @__PURE__ */ new Set([
        "filter",
        "slice",
        "concat",
        "flat",
        ...FIND_METHODS,
        "findIndex",
        "findLastIndex",
        "some",
        "every",
        "indexOf",
        "lastIndexOf",
        "includes",
        "join",
        "toString",
        "toLocaleString"
    ]);
    function isMutatingArrayMethod(method) {
        return MUTATING_METHODS.has(method);
    }
    function isNonMutatingArrayMethod(method) {
        return NON_MUTATING_METHODS.has(method);
    }
    function isArrayOperationMethod(method) {
        return isMutatingArrayMethod(method) || isNonMutatingArrayMethod(method);
    }
    function enterOperation(state, method) {
        state.operationMethod = method;
    }
    function exitOperation(state) {
        state.operationMethod = void 0;
    }
    function executeArrayMethod(state, operation, markLength = true) {
        prepareCopy(state);
        const result = operation();
        markChanged(state);
        if (markLength) state.assigned_.set("length", true);
        return result;
    }
    function markAllIndicesReassigned(state) {
        state.allIndicesReassigned_ = true;
    }
    function normalizeSliceIndex(index, length) {
        if (index < 0) {
            return Math.max(length + index, 0);
        }
        return Math.min(index, length);
    }
    function handleInsertedValues(state, startIndex, values) {
        for(let i = 0; i < values.length; i++){
            const index = startIndex + i;
            state.assigned_.set(index, true);
            handleCrossReference(state, index, values[i]);
        }
    }
    function handleSimpleOperation(state, method, args) {
        return executeArrayMethod(state, ()=>{
            const lengthBefore = state.copy_.length;
            const result = state.copy_[method](...args);
            if (SHIFTING_METHODS.has(method)) {
                markAllIndicesReassigned(state);
            }
            if (method === "push" && args.length > 0) {
                handleInsertedValues(state, lengthBefore, args);
            } else if (method === "unshift" && args.length > 0) {
                handleInsertedValues(state, 0, args);
            }
            return RESULT_RETURNING_METHODS.has(method) ? result : state.draft_;
        });
    }
    function handleReorderingOperation(state, method, args) {
        return executeArrayMethod(state, ()=>{
            ;
            state.copy_[method](...args);
            markAllIndicesReassigned(state);
            return state.draft_;
        }, false);
    }
    function createMethodInterceptor(state, originalMethod) {
        return function interceptedMethod(...args) {
            const method = originalMethod;
            enterOperation(state, method);
            try {
                if (isMutatingArrayMethod(method)) {
                    if (RESULT_RETURNING_METHODS.has(method)) {
                        return handleSimpleOperation(state, method, args);
                    }
                    if (REORDERING_METHODS.has(method)) {
                        return handleReorderingOperation(state, method, args);
                    }
                    if (method === "splice") {
                        const res = executeArrayMethod(state, ()=>state.copy_.splice(...args));
                        markAllIndicesReassigned(state);
                        if (args.length > 2) {
                            const startIndex = normalizeSliceIndex(args[0] ?? 0, state.copy_.length);
                            handleInsertedValues(state, startIndex, args.slice(2));
                        }
                        return res;
                    }
                } else {
                    return handleNonMutatingOperation(state, method, args);
                }
            } finally{
                exitOperation(state);
            }
        };
    }
    function handleNonMutatingOperation(state, method, args) {
        const source = latest(state);
        if (method === "filter") {
            const predicate = args[0];
            const result = [];
            for(let i = 0; i < source.length; i++){
                if (predicate(source[i], i, source)) {
                    result.push(state.draft_[i]);
                }
            }
            return result;
        }
        if (FIND_METHODS.has(method)) {
            const predicate = args[0];
            const isForward = method === "find";
            const step = isForward ? 1 : -1;
            const start = isForward ? 0 : source.length - 1;
            for(let i = start; i >= 0 && i < source.length; i += step){
                if (predicate(source[i], i, source)) {
                    return state.draft_[i];
                }
            }
            return void 0;
        }
        if (method === "slice") {
            const rawStart = args[0] ?? 0;
            const rawEnd = args[1] ?? source.length;
            const start = normalizeSliceIndex(rawStart, source.length);
            const end = normalizeSliceIndex(rawEnd, source.length);
            const result = [];
            for(let i = start; i < end; i++){
                result.push(state.draft_[i]);
            }
            return result;
        }
        return source[method](...args);
    }
    loadPlugin(PluginArrayMethods, {
        createMethodInterceptor,
        isArrayOperationMethod,
        isMutatingArrayMethod
    });
}
// src/immer.ts
var immer = new Immer2();
var produce = immer.produce;
var produceWithPatches = /* @__PURE__ */ immer.produceWithPatches.bind(immer);
var setAutoFreeze = /* @__PURE__ */ immer.setAutoFreeze.bind(immer);
var setUseStrictShallowCopy = /* @__PURE__ */ immer.setUseStrictShallowCopy.bind(immer);
var setUseStrictIteration = /* @__PURE__ */ immer.setUseStrictIteration.bind(immer);
var applyPatches = /* @__PURE__ */ immer.applyPatches.bind(immer);
var createDraft = /* @__PURE__ */ immer.createDraft.bind(immer);
var finishDraft = /* @__PURE__ */ immer.finishDraft.bind(immer);
var castDraft = (value)=>value;
var castImmutable = (value)=>value;
;
 //# sourceMappingURL=immer.mjs.map
}),
"[project]/node_modules/reselect/dist/reselect.mjs [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createSelector",
    ()=>createSelector,
    "createSelectorCreator",
    ()=>createSelectorCreator,
    "createStructuredSelector",
    ()=>createStructuredSelector,
    "lruMemoize",
    ()=>lruMemoize,
    "referenceEqualityCheck",
    ()=>referenceEqualityCheck,
    "setGlobalDevModeChecks",
    ()=>setGlobalDevModeChecks,
    "unstable_autotrackMemoize",
    ()=>autotrackMemoize,
    "weakMapMemoize",
    ()=>weakMapMemoize
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [app-client] (ecmascript)");
// src/devModeChecks/identityFunctionCheck.ts
var runIdentityFunctionCheck = (resultFunc, inputSelectorsResults, outputSelectorResult)=>{
    if (inputSelectorsResults.length === 1 && inputSelectorsResults[0] === outputSelectorResult) {
        let isInputSameAsOutput = false;
        try {
            const emptyObject = {};
            if (resultFunc(emptyObject) === emptyObject) isInputSameAsOutput = true;
        } catch  {}
        if (isInputSameAsOutput) {
            let stack = void 0;
            try {
                throw new Error();
            } catch (e) {
                ;
                ({ stack } = e);
            }
            console.warn("The result function returned its own inputs without modification. e.g\n`createSelector([state => state.todos], todos => todos)`\nThis could lead to inefficient memoization and unnecessary re-renders.\nEnsure transformation logic is in the result function, and extraction logic is in the input selectors.", {
                stack
            });
        }
    }
};
// src/devModeChecks/inputStabilityCheck.ts
var runInputStabilityCheck = (inputSelectorResultsObject, options, inputSelectorArgs)=>{
    const { memoize, memoizeOptions } = options;
    const { inputSelectorResults, inputSelectorResultsCopy } = inputSelectorResultsObject;
    const createAnEmptyObject = memoize(()=>({}), ...memoizeOptions);
    const areInputSelectorResultsEqual = createAnEmptyObject.apply(null, inputSelectorResults) === createAnEmptyObject.apply(null, inputSelectorResultsCopy);
    if (!areInputSelectorResultsEqual) {
        let stack = void 0;
        try {
            throw new Error();
        } catch (e) {
            ;
            ({ stack } = e);
        }
        console.warn("An input selector returned a different result when passed same arguments.\nThis means your output selector will likely run more frequently than intended.\nAvoid returning a new reference inside your input selector, e.g.\n`createSelector([state => state.todos.map(todo => todo.id)], todoIds => todoIds.length)`", {
            arguments: inputSelectorArgs,
            firstInputs: inputSelectorResults,
            secondInputs: inputSelectorResultsCopy,
            stack
        });
    }
};
// src/devModeChecks/setGlobalDevModeChecks.ts
var globalDevModeChecks = {
    inputStabilityCheck: "once",
    identityFunctionCheck: "once"
};
var setGlobalDevModeChecks = (devModeChecks)=>{
    Object.assign(globalDevModeChecks, devModeChecks);
};
// src/utils.ts
var NOT_FOUND = /* @__PURE__ */ Symbol("NOT_FOUND");
function assertIsFunction(func, errorMessage = `expected a function, instead received ${typeof func}`) {
    if (typeof func !== "function") {
        throw new TypeError(errorMessage);
    }
}
function assertIsObject(object, errorMessage = `expected an object, instead received ${typeof object}`) {
    if (typeof object !== "object") {
        throw new TypeError(errorMessage);
    }
}
function assertIsArrayOfFunctions(array, errorMessage = `expected all items to be functions, instead received the following types: `) {
    if (!array.every((item)=>typeof item === "function")) {
        const itemTypes = array.map((item)=>typeof item === "function" ? `function ${item.name || "unnamed"}()` : typeof item).join(", ");
        throw new TypeError(`${errorMessage}[${itemTypes}]`);
    }
}
var ensureIsArray = (item)=>{
    return Array.isArray(item) ? item : [
        item
    ];
};
function getDependencies(createSelectorArgs) {
    const dependencies = Array.isArray(createSelectorArgs[0]) ? createSelectorArgs[0] : createSelectorArgs;
    assertIsArrayOfFunctions(dependencies, `createSelector expects all input-selectors to be functions, but received the following types: `);
    return dependencies;
}
function collectInputSelectorResults(dependencies, inputSelectorArgs) {
    const inputSelectorResults = [];
    const { length } = dependencies;
    for(let i = 0; i < length; i++){
        inputSelectorResults.push(dependencies[i].apply(null, inputSelectorArgs));
    }
    return inputSelectorResults;
}
var getDevModeChecksExecutionInfo = (firstRun, devModeChecks)=>{
    const { identityFunctionCheck, inputStabilityCheck } = {
        ...globalDevModeChecks,
        ...devModeChecks
    };
    return {
        identityFunctionCheck: {
            shouldRun: identityFunctionCheck === "always" || identityFunctionCheck === "once" && firstRun,
            run: runIdentityFunctionCheck
        },
        inputStabilityCheck: {
            shouldRun: inputStabilityCheck === "always" || inputStabilityCheck === "once" && firstRun,
            run: runInputStabilityCheck
        }
    };
};
// src/autotrackMemoize/autotracking.ts
var $REVISION = 0;
var CURRENT_TRACKER = null;
var Cell = class {
    revision = $REVISION;
    _value;
    _lastValue;
    _isEqual = tripleEq;
    constructor(initialValue, isEqual = tripleEq){
        this._value = this._lastValue = initialValue;
        this._isEqual = isEqual;
    }
    // Whenever a storage value is read, it'll add itself to the current tracker if
    // one exists, entangling its state with that cache.
    get value() {
        CURRENT_TRACKER?.add(this);
        return this._value;
    }
    // Whenever a storage value is updated, we bump the global revision clock,
    // assign the revision for this storage to the new value, _and_ we schedule a
    // rerender. This is important, and it's what makes autotracking  _pull_
    // based. We don't actively tell the caches which depend on the storage that
    // anything has happened. Instead, we recompute the caches when needed.
    set value(newValue) {
        if (this.value === newValue) return;
        this._value = newValue;
        this.revision = ++$REVISION;
    }
};
function tripleEq(a, b) {
    return a === b;
}
var TrackingCache = class {
    _cachedValue;
    _cachedRevision = -1;
    _deps = [];
    hits = 0;
    fn;
    constructor(fn){
        this.fn = fn;
    }
    clear() {
        this._cachedValue = void 0;
        this._cachedRevision = -1;
        this._deps = [];
        this.hits = 0;
    }
    get value() {
        if (this.revision > this._cachedRevision) {
            const { fn } = this;
            const currentTracker = /* @__PURE__ */ new Set();
            const prevTracker = CURRENT_TRACKER;
            CURRENT_TRACKER = currentTracker;
            this._cachedValue = fn();
            CURRENT_TRACKER = prevTracker;
            this.hits++;
            this._deps = Array.from(currentTracker);
            this._cachedRevision = this.revision;
        }
        CURRENT_TRACKER?.add(this);
        return this._cachedValue;
    }
    get revision() {
        return Math.max(...this._deps.map((d)=>d.revision), 0);
    }
};
function getValue(cell) {
    if (!(cell instanceof Cell)) {
        console.warn("Not a valid cell! ", cell);
    }
    return cell.value;
}
function setValue(storage, value) {
    if (!(storage instanceof Cell)) {
        throw new TypeError("setValue must be passed a tracked store created with `createStorage`.");
    }
    storage.value = storage._lastValue = value;
}
function createCell(initialValue, isEqual = tripleEq) {
    return new Cell(initialValue, isEqual);
}
function createCache(fn) {
    assertIsFunction(fn, "the first parameter to `createCache` must be a function");
    return new TrackingCache(fn);
}
// src/autotrackMemoize/tracking.ts
var neverEq = (a, b)=>false;
function createTag() {
    return createCell(null, neverEq);
}
function dirtyTag(tag, value) {
    setValue(tag, value);
}
var consumeCollection = (node)=>{
    let tag = node.collectionTag;
    if (tag === null) {
        tag = node.collectionTag = createTag();
    }
    getValue(tag);
};
var dirtyCollection = (node)=>{
    const tag = node.collectionTag;
    if (tag !== null) {
        dirtyTag(tag, null);
    }
};
// src/autotrackMemoize/proxy.ts
var REDUX_PROXY_LABEL = Symbol();
var nextId = 0;
var proto = Object.getPrototypeOf({});
var ObjectTreeNode = class {
    constructor(value){
        this.value = value;
        this.value = value;
        this.tag.value = value;
    }
    proxy = new Proxy(this, objectProxyHandler);
    tag = createTag();
    tags = {};
    children = {};
    collectionTag = null;
    id = nextId++;
};
var objectProxyHandler = {
    get (node, key) {
        function calculateResult() {
            const { value } = node;
            const childValue = Reflect.get(value, key);
            if (typeof key === "symbol") {
                return childValue;
            }
            if (key in proto) {
                return childValue;
            }
            if (typeof childValue === "object" && childValue !== null) {
                let childNode = node.children[key];
                if (childNode === void 0) {
                    childNode = node.children[key] = createNode(childValue);
                }
                if (childNode.tag) {
                    getValue(childNode.tag);
                }
                return childNode.proxy;
            } else {
                let tag = node.tags[key];
                if (tag === void 0) {
                    tag = node.tags[key] = createTag();
                    tag.value = childValue;
                }
                getValue(tag);
                return childValue;
            }
        }
        const res = calculateResult();
        return res;
    },
    ownKeys (node) {
        consumeCollection(node);
        return Reflect.ownKeys(node.value);
    },
    getOwnPropertyDescriptor (node, prop) {
        return Reflect.getOwnPropertyDescriptor(node.value, prop);
    },
    has (node, prop) {
        return Reflect.has(node.value, prop);
    }
};
var ArrayTreeNode = class {
    constructor(value){
        this.value = value;
        this.value = value;
        this.tag.value = value;
    }
    proxy = new Proxy([
        this
    ], arrayProxyHandler);
    tag = createTag();
    tags = {};
    children = {};
    collectionTag = null;
    id = nextId++;
};
var arrayProxyHandler = {
    get ([node], key) {
        if (key === "length") {
            consumeCollection(node);
        }
        return objectProxyHandler.get(node, key);
    },
    ownKeys ([node]) {
        return objectProxyHandler.ownKeys(node);
    },
    getOwnPropertyDescriptor ([node], prop) {
        return objectProxyHandler.getOwnPropertyDescriptor(node, prop);
    },
    has ([node], prop) {
        return objectProxyHandler.has(node, prop);
    }
};
function createNode(value) {
    if (Array.isArray(value)) {
        return new ArrayTreeNode(value);
    }
    return new ObjectTreeNode(value);
}
function updateNode(node, newValue) {
    const { value, tags, children } = node;
    node.value = newValue;
    if (Array.isArray(value) && Array.isArray(newValue) && value.length !== newValue.length) {
        dirtyCollection(node);
    } else {
        if (value !== newValue) {
            let oldKeysSize = 0;
            let newKeysSize = 0;
            let anyKeysAdded = false;
            for(const _key in value){
                oldKeysSize++;
            }
            for(const key in newValue){
                newKeysSize++;
                if (!(key in value)) {
                    anyKeysAdded = true;
                    break;
                }
            }
            const isDifferent = anyKeysAdded || oldKeysSize !== newKeysSize;
            if (isDifferent) {
                dirtyCollection(node);
            }
        }
    }
    for(const key in tags){
        const childValue = value[key];
        const newChildValue = newValue[key];
        if (childValue !== newChildValue) {
            dirtyCollection(node);
            dirtyTag(tags[key], newChildValue);
        }
        if (typeof newChildValue === "object" && newChildValue !== null) {
            delete tags[key];
        }
    }
    for(const key in children){
        const childNode = children[key];
        const newChildValue = newValue[key];
        const childValue = childNode.value;
        if (childValue === newChildValue) {
            continue;
        } else if (typeof newChildValue === "object" && newChildValue !== null) {
            updateNode(childNode, newChildValue);
        } else {
            deleteNode(childNode);
            delete children[key];
        }
    }
}
function deleteNode(node) {
    if (node.tag) {
        dirtyTag(node.tag, null);
    }
    dirtyCollection(node);
    for(const key in node.tags){
        dirtyTag(node.tags[key], null);
    }
    for(const key in node.children){
        deleteNode(node.children[key]);
    }
}
// src/lruMemoize.ts
function createSingletonCache(equals) {
    let entry;
    return {
        get (key) {
            if (entry && equals(entry.key, key)) {
                return entry.value;
            }
            return NOT_FOUND;
        },
        put (key, value) {
            entry = {
                key,
                value
            };
        },
        getEntries () {
            return entry ? [
                entry
            ] : [];
        },
        clear () {
            entry = void 0;
        }
    };
}
function createLruCache(maxSize, equals) {
    let entries = [];
    function get(key) {
        const cacheIndex = entries.findIndex((entry)=>equals(key, entry.key));
        if (cacheIndex > -1) {
            const entry = entries[cacheIndex];
            if (cacheIndex > 0) {
                entries.splice(cacheIndex, 1);
                entries.unshift(entry);
            }
            return entry.value;
        }
        return NOT_FOUND;
    }
    function put(key, value) {
        if (get(key) === NOT_FOUND) {
            entries.unshift({
                key,
                value
            });
            if (entries.length > maxSize) {
                entries.pop();
            }
        }
    }
    function getEntries() {
        return entries;
    }
    function clear() {
        entries = [];
    }
    return {
        get,
        put,
        getEntries,
        clear
    };
}
var referenceEqualityCheck = (a, b)=>a === b;
function createCacheKeyComparator(equalityCheck) {
    return function areArgumentsShallowlyEqual(prev, next) {
        if (prev === null || next === null || prev.length !== next.length) {
            return false;
        }
        const { length } = prev;
        for(let i = 0; i < length; i++){
            if (!equalityCheck(prev[i], next[i])) {
                return false;
            }
        }
        return true;
    };
}
function lruMemoize(func, equalityCheckOrOptions) {
    const providedOptions = typeof equalityCheckOrOptions === "object" ? equalityCheckOrOptions : {
        equalityCheck: equalityCheckOrOptions
    };
    const { equalityCheck = referenceEqualityCheck, maxSize = 1, resultEqualityCheck } = providedOptions;
    const comparator = createCacheKeyComparator(equalityCheck);
    let resultsCount = 0;
    const cache = maxSize <= 1 ? createSingletonCache(comparator) : createLruCache(maxSize, comparator);
    function memoized() {
        let value = cache.get(arguments);
        if (value === NOT_FOUND) {
            value = func.apply(null, arguments);
            resultsCount++;
            if (resultEqualityCheck) {
                const entries = cache.getEntries();
                const matchingEntry = entries.find((entry)=>resultEqualityCheck(entry.value, value));
                if (matchingEntry) {
                    value = matchingEntry.value;
                    resultsCount !== 0 && resultsCount--;
                }
            }
            cache.put(arguments, value);
        }
        return value;
    }
    memoized.clearCache = ()=>{
        cache.clear();
        memoized.resetResultsCount();
    };
    memoized.resultsCount = ()=>resultsCount;
    memoized.resetResultsCount = ()=>{
        resultsCount = 0;
    };
    return memoized;
}
// src/autotrackMemoize/autotrackMemoize.ts
function autotrackMemoize(func) {
    const node = createNode([]);
    let lastArgs = null;
    const shallowEqual = createCacheKeyComparator(referenceEqualityCheck);
    const cache = createCache(()=>{
        const res = func.apply(null, node.proxy);
        return res;
    });
    function memoized() {
        if (!shallowEqual(lastArgs, arguments)) {
            updateNode(node, arguments);
            lastArgs = arguments;
        }
        return cache.value;
    }
    memoized.clearCache = ()=>{
        return cache.clear();
    };
    return memoized;
}
// src/weakMapMemoize.ts
var StrongRef = class {
    constructor(value){
        this.value = value;
    }
    deref() {
        return this.value;
    }
};
var Ref = typeof WeakRef !== "undefined" ? WeakRef : StrongRef;
var UNTERMINATED = 0;
var TERMINATED = 1;
function createCacheNode() {
    return {
        s: UNTERMINATED,
        v: void 0,
        o: null,
        p: null
    };
}
function weakMapMemoize(func, options = {}) {
    let fnNode = createCacheNode();
    const { resultEqualityCheck } = options;
    let lastResult;
    let resultsCount = 0;
    function memoized() {
        let cacheNode = fnNode;
        const { length } = arguments;
        for(let i = 0, l = length; i < l; i++){
            const arg = arguments[i];
            if (typeof arg === "function" || typeof arg === "object" && arg !== null) {
                let objectCache = cacheNode.o;
                if (objectCache === null) {
                    cacheNode.o = objectCache = /* @__PURE__ */ new WeakMap();
                }
                const objectNode = objectCache.get(arg);
                if (objectNode === void 0) {
                    cacheNode = createCacheNode();
                    objectCache.set(arg, cacheNode);
                } else {
                    cacheNode = objectNode;
                }
            } else {
                let primitiveCache = cacheNode.p;
                if (primitiveCache === null) {
                    cacheNode.p = primitiveCache = /* @__PURE__ */ new Map();
                }
                const primitiveNode = primitiveCache.get(arg);
                if (primitiveNode === void 0) {
                    cacheNode = createCacheNode();
                    primitiveCache.set(arg, cacheNode);
                } else {
                    cacheNode = primitiveNode;
                }
            }
        }
        const terminatedNode = cacheNode;
        let result;
        if (cacheNode.s === TERMINATED) {
            result = cacheNode.v;
        } else {
            result = func.apply(null, arguments);
            resultsCount++;
            if (resultEqualityCheck) {
                const lastResultValue = lastResult?.deref?.() ?? lastResult;
                if (lastResultValue != null && resultEqualityCheck(lastResultValue, result)) {
                    result = lastResultValue;
                    resultsCount !== 0 && resultsCount--;
                }
                const needsWeakRef = typeof result === "object" && result !== null || typeof result === "function";
                lastResult = needsWeakRef ? new Ref(result) : result;
            }
        }
        terminatedNode.s = TERMINATED;
        terminatedNode.v = result;
        return result;
    }
    memoized.clearCache = ()=>{
        fnNode = createCacheNode();
        memoized.resetResultsCount();
    };
    memoized.resultsCount = ()=>resultsCount;
    memoized.resetResultsCount = ()=>{
        resultsCount = 0;
    };
    return memoized;
}
// src/createSelectorCreator.ts
function createSelectorCreator(memoizeOrOptions, ...memoizeOptionsFromArgs) {
    const createSelectorCreatorOptions = typeof memoizeOrOptions === "function" ? {
        memoize: memoizeOrOptions,
        memoizeOptions: memoizeOptionsFromArgs
    } : memoizeOrOptions;
    const createSelector2 = (...createSelectorArgs)=>{
        let recomputations = 0;
        let dependencyRecomputations = 0;
        let lastResult;
        let directlyPassedOptions = {};
        let resultFunc = createSelectorArgs.pop();
        if (typeof resultFunc === "object") {
            directlyPassedOptions = resultFunc;
            resultFunc = createSelectorArgs.pop();
        }
        assertIsFunction(resultFunc, `createSelector expects an output function after the inputs, but received: [${typeof resultFunc}]`);
        const combinedOptions = {
            ...createSelectorCreatorOptions,
            ...directlyPassedOptions
        };
        const { memoize, memoizeOptions = [], argsMemoize = weakMapMemoize, argsMemoizeOptions = [], devModeChecks = {} } = combinedOptions;
        const finalMemoizeOptions = ensureIsArray(memoizeOptions);
        const finalArgsMemoizeOptions = ensureIsArray(argsMemoizeOptions);
        const dependencies = getDependencies(createSelectorArgs);
        const memoizedResultFunc = memoize(function recomputationWrapper() {
            recomputations++;
            return resultFunc.apply(null, arguments);
        }, ...finalMemoizeOptions);
        let firstRun = true;
        const selector = argsMemoize(function dependenciesChecker() {
            dependencyRecomputations++;
            const inputSelectorResults = collectInputSelectorResults(dependencies, arguments);
            lastResult = memoizedResultFunc.apply(null, inputSelectorResults);
            if ("TURBOPACK compile-time truthy", 1) {
                const { identityFunctionCheck, inputStabilityCheck } = getDevModeChecksExecutionInfo(firstRun, devModeChecks);
                if (identityFunctionCheck.shouldRun) {
                    identityFunctionCheck.run(resultFunc, inputSelectorResults, lastResult);
                }
                if (inputStabilityCheck.shouldRun) {
                    const inputSelectorResultsCopy = collectInputSelectorResults(dependencies, arguments);
                    inputStabilityCheck.run({
                        inputSelectorResults,
                        inputSelectorResultsCopy
                    }, {
                        memoize,
                        memoizeOptions: finalMemoizeOptions
                    }, arguments);
                }
                if (firstRun) firstRun = false;
            }
            return lastResult;
        }, ...finalArgsMemoizeOptions);
        return Object.assign(selector, {
            resultFunc,
            memoizedResultFunc,
            dependencies,
            dependencyRecomputations: ()=>dependencyRecomputations,
            resetDependencyRecomputations: ()=>{
                dependencyRecomputations = 0;
            },
            lastResult: ()=>lastResult,
            recomputations: ()=>recomputations,
            resetRecomputations: ()=>{
                recomputations = 0;
            },
            memoize,
            argsMemoize
        });
    };
    Object.assign(createSelector2, {
        withTypes: ()=>createSelector2
    });
    return createSelector2;
}
var createSelector = /* @__PURE__ */ createSelectorCreator(weakMapMemoize);
// src/createStructuredSelector.ts
var createStructuredSelector = Object.assign((inputSelectorsObject, selectorCreator = createSelector)=>{
    assertIsObject(inputSelectorsObject, `createStructuredSelector expects first argument to be an object where each property is a selector, instead received a ${typeof inputSelectorsObject}`);
    const inputSelectorKeys = Object.keys(inputSelectorsObject);
    const dependencies = inputSelectorKeys.map((key)=>inputSelectorsObject[key]);
    const structuredSelector = selectorCreator(dependencies, (...inputSelectorResults)=>{
        return inputSelectorResults.reduce((composition, value, index)=>{
            composition[inputSelectorKeys[index]] = value;
            return composition;
        }, {});
    });
    return structuredSelector;
}, {
    withTypes: ()=>createStructuredSelector
});
;
 //# sourceMappingURL=reselect.mjs.map
}),
"[project]/node_modules/redux/dist/redux.mjs [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "__DO_NOT_USE__ActionTypes",
    ()=>actionTypes_default,
    "applyMiddleware",
    ()=>applyMiddleware,
    "bindActionCreators",
    ()=>bindActionCreators,
    "combineReducers",
    ()=>combineReducers,
    "compose",
    ()=>compose,
    "createStore",
    ()=>createStore,
    "isAction",
    ()=>isAction,
    "isPlainObject",
    ()=>isPlainObject,
    "legacy_createStore",
    ()=>legacy_createStore
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [app-client] (ecmascript)");
// src/utils/formatProdErrorMessage.ts
function formatProdErrorMessage(code) {
    return `Minified Redux error #${code}; visit https://redux.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}
// src/utils/symbol-observable.ts
var $$observable = /* @__PURE__ */ (()=>typeof Symbol === "function" && Symbol.observable || "@@observable")();
var symbol_observable_default = $$observable;
// src/utils/actionTypes.ts
var randomString = ()=>Math.random().toString(36).substring(7).split("").join(".");
var ActionTypes = {
    INIT: `@@redux/INIT${/* @__PURE__ */ randomString()}`,
    REPLACE: `@@redux/REPLACE${/* @__PURE__ */ randomString()}`,
    PROBE_UNKNOWN_ACTION: ()=>`@@redux/PROBE_UNKNOWN_ACTION${randomString()}`
};
var actionTypes_default = ActionTypes;
// src/utils/isPlainObject.ts
function isPlainObject(obj) {
    if (typeof obj !== "object" || obj === null) return false;
    let proto = obj;
    while(Object.getPrototypeOf(proto) !== null){
        proto = Object.getPrototypeOf(proto);
    }
    return Object.getPrototypeOf(obj) === proto || Object.getPrototypeOf(obj) === null;
}
// src/utils/kindOf.ts
function miniKindOf(val) {
    if (val === void 0) return "undefined";
    if (val === null) return "null";
    const type = typeof val;
    switch(type){
        case "boolean":
        case "string":
        case "number":
        case "symbol":
        case "function":
            {
                return type;
            }
    }
    if (Array.isArray(val)) return "array";
    if (isDate(val)) return "date";
    if (isError(val)) return "error";
    const constructorName = ctorName(val);
    switch(constructorName){
        case "Symbol":
        case "Promise":
        case "WeakMap":
        case "WeakSet":
        case "Map":
        case "Set":
            return constructorName;
    }
    return Object.prototype.toString.call(val).slice(8, -1).toLowerCase().replace(/\s/g, "");
}
function ctorName(val) {
    return typeof val.constructor === "function" ? val.constructor.name : null;
}
function isError(val) {
    return val instanceof Error || typeof val.message === "string" && val.constructor && typeof val.constructor.stackTraceLimit === "number";
}
function isDate(val) {
    if (val instanceof Date) return true;
    return typeof val.toDateString === "function" && typeof val.getDate === "function" && typeof val.setDate === "function";
}
function kindOf(val) {
    let typeOfVal = typeof val;
    if ("TURBOPACK compile-time truthy", 1) {
        typeOfVal = miniKindOf(val);
    }
    return typeOfVal;
}
// src/createStore.ts
function createStore(reducer, preloadedState, enhancer) {
    if (typeof reducer !== "function") {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Expected the root reducer to be a function. Instead, received: '${kindOf(reducer)}'`);
    }
    if (typeof preloadedState === "function" && typeof enhancer === "function" || typeof enhancer === "function" && typeof arguments[3] === "function") {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "It looks like you are passing several store enhancers to createStore(). This is not supported. Instead, compose them together to a single function. See https://redux.js.org/tutorials/fundamentals/part-4-store#creating-a-store-with-enhancers for an example.");
    }
    if (typeof preloadedState === "function" && typeof enhancer === "undefined") {
        enhancer = preloadedState;
        preloadedState = void 0;
    }
    if (typeof enhancer !== "undefined") {
        if (typeof enhancer !== "function") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Expected the enhancer to be a function. Instead, received: '${kindOf(enhancer)}'`);
        }
        return enhancer(createStore)(reducer, preloadedState);
    }
    let currentReducer = reducer;
    let currentState = preloadedState;
    let currentListeners = /* @__PURE__ */ new Map();
    let nextListeners = currentListeners;
    let listenerIdCounter = 0;
    let isDispatching = false;
    function ensureCanMutateNextListeners() {
        if (nextListeners === currentListeners) {
            nextListeners = /* @__PURE__ */ new Map();
            currentListeners.forEach((listener, key)=>{
                nextListeners.set(key, listener);
            });
        }
    }
    function getState() {
        if (isDispatching) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "You may not call store.getState() while the reducer is executing. The reducer has already received the state as an argument. Pass it down from the top reducer instead of reading it from the store.");
        }
        return currentState;
    }
    function subscribe(listener) {
        if (typeof listener !== "function") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Expected the listener to be a function. Instead, received: '${kindOf(listener)}'`);
        }
        if (isDispatching) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "You may not call store.subscribe() while the reducer is executing. If you would like to be notified after the store has been updated, subscribe from a component and invoke store.getState() in the callback to access the latest state. See https://redux.js.org/api/store#subscribelistener for more details.");
        }
        let isSubscribed = true;
        ensureCanMutateNextListeners();
        const listenerId = listenerIdCounter++;
        nextListeners.set(listenerId, listener);
        return function unsubscribe() {
            if (!isSubscribed) {
                return;
            }
            if (isDispatching) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "You may not unsubscribe from a store listener while the reducer is executing. See https://redux.js.org/api/store#subscribelistener for more details.");
            }
            isSubscribed = false;
            ensureCanMutateNextListeners();
            nextListeners.delete(listenerId);
            currentListeners = null;
        };
    }
    function dispatch(action) {
        if (!isPlainObject(action)) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Actions must be plain objects. Instead, the actual type was: '${kindOf(action)}'. You may need to add middleware to your store setup to handle dispatching other values, such as 'redux-thunk' to handle dispatching functions. See https://redux.js.org/tutorials/fundamentals/part-4-store#middleware and https://redux.js.org/tutorials/fundamentals/part-6-async-logic#using-the-redux-thunk-middleware for examples.`);
        }
        if (typeof action.type === "undefined") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : 'Actions may not have an undefined "type" property. You may have misspelled an action type string constant.');
        }
        if (typeof action.type !== "string") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Action "type" property must be a string. Instead, the actual type was: '${kindOf(action.type)}'. Value was: '${action.type}' (stringified)`);
        }
        if (isDispatching) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Reducers may not dispatch actions.");
        }
        try {
            isDispatching = true;
            currentState = currentReducer(currentState, action);
        } finally{
            isDispatching = false;
        }
        const listeners = currentListeners = nextListeners;
        listeners.forEach((listener)=>{
            listener();
        });
        return action;
    }
    function replaceReducer(nextReducer) {
        if (typeof nextReducer !== "function") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Expected the nextReducer to be a function. Instead, received: '${kindOf(nextReducer)}`);
        }
        currentReducer = nextReducer;
        dispatch({
            type: actionTypes_default.REPLACE
        });
    }
    function observable() {
        const outerSubscribe = subscribe;
        return {
            /**
       * The minimal observable subscription method.
       * @param observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */ subscribe (observer) {
                if (typeof observer !== "object" || observer === null) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `Expected the observer to be an object. Instead, received: '${kindOf(observer)}'`);
                }
                function observeState() {
                    const observerAsObserver = observer;
                    if (observerAsObserver.next) {
                        observerAsObserver.next(getState());
                    }
                }
                observeState();
                const unsubscribe = outerSubscribe(observeState);
                return {
                    unsubscribe
                };
            },
            [symbol_observable_default] () {
                return this;
            }
        };
    }
    dispatch({
        type: actionTypes_default.INIT
    });
    const store = {
        dispatch,
        subscribe,
        getState,
        replaceReducer,
        [symbol_observable_default]: observable
    };
    return store;
}
function legacy_createStore(reducer, preloadedState, enhancer) {
    return createStore(reducer, preloadedState, enhancer);
}
// src/utils/warning.ts
function warning(message) {
    if (typeof console !== "undefined" && typeof console.error === "function") {
        console.error(message);
    }
    try {
        throw new Error(message);
    } catch (e) {}
}
// src/combineReducers.ts
function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
    const reducerKeys = Object.keys(reducers);
    const argumentName = action && action.type === actionTypes_default.INIT ? "preloadedState argument passed to createStore" : "previous state received by the reducer";
    if (reducerKeys.length === 0) {
        return "Store does not have a valid reducer. Make sure the argument passed to combineReducers is an object whose values are reducers.";
    }
    if (!isPlainObject(inputState)) {
        return `The ${argumentName} has unexpected type of "${kindOf(inputState)}". Expected argument to be an object with the following keys: "${reducerKeys.join('", "')}"`;
    }
    const unexpectedKeys = Object.keys(inputState).filter((key)=>!reducers.hasOwnProperty(key) && !unexpectedKeyCache[key]);
    unexpectedKeys.forEach((key)=>{
        unexpectedKeyCache[key] = true;
    });
    if (action && action.type === actionTypes_default.REPLACE) return;
    if (unexpectedKeys.length > 0) {
        return `Unexpected ${unexpectedKeys.length > 1 ? "keys" : "key"} "${unexpectedKeys.join('", "')}" found in ${argumentName}. Expected to find one of the known reducer keys instead: "${reducerKeys.join('", "')}". Unexpected keys will be ignored.`;
    }
}
function assertReducerShape(reducers) {
    Object.keys(reducers).forEach((key)=>{
        const reducer = reducers[key];
        const initialState = reducer(void 0, {
            type: actionTypes_default.INIT
        });
        if (typeof initialState === "undefined") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `The slice reducer for key "${key}" returned undefined during initialization. If the state passed to the reducer is undefined, you must explicitly return the initial state. The initial state may not be undefined. If you don't want to set a value for this reducer, you can use null instead of undefined.`);
        }
        if (typeof reducer(void 0, {
            type: actionTypes_default.PROBE_UNKNOWN_ACTION()
        }) === "undefined") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `The slice reducer for key "${key}" returned undefined when probed with a random type. Don't try to handle '${actionTypes_default.INIT}' or other actions in "redux/*" namespace. They are considered private. Instead, you must return the current state for any unknown actions, unless it is undefined, in which case you must return the initial state, regardless of the action type. The initial state may not be undefined, but can be null.`);
        }
    });
}
function combineReducers(reducers) {
    const reducerKeys = Object.keys(reducers);
    const finalReducers = {};
    for(let i = 0; i < reducerKeys.length; i++){
        const key = reducerKeys[i];
        if ("TURBOPACK compile-time truthy", 1) {
            if (typeof reducers[key] === "undefined") {
                warning(`No reducer provided for key "${key}"`);
            }
        }
        if (typeof reducers[key] === "function") {
            finalReducers[key] = reducers[key];
        }
    }
    const finalReducerKeys = Object.keys(finalReducers);
    let unexpectedKeyCache;
    if (("TURBOPACK compile-time value", "development") !== "production") {
        unexpectedKeyCache = {};
    }
    let shapeAssertionError;
    try {
        assertReducerShape(finalReducers);
    } catch (e) {
        shapeAssertionError = e;
    }
    return function combination(state = {}, action) {
        if (shapeAssertionError) {
            throw shapeAssertionError;
        }
        if ("TURBOPACK compile-time truthy", 1) {
            const warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);
            if (warningMessage) {
                warning(warningMessage);
            }
        }
        let hasChanged = false;
        const nextState = {};
        for(let i = 0; i < finalReducerKeys.length; i++){
            const key = finalReducerKeys[i];
            const reducer = finalReducers[key];
            const previousStateForKey = state[key];
            const nextStateForKey = reducer(previousStateForKey, action);
            if (typeof nextStateForKey === "undefined") {
                const actionType = action && action.type;
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `When called with an action of type ${actionType ? `"${String(actionType)}"` : "(unknown type)"}, the slice reducer for key "${key}" returned undefined. To ignore an action, you must explicitly return the previous state. If you want this reducer to hold no value, you can return null instead of undefined.`);
            }
            nextState[key] = nextStateForKey;
            hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
        }
        hasChanged = hasChanged || finalReducerKeys.length !== Object.keys(state).length;
        return hasChanged ? nextState : state;
    };
}
// src/bindActionCreators.ts
function bindActionCreator(actionCreator, dispatch) {
    return function(...args) {
        return dispatch(actionCreator.apply(this, args));
    };
}
function bindActionCreators(actionCreators, dispatch) {
    if (typeof actionCreators === "function") {
        return bindActionCreator(actionCreators, dispatch);
    }
    if (typeof actionCreators !== "object" || actionCreators === null) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `bindActionCreators expected an object or a function, but instead received: '${kindOf(actionCreators)}'. Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?`);
    }
    const boundActionCreators = {};
    for(const key in actionCreators){
        const actionCreator = actionCreators[key];
        if (typeof actionCreator === "function") {
            boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
        }
    }
    return boundActionCreators;
}
// src/compose.ts
function compose(...funcs) {
    if (funcs.length === 0) {
        return (arg)=>arg;
    }
    if (funcs.length === 1) {
        return funcs[0];
    }
    return funcs.reduce((a, b)=>(...args)=>a(b(...args)));
}
// src/applyMiddleware.ts
function applyMiddleware(...middlewares) {
    return (createStore2)=>(reducer, preloadedState)=>{
            const store = createStore2(reducer, preloadedState);
            let dispatch = ()=>{
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
            };
            const middlewareAPI = {
                getState: store.getState,
                dispatch: (action, ...args)=>dispatch(action, ...args)
            };
            const chain = middlewares.map((middleware)=>middleware(middlewareAPI));
            dispatch = compose(...chain)(store.dispatch);
            return {
                ...store,
                dispatch
            };
        };
}
// src/utils/isAction.ts
function isAction(action) {
    return isPlainObject(action) && "type" in action && typeof action.type === "string";
}
;
 //# sourceMappingURL=redux.mjs.map
}),
"[project]/node_modules/redux-thunk/dist/redux-thunk.mjs [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "thunk",
    ()=>thunk,
    "withExtraArgument",
    ()=>withExtraArgument
]);
// src/index.ts
function createThunkMiddleware(extraArgument) {
    const middleware = ({ dispatch, getState })=>(next)=>(action)=>{
                if (typeof action === "function") {
                    return action(dispatch, getState, extraArgument);
                }
                return next(action);
            };
    return middleware;
}
var thunk = createThunkMiddleware();
var withExtraArgument = createThunkMiddleware;
;
}),
"[project]/node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

// src/index.ts
__turbopack_context__.s([
    "ReducerType",
    ()=>ReducerType,
    "SHOULD_AUTOBATCH",
    ()=>SHOULD_AUTOBATCH,
    "TaskAbortError",
    ()=>TaskAbortError,
    "Tuple",
    ()=>Tuple,
    "addListener",
    ()=>addListener,
    "asyncThunkCreator",
    ()=>asyncThunkCreator,
    "autoBatchEnhancer",
    ()=>autoBatchEnhancer,
    "buildCreateSlice",
    ()=>buildCreateSlice,
    "clearAllListeners",
    ()=>clearAllListeners,
    "combineSlices",
    ()=>combineSlices,
    "configureStore",
    ()=>configureStore,
    "createAction",
    ()=>createAction,
    "createActionCreatorInvariantMiddleware",
    ()=>createActionCreatorInvariantMiddleware,
    "createAsyncThunk",
    ()=>createAsyncThunk,
    "createDraftSafeSelector",
    ()=>createDraftSafeSelector,
    "createDraftSafeSelectorCreator",
    ()=>createDraftSafeSelectorCreator,
    "createDynamicMiddleware",
    ()=>createDynamicMiddleware,
    "createEntityAdapter",
    ()=>createEntityAdapter,
    "createImmutableStateInvariantMiddleware",
    ()=>createImmutableStateInvariantMiddleware,
    "createListenerMiddleware",
    ()=>createListenerMiddleware,
    "createReducer",
    ()=>createReducer,
    "createSerializableStateInvariantMiddleware",
    ()=>createSerializableStateInvariantMiddleware,
    "createSlice",
    ()=>createSlice,
    "findNonSerializableValue",
    ()=>findNonSerializableValue,
    "formatProdErrorMessage",
    ()=>formatProdErrorMessage,
    "isActionCreator",
    ()=>isActionCreator,
    "isAllOf",
    ()=>isAllOf,
    "isAnyOf",
    ()=>isAnyOf,
    "isAsyncThunkAction",
    ()=>isAsyncThunkAction,
    "isFluxStandardAction",
    ()=>isFSA,
    "isFulfilled",
    ()=>isFulfilled,
    "isImmutableDefault",
    ()=>isImmutableDefault,
    "isPending",
    ()=>isPending,
    "isPlain",
    ()=>isPlain,
    "isRejected",
    ()=>isRejected,
    "isRejectedWithValue",
    ()=>isRejectedWithValue,
    "miniSerializeError",
    ()=>miniSerializeError,
    "nanoid",
    ()=>nanoid,
    "prepareAutoBatched",
    ()=>prepareAutoBatched,
    "removeListener",
    ()=>removeListener,
    "unwrapResult",
    ()=>unwrapResult
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@reduxjs/toolkit/node_modules/immer/dist/immer.mjs [app-client] (ecmascript)");
// src/index.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/reselect/dist/reselect.mjs [app-client] (ecmascript)");
// src/reduxImports.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/redux/dist/redux.mjs [app-client] (ecmascript)");
// src/getDefaultMiddleware.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2d$thunk$2f$dist$2f$redux$2d$thunk$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/redux-thunk/dist/redux-thunk.mjs [app-client] (ecmascript)");
;
;
;
;
;
// src/createDraftSafeSelector.ts
var createDraftSafeSelectorCreator = (...args)=>{
    const createSelector2 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSelectorCreator"])(...args);
    const createDraftSafeSelector2 = Object.assign((...args2)=>{
        const selector = createSelector2(...args2);
        const wrappedSelector = (value, ...rest)=>selector((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraft"])(value) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["current"])(value) : value, ...rest);
        Object.assign(wrappedSelector, selector);
        return wrappedSelector;
    }, {
        withTypes: ()=>createDraftSafeSelector2
    });
    return createDraftSafeSelector2;
};
var createDraftSafeSelector = /* @__PURE__ */ createDraftSafeSelectorCreator(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["weakMapMemoize"]);
;
// src/devtoolsExtension.ts
var composeWithDevTools = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ : function() {
    if (arguments.length === 0) return void 0;
    if (typeof arguments[0] === "object") return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"];
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"].apply(null, arguments);
};
var devToolsEnhancer = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION__ ? window.__REDUX_DEVTOOLS_EXTENSION__ : function() {
    return function(noop3) {
        return noop3;
    };
};
;
// src/tsHelpers.ts
var hasMatchFunction = (v)=>{
    return v && typeof v.match === "function";
};
// src/createAction.ts
function createAction(type, prepareAction) {
    function actionCreator(...args) {
        if (prepareAction) {
            let prepared = prepareAction(...args);
            if (!prepared) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "prepareAction did not return an object");
            }
            return {
                type,
                payload: prepared.payload,
                ..."meta" in prepared && {
                    meta: prepared.meta
                },
                ..."error" in prepared && {
                    error: prepared.error
                }
            };
        }
        return {
            type,
            payload: args[0]
        };
    }
    actionCreator.toString = ()=>`${type}`;
    actionCreator.type = type;
    actionCreator.match = (action)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAction"])(action) && action.type === type;
    return actionCreator;
}
function isActionCreator(action) {
    return typeof action === "function" && "type" in action && // hasMatchFunction only wants Matchers but I don't see the point in rewriting it
    hasMatchFunction(action);
}
function isFSA(action) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAction"])(action) && Object.keys(action).every(isValidKey);
}
function isValidKey(key) {
    return [
        "type",
        "payload",
        "error",
        "meta"
    ].indexOf(key) > -1;
}
// src/actionCreatorInvariantMiddleware.ts
function getMessage(type) {
    const splitType = type ? `${type}`.split("/") : [];
    const actionName = splitType[splitType.length - 1] || "actionCreator";
    return `Detected an action creator with type "${type || "unknown"}" being dispatched. 
Make sure you're calling the action creator before dispatching, i.e. \`dispatch(${actionName}())\` instead of \`dispatch(${actionName})\`. This is necessary even if the action has no payload.`;
}
function createActionCreatorInvariantMiddleware(options = {}) {
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    const { isActionCreator: isActionCreator2 = isActionCreator } = options;
    return ()=>(next)=>(action)=>{
                if (isActionCreator2(action)) {
                    console.warn(getMessage(action.type));
                }
                return next(action);
            };
}
// src/utils.ts
function getTimeMeasureUtils(maxDelay, fnName) {
    let elapsed = 0;
    return {
        measureTime (fn) {
            const started = Date.now();
            try {
                return fn();
            } finally{
                const finished = Date.now();
                elapsed += finished - started;
            }
        },
        warnIfExceeded () {
            if (elapsed > maxDelay) {
                console.warn(`${fnName} took ${elapsed}ms, which is more than the warning threshold of ${maxDelay}ms. 
If your state or actions are very large, you may want to disable the middleware as it might cause too much of a slowdown in development mode. See https://redux-toolkit.js.org/api/getDefaultMiddleware for instructions.
It is disabled in production builds, so you don't need to worry about that.`);
            }
        }
    };
}
var Tuple = class _Tuple extends Array {
    constructor(...items){
        super(...items);
        Object.setPrototypeOf(this, _Tuple.prototype);
    }
    static get [Symbol.species]() {
        return _Tuple;
    }
    concat(...arr) {
        return super.concat.apply(this, arr);
    }
    prepend(...arr) {
        if (arr.length === 1 && Array.isArray(arr[0])) {
            return new _Tuple(...arr[0].concat(this));
        }
        return new _Tuple(...arr.concat(this));
    }
};
function freezeDraftable(val) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraftable"])(val) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["produce"])(val, ()=>{}) : val;
}
function getOrInsertComputed(map, key, compute) {
    if (map.has(key)) return map.get(key);
    return map.set(key, compute(key)).get(key);
}
// src/immutableStateInvariantMiddleware.ts
function isImmutableDefault(value) {
    return typeof value !== "object" || value == null || Object.isFrozen(value);
}
function trackForMutations(isImmutable, ignoredPaths, obj) {
    const trackedProperties = trackProperties(isImmutable, ignoredPaths, obj);
    return {
        detectMutations () {
            return detectMutations(isImmutable, ignoredPaths, trackedProperties, obj);
        }
    };
}
function trackProperties(isImmutable, ignoredPaths = [], obj, path = "", checkedObjects = /* @__PURE__ */ new Set()) {
    const tracked = {
        value: obj
    };
    if (!isImmutable(obj) && !checkedObjects.has(obj)) {
        checkedObjects.add(obj);
        tracked.children = {};
        const hasIgnoredPaths = ignoredPaths.length > 0;
        for(const key in obj){
            const nestedPath = path ? path + "." + key : key;
            if (hasIgnoredPaths) {
                const hasMatches = ignoredPaths.some((ignored)=>{
                    if (ignored instanceof RegExp) {
                        return ignored.test(nestedPath);
                    }
                    return nestedPath === ignored;
                });
                if (hasMatches) {
                    continue;
                }
            }
            tracked.children[key] = trackProperties(isImmutable, ignoredPaths, obj[key], nestedPath);
        }
    }
    return tracked;
}
function detectMutations(isImmutable, ignoredPaths = [], trackedProperty, obj, sameParentRef = false, path = "") {
    const prevObj = trackedProperty ? trackedProperty.value : void 0;
    const sameRef = prevObj === obj;
    if (sameParentRef && !sameRef && !Number.isNaN(obj)) {
        return {
            wasMutated: true,
            path
        };
    }
    if (isImmutable(prevObj) || isImmutable(obj)) {
        return {
            wasMutated: false
        };
    }
    const keysToDetect = {};
    for(let key in trackedProperty.children){
        keysToDetect[key] = true;
    }
    for(let key in obj){
        keysToDetect[key] = true;
    }
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for(let key in keysToDetect){
        const nestedPath = path ? path + "." + key : key;
        if (hasIgnoredPaths) {
            const hasMatches = ignoredPaths.some((ignored)=>{
                if (ignored instanceof RegExp) {
                    return ignored.test(nestedPath);
                }
                return nestedPath === ignored;
            });
            if (hasMatches) {
                continue;
            }
        }
        const result = detectMutations(isImmutable, ignoredPaths, trackedProperty.children[key], obj[key], sameRef, nestedPath);
        if (result.wasMutated) {
            return result;
        }
    }
    return {
        wasMutated: false
    };
}
function createImmutableStateInvariantMiddleware(options = {}) {
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    else {
        let stringify2 = function(obj, serializer, indent, decycler) {
            return JSON.stringify(obj, getSerialize2(serializer, decycler), indent);
        }, getSerialize2 = function(serializer, decycler) {
            let stack = [], keys = [];
            if (!decycler) decycler = function(_, value) {
                if (stack[0] === value) return "[Circular ~]";
                return "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
            };
            return function(key, value) {
                if (stack.length > 0) {
                    var thisPos = stack.indexOf(this);
                    ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
                    ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
                    if (~stack.indexOf(value)) value = decycler.call(this, key, value);
                } else stack.push(value);
                return serializer == null ? value : serializer.call(this, key, value);
            };
        };
        var stringify = stringify2, getSerialize = getSerialize2;
        let { isImmutable = isImmutableDefault, ignoredPaths, warnAfter = 32 } = options;
        const track = trackForMutations.bind(null, isImmutable, ignoredPaths);
        return ({ getState })=>{
            let state = getState();
            let tracker = track(state);
            let result;
            return (next)=>(action)=>{
                    const measureUtils = getTimeMeasureUtils(warnAfter, "ImmutableStateInvariantMiddleware");
                    measureUtils.measureTime(()=>{
                        state = getState();
                        result = tracker.detectMutations();
                        tracker = track(state);
                        if (result.wasMutated) {
                            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `A state mutation was detected between dispatches, in the path '${result.path || ""}'.  This may cause incorrect behavior. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
                        }
                    });
                    const dispatchedAction = next(action);
                    measureUtils.measureTime(()=>{
                        state = getState();
                        result = tracker.detectMutations();
                        tracker = track(state);
                        if (result.wasMutated) {
                            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `A state mutation was detected inside a dispatch, in the path: ${result.path || ""}. Take a look at the reducer(s) handling the action ${stringify2(action)}. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
                        }
                    });
                    measureUtils.warnIfExceeded();
                    return dispatchedAction;
                };
        };
    }
}
// src/serializableStateInvariantMiddleware.ts
function isPlain(val) {
    const type = typeof val;
    return val == null || type === "string" || type === "boolean" || type === "number" || Array.isArray(val) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isPlainObject"])(val);
}
function findNonSerializableValue(value, path = "", isSerializable = isPlain, getEntries, ignoredPaths = [], cache) {
    let foundNestedSerializable;
    if (!isSerializable(value)) {
        return {
            keyPath: path || "<root>",
            value
        };
    }
    if (typeof value !== "object" || value === null) {
        return false;
    }
    if (cache?.has(value)) return false;
    const entries = getEntries != null ? getEntries(value) : Object.entries(value);
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for (const [key, nestedValue] of entries){
        const nestedPath = path ? path + "." + key : key;
        if (hasIgnoredPaths) {
            const hasMatches = ignoredPaths.some((ignored)=>{
                if (ignored instanceof RegExp) {
                    return ignored.test(nestedPath);
                }
                return nestedPath === ignored;
            });
            if (hasMatches) {
                continue;
            }
        }
        if (!isSerializable(nestedValue)) {
            return {
                keyPath: nestedPath,
                value: nestedValue
            };
        }
        if (typeof nestedValue === "object") {
            foundNestedSerializable = findNonSerializableValue(nestedValue, nestedPath, isSerializable, getEntries, ignoredPaths, cache);
            if (foundNestedSerializable) {
                return foundNestedSerializable;
            }
        }
    }
    if (cache && isNestedFrozen(value)) cache.add(value);
    return false;
}
function isNestedFrozen(value) {
    if (!Object.isFrozen(value)) return false;
    for (const nestedValue of Object.values(value)){
        if (typeof nestedValue !== "object" || nestedValue === null) continue;
        if (!isNestedFrozen(nestedValue)) return false;
    }
    return true;
}
function createSerializableStateInvariantMiddleware(options = {}) {
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    else {
        const { isSerializable = isPlain, getEntries, ignoredActions = [], ignoredActionPaths = [
            "meta.arg",
            "meta.baseQueryMeta"
        ], ignoredPaths = [], warnAfter = 32, ignoreState = false, ignoreActions = false, disableCache = false } = options;
        const cache = !disableCache && WeakSet ? /* @__PURE__ */ new WeakSet() : void 0;
        return (storeAPI)=>(next)=>(action)=>{
                    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAction"])(action)) {
                        return next(action);
                    }
                    const result = next(action);
                    const measureUtils = getTimeMeasureUtils(warnAfter, "SerializableStateInvariantMiddleware");
                    if (!ignoreActions && !(ignoredActions.length && ignoredActions.indexOf(action.type) !== -1)) {
                        measureUtils.measureTime(()=>{
                            const foundActionNonSerializableValue = findNonSerializableValue(action, "", isSerializable, getEntries, ignoredActionPaths, cache);
                            if (foundActionNonSerializableValue) {
                                const { keyPath, value } = foundActionNonSerializableValue;
                                console.error(`A non-serializable value was detected in an action, in the path: \`${keyPath}\`. Value:`, value, "\nTake a look at the logic that dispatched this action: ", action, "\n(See https://redux.js.org/faq/actions#why-should-type-be-a-string-or-at-least-serializable-why-should-my-action-types-be-constants)", "\n(To allow non-serializable values see: https://redux-toolkit.js.org/usage/usage-guide#working-with-non-serializable-data)");
                            }
                        });
                    }
                    if (!ignoreState) {
                        measureUtils.measureTime(()=>{
                            const state = storeAPI.getState();
                            const foundStateNonSerializableValue = findNonSerializableValue(state, "", isSerializable, getEntries, ignoredPaths, cache);
                            if (foundStateNonSerializableValue) {
                                const { keyPath, value } = foundStateNonSerializableValue;
                                console.error(`A non-serializable value was detected in the state, in the path: \`${keyPath}\`. Value:`, value, `
Take a look at the reducer(s) handling this action type: ${action.type}.
(See https://redux.js.org/faq/organizing-state#can-i-put-functions-promises-or-other-non-serializable-items-in-my-store-state)`);
                            }
                        });
                        measureUtils.warnIfExceeded();
                    }
                    return result;
                };
    }
}
// src/getDefaultMiddleware.ts
function isBoolean(x) {
    return typeof x === "boolean";
}
var buildGetDefaultMiddleware = ()=>function getDefaultMiddleware(options) {
        const { thunk = true, immutableCheck = true, serializableCheck = true, actionCreatorCheck = true } = options ?? {};
        let middlewareArray = new Tuple();
        if (thunk) {
            if (isBoolean(thunk)) {
                middlewareArray.push(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2d$thunk$2f$dist$2f$redux$2d$thunk$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["thunk"]);
            } else {
                middlewareArray.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2d$thunk$2f$dist$2f$redux$2d$thunk$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["withExtraArgument"])(thunk.extraArgument));
            }
        }
        if ("TURBOPACK compile-time truthy", 1) {
            if (immutableCheck) {
                let immutableOptions = {};
                if (!isBoolean(immutableCheck)) {
                    immutableOptions = immutableCheck;
                }
                middlewareArray.unshift(createImmutableStateInvariantMiddleware(immutableOptions));
            }
            if (serializableCheck) {
                let serializableOptions = {};
                if (!isBoolean(serializableCheck)) {
                    serializableOptions = serializableCheck;
                }
                middlewareArray.push(createSerializableStateInvariantMiddleware(serializableOptions));
            }
            if (actionCreatorCheck) {
                let actionCreatorOptions = {};
                if (!isBoolean(actionCreatorCheck)) {
                    actionCreatorOptions = actionCreatorCheck;
                }
                middlewareArray.unshift(createActionCreatorInvariantMiddleware(actionCreatorOptions));
            }
        }
        return middlewareArray;
    };
// src/autoBatchEnhancer.ts
var SHOULD_AUTOBATCH = "RTK_autoBatch";
var prepareAutoBatched = ()=>(payload)=>({
            payload,
            meta: {
                [SHOULD_AUTOBATCH]: true
            }
        });
var createQueueWithTimer = (timeout)=>{
    return (notify)=>{
        setTimeout(notify, timeout);
    };
};
var autoBatchEnhancer = (options = {
    type: "raf"
})=>(next)=>(...args)=>{
            const store = next(...args);
            let notifying = true;
            let shouldNotifyAtEndOfTick = false;
            let notificationQueued = false;
            const listeners = /* @__PURE__ */ new Set();
            const queueCallback = options.type === "tick" ? queueMicrotask : options.type === "raf" ? // requestAnimationFrame won't exist in SSR environments. Fall back to a vague approximation just to keep from erroring.
            typeof window !== "undefined" && window.requestAnimationFrame ? window.requestAnimationFrame : createQueueWithTimer(10) : options.type === "callback" ? options.queueNotification : createQueueWithTimer(options.timeout);
            const notifyListeners = ()=>{
                notificationQueued = false;
                if (shouldNotifyAtEndOfTick) {
                    shouldNotifyAtEndOfTick = false;
                    listeners.forEach((l)=>l());
                }
            };
            return Object.assign({}, store, {
                // Override the base `store.subscribe` method to keep original listeners
                // from running if we're delaying notifications
                subscribe (listener2) {
                    const wrappedListener = ()=>notifying && listener2();
                    const unsubscribe = store.subscribe(wrappedListener);
                    listeners.add(listener2);
                    return ()=>{
                        unsubscribe();
                        listeners.delete(listener2);
                    };
                },
                // Override the base `store.dispatch` method so that we can check actions
                // for the `shouldAutoBatch` flag and determine if batching is active
                dispatch (action) {
                    try {
                        notifying = !action?.meta?.[SHOULD_AUTOBATCH];
                        shouldNotifyAtEndOfTick = !notifying;
                        if (shouldNotifyAtEndOfTick) {
                            if (!notificationQueued) {
                                notificationQueued = true;
                                queueCallback(notifyListeners);
                            }
                        }
                        return store.dispatch(action);
                    } finally{
                        notifying = true;
                    }
                }
            });
        };
// src/getDefaultEnhancers.ts
var buildGetDefaultEnhancers = (middlewareEnhancer)=>function getDefaultEnhancers(options) {
        const { autoBatch = true } = options ?? {};
        let enhancerArray = new Tuple(middlewareEnhancer);
        if (autoBatch) {
            enhancerArray.push(autoBatchEnhancer(typeof autoBatch === "object" ? autoBatch : void 0));
        }
        return enhancerArray;
    };
// src/configureStore.ts
function configureStore(options) {
    const getDefaultMiddleware = buildGetDefaultMiddleware();
    const { reducer = void 0, middleware, devTools = true, duplicateMiddlewareCheck = true, preloadedState = void 0, enhancers = void 0 } = options || {};
    let rootReducer;
    if (typeof reducer === "function") {
        rootReducer = reducer;
    } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isPlainObject"])(reducer)) {
        rootReducer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["combineReducers"])(reducer);
    } else {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`reducer` is a required argument, and must be a function or an object of functions that can be passed to combineReducers");
    }
    if (("TURBOPACK compile-time value", "development") !== "production" && middleware && typeof middleware !== "function") {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`middleware` field must be a callback");
    }
    let finalMiddleware;
    if (typeof middleware === "function") {
        finalMiddleware = middleware(getDefaultMiddleware);
        if (("TURBOPACK compile-time value", "development") !== "production" && !Array.isArray(finalMiddleware)) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "when using a middleware builder function, an array of middleware must be returned");
        }
    } else {
        finalMiddleware = getDefaultMiddleware();
    }
    if (("TURBOPACK compile-time value", "development") !== "production" && finalMiddleware.some((item)=>typeof item !== "function")) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "each middleware provided to configureStore must be a function");
    }
    if (("TURBOPACK compile-time value", "development") !== "production" && duplicateMiddlewareCheck) {
        let middlewareReferences = /* @__PURE__ */ new Set();
        finalMiddleware.forEach((middleware2)=>{
            if (middlewareReferences.has(middleware2)) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Duplicate middleware references found when creating the store. Ensure that each middleware is only included once.");
            }
            middlewareReferences.add(middleware2);
        });
    }
    let finalCompose = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"];
    if (devTools) {
        finalCompose = composeWithDevTools({
            // Enable capture of stack traces for dispatched Redux actions
            trace: ("TURBOPACK compile-time value", "development") !== "production",
            ...typeof devTools === "object" && devTools
        });
    }
    const middlewareEnhancer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["applyMiddleware"])(...finalMiddleware);
    const getDefaultEnhancers = buildGetDefaultEnhancers(middlewareEnhancer);
    if (("TURBOPACK compile-time value", "development") !== "production" && enhancers && typeof enhancers !== "function") {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`enhancers` field must be a callback");
    }
    let storeEnhancers = typeof enhancers === "function" ? enhancers(getDefaultEnhancers) : getDefaultEnhancers();
    if (("TURBOPACK compile-time value", "development") !== "production" && !Array.isArray(storeEnhancers)) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`enhancers` callback must return an array");
    }
    if (("TURBOPACK compile-time value", "development") !== "production" && storeEnhancers.some((item)=>typeof item !== "function")) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "each enhancer provided to configureStore must be a function");
    }
    if (("TURBOPACK compile-time value", "development") !== "production" && finalMiddleware.length && !storeEnhancers.includes(middlewareEnhancer)) {
        console.error("middlewares were provided, but middleware enhancer was not included in final enhancers - make sure to call `getDefaultEnhancers`");
    }
    const composedEnhancer = finalCompose(...storeEnhancers);
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createStore"])(rootReducer, preloadedState, composedEnhancer);
}
// src/mapBuilders.ts
function executeReducerBuilderCallback(builderCallback) {
    const actionsMap = {};
    const actionMatchers = [];
    let defaultCaseReducer;
    const builder = {
        addCase (typeOrActionCreator, reducer) {
            if ("TURBOPACK compile-time truthy", 1) {
                if (actionMatchers.length > 0) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addCase` should only be called before calling `builder.addMatcher`");
                }
                if (defaultCaseReducer) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addCase` should only be called before calling `builder.addDefaultCase`");
                }
            }
            const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
            if (!type) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addCase` cannot be called with an empty action type");
            }
            if (type in actionsMap) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `\`builder.addCase\` cannot be called with two reducers for the same action type '${type}'`);
            }
            actionsMap[type] = reducer;
            return builder;
        },
        addAsyncThunk (asyncThunk, reducers) {
            if ("TURBOPACK compile-time truthy", 1) {
                if (defaultCaseReducer) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addAsyncThunk` should only be called before calling `builder.addDefaultCase`");
                }
            }
            if (reducers.pending) actionsMap[asyncThunk.pending.type] = reducers.pending;
            if (reducers.rejected) actionsMap[asyncThunk.rejected.type] = reducers.rejected;
            if (reducers.fulfilled) actionsMap[asyncThunk.fulfilled.type] = reducers.fulfilled;
            if (reducers.settled) actionMatchers.push({
                matcher: asyncThunk.settled,
                reducer: reducers.settled
            });
            return builder;
        },
        addMatcher (matcher, reducer) {
            if ("TURBOPACK compile-time truthy", 1) {
                if (defaultCaseReducer) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addMatcher` should only be called before calling `builder.addDefaultCase`");
                }
            }
            actionMatchers.push({
                matcher,
                reducer
            });
            return builder;
        },
        addDefaultCase (reducer) {
            if ("TURBOPACK compile-time truthy", 1) {
                if (defaultCaseReducer) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`builder.addDefaultCase` can only be called once");
                }
            }
            defaultCaseReducer = reducer;
            return builder;
        }
    };
    builderCallback(builder);
    return [
        actionsMap,
        actionMatchers,
        defaultCaseReducer
    ];
}
// src/createReducer.ts
function isStateFunction(x) {
    return typeof x === "function";
}
function createReducer(initialState, mapOrBuilderCallback) {
    if ("TURBOPACK compile-time truthy", 1) {
        if (typeof mapOrBuilderCallback === "object") {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "The object notation for `createReducer` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createReducer");
        }
    }
    let [actionsMap, finalActionMatchers, finalDefaultCaseReducer] = executeReducerBuilderCallback(mapOrBuilderCallback);
    let getInitialState;
    if (isStateFunction(initialState)) {
        getInitialState = ()=>freezeDraftable(initialState());
    } else {
        const frozenInitialState = freezeDraftable(initialState);
        getInitialState = ()=>frozenInitialState;
    }
    function reducer(state = getInitialState(), action) {
        let caseReducers = [
            actionsMap[action.type],
            ...finalActionMatchers.filter(({ matcher })=>matcher(action)).map(({ reducer: reducer2 })=>reducer2)
        ];
        if (caseReducers.filter((cr)=>!!cr).length === 0) {
            caseReducers = [
                finalDefaultCaseReducer
            ];
        }
        return caseReducers.reduce((previousState, caseReducer)=>{
            if (caseReducer) {
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraft"])(previousState)) {
                    const draft = previousState;
                    const result = caseReducer(draft, action);
                    if (result === void 0) {
                        return previousState;
                    }
                    return result;
                } else if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraftable"])(previousState)) {
                    const result = caseReducer(previousState, action);
                    if (result === void 0) {
                        if (previousState === null) {
                            return previousState;
                        }
                        throw Error("A case reducer on a non-draftable value must not return undefined");
                    }
                    return result;
                } else {
                    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["produce"])(previousState, (draft)=>{
                        return caseReducer(draft, action);
                    });
                }
            }
            return previousState;
        }, state);
    }
    reducer.getInitialState = getInitialState;
    return reducer;
}
// src/matchers.ts
var matches = (matcher, action)=>{
    if (hasMatchFunction(matcher)) {
        return matcher.match(action);
    } else {
        return matcher(action);
    }
};
function isAnyOf(...matchers) {
    return (action)=>{
        return matchers.some((matcher)=>matches(matcher, action));
    };
}
function isAllOf(...matchers) {
    return (action)=>{
        return matchers.every((matcher)=>matches(matcher, action));
    };
}
function hasExpectedRequestMetadata(action, validStatus) {
    if (!action || !action.meta) return false;
    const hasValidRequestId = typeof action.meta.requestId === "string";
    const hasValidRequestStatus = validStatus.indexOf(action.meta.requestStatus) > -1;
    return hasValidRequestId && hasValidRequestStatus;
}
function isAsyncThunkArray(a) {
    return typeof a[0] === "function" && "pending" in a[0] && "fulfilled" in a[0] && "rejected" in a[0];
}
function isPending(...asyncThunks) {
    if (asyncThunks.length === 0) {
        return (action)=>hasExpectedRequestMetadata(action, [
                "pending"
            ]);
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isPending()(asyncThunks[0]);
    }
    return isAnyOf(...asyncThunks.map((asyncThunk)=>asyncThunk.pending));
}
function isRejected(...asyncThunks) {
    if (asyncThunks.length === 0) {
        return (action)=>hasExpectedRequestMetadata(action, [
                "rejected"
            ]);
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isRejected()(asyncThunks[0]);
    }
    return isAnyOf(...asyncThunks.map((asyncThunk)=>asyncThunk.rejected));
}
function isRejectedWithValue(...asyncThunks) {
    const hasFlag = (action)=>{
        return action && action.meta && action.meta.rejectedWithValue;
    };
    if (asyncThunks.length === 0) {
        return isAllOf(isRejected(...asyncThunks), hasFlag);
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isRejectedWithValue()(asyncThunks[0]);
    }
    return isAllOf(isRejected(...asyncThunks), hasFlag);
}
function isFulfilled(...asyncThunks) {
    if (asyncThunks.length === 0) {
        return (action)=>hasExpectedRequestMetadata(action, [
                "fulfilled"
            ]);
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isFulfilled()(asyncThunks[0]);
    }
    return isAnyOf(...asyncThunks.map((asyncThunk)=>asyncThunk.fulfilled));
}
function isAsyncThunkAction(...asyncThunks) {
    if (asyncThunks.length === 0) {
        return (action)=>hasExpectedRequestMetadata(action, [
                "pending",
                "fulfilled",
                "rejected"
            ]);
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isAsyncThunkAction()(asyncThunks[0]);
    }
    return isAnyOf(...asyncThunks.flatMap((asyncThunk)=>[
            asyncThunk.pending,
            asyncThunk.rejected,
            asyncThunk.fulfilled
        ]));
}
// src/nanoid.ts
var urlAlphabet = "ModuleSymbhasOwnPr-0123456789ABCDEFGHNRVfgctiUvz_KqYTJkLxpZXIjQW";
var nanoid = (size = 21)=>{
    let id = "";
    let i = size;
    while(i--){
        id += urlAlphabet[Math.random() * 64 | 0];
    }
    return id;
};
// src/createAsyncThunk.ts
var commonProperties = [
    "name",
    "message",
    "stack",
    "code"
];
var RejectWithValue = class {
    constructor(payload, meta){
        this.payload = payload;
        this.meta = meta;
    }
    /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */ _type;
};
var FulfillWithMeta = class {
    constructor(payload, meta){
        this.payload = payload;
        this.meta = meta;
    }
    /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */ _type;
};
var miniSerializeError = (value)=>{
    if (typeof value === "object" && value !== null) {
        const simpleError = {};
        for (const property of commonProperties){
            if (typeof value[property] === "string") {
                simpleError[property] = value[property];
            }
        }
        return simpleError;
    }
    return {
        message: String(value)
    };
};
var externalAbortMessage = "External signal was aborted";
var createAsyncThunk = /* @__PURE__ */ (()=>{
    function createAsyncThunk2(typePrefix, payloadCreator, options) {
        const fulfilled = createAction(typePrefix + "/fulfilled", (payload, requestId, arg, meta)=>({
                payload,
                meta: {
                    ...meta || {},
                    arg,
                    requestId,
                    requestStatus: "fulfilled"
                }
            }));
        const pending = createAction(typePrefix + "/pending", (requestId, arg, meta)=>({
                payload: void 0,
                meta: {
                    ...meta || {},
                    arg,
                    requestId,
                    requestStatus: "pending"
                }
            }));
        const rejected = createAction(typePrefix + "/rejected", (error, requestId, arg, payload, meta)=>({
                payload,
                error: (options && options.serializeError || miniSerializeError)(error || "Rejected"),
                meta: {
                    ...meta || {},
                    arg,
                    requestId,
                    rejectedWithValue: !!payload,
                    requestStatus: "rejected",
                    aborted: error?.name === "AbortError",
                    condition: error?.name === "ConditionError"
                }
            }));
        function actionCreator(arg, { signal } = {}) {
            return (dispatch, getState, extra)=>{
                const requestId = options?.idGenerator ? options.idGenerator(arg) : nanoid();
                const abortController = new AbortController();
                let abortHandler;
                let abortReason;
                function abort(reason) {
                    abortReason = reason;
                    abortController.abort();
                }
                if (signal) {
                    if (signal.aborted) {
                        abort(externalAbortMessage);
                    } else {
                        signal.addEventListener("abort", ()=>abort(externalAbortMessage), {
                            once: true
                        });
                    }
                }
                const promise = async function() {
                    let finalAction;
                    try {
                        let conditionResult = options?.condition?.(arg, {
                            getState,
                            extra
                        });
                        if (isThenable(conditionResult)) {
                            conditionResult = await conditionResult;
                        }
                        if (conditionResult === false || abortController.signal.aborted) {
                            throw {
                                name: "ConditionError",
                                message: "Aborted due to condition callback returning false."
                            };
                        }
                        const abortedPromise = new Promise((_, reject)=>{
                            abortHandler = ()=>{
                                reject({
                                    name: "AbortError",
                                    message: abortReason || "Aborted"
                                });
                            };
                            abortController.signal.addEventListener("abort", abortHandler, {
                                once: true
                            });
                        });
                        dispatch(pending(requestId, arg, options?.getPendingMeta?.({
                            requestId,
                            arg
                        }, {
                            getState,
                            extra
                        })));
                        finalAction = await Promise.race([
                            abortedPromise,
                            Promise.resolve(payloadCreator(arg, {
                                dispatch,
                                getState,
                                extra,
                                requestId,
                                signal: abortController.signal,
                                abort,
                                rejectWithValue: (value, meta)=>{
                                    return new RejectWithValue(value, meta);
                                },
                                fulfillWithValue: (value, meta)=>{
                                    return new FulfillWithMeta(value, meta);
                                }
                            })).then((result)=>{
                                if (result instanceof RejectWithValue) {
                                    throw result;
                                }
                                if (result instanceof FulfillWithMeta) {
                                    return fulfilled(result.payload, requestId, arg, result.meta);
                                }
                                return fulfilled(result, requestId, arg);
                            })
                        ]);
                    } catch (err) {
                        finalAction = err instanceof RejectWithValue ? rejected(null, requestId, arg, err.payload, err.meta) : rejected(err, requestId, arg);
                    } finally{
                        if (abortHandler) {
                            abortController.signal.removeEventListener("abort", abortHandler);
                        }
                    }
                    const skipDispatch = options && !options.dispatchConditionRejection && rejected.match(finalAction) && finalAction.meta.condition;
                    if (!skipDispatch) {
                        dispatch(finalAction);
                    }
                    return finalAction;
                }();
                return Object.assign(promise, {
                    abort,
                    requestId,
                    arg,
                    unwrap () {
                        return promise.then(unwrapResult);
                    }
                });
            };
        }
        return Object.assign(actionCreator, {
            pending,
            rejected,
            fulfilled,
            settled: isAnyOf(rejected, fulfilled),
            typePrefix
        });
    }
    createAsyncThunk2.withTypes = ()=>createAsyncThunk2;
    return createAsyncThunk2;
})();
function unwrapResult(action) {
    if (action.meta && action.meta.rejectedWithValue) {
        throw action.payload;
    }
    if (action.error) {
        throw action.error;
    }
    return action.payload;
}
function isThenable(value) {
    return value !== null && typeof value === "object" && typeof value.then === "function";
}
// src/createSlice.ts
var asyncThunkSymbol = /* @__PURE__ */ Symbol.for("rtk-slice-createasyncthunk");
var asyncThunkCreator = {
    [asyncThunkSymbol]: createAsyncThunk
};
var ReducerType = /* @__PURE__ */ ((ReducerType2)=>{
    ReducerType2["reducer"] = "reducer";
    ReducerType2["reducerWithPrepare"] = "reducerWithPrepare";
    ReducerType2["asyncThunk"] = "asyncThunk";
    return ReducerType2;
})(ReducerType || {});
function getType(slice, actionKey) {
    return `${slice}/${actionKey}`;
}
function buildCreateSlice({ creators } = {}) {
    const cAT = creators?.asyncThunk?.[asyncThunkSymbol];
    return function createSlice2(options) {
        const { name, reducerPath = name } = options;
        if (!name) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`name` is a required option for createSlice");
        }
        if (typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"] !== "undefined" && ("TURBOPACK compile-time value", "development") === "development") {
            if (options.initialState === void 0) {
                console.error("You must provide an `initialState` value that is not `undefined`. You may have misspelled `initialState`");
            }
        }
        const reducers = (typeof options.reducers === "function" ? options.reducers(buildReducerCreators()) : options.reducers) || {};
        const reducerNames = Object.keys(reducers);
        const context = {
            sliceCaseReducersByName: {},
            sliceCaseReducersByType: {},
            actionCreators: {},
            sliceMatchers: []
        };
        const contextMethods = {
            addCase (typeOrActionCreator, reducer2) {
                const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
                if (!type) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`context.addCase` cannot be called with an empty action type");
                }
                if (type in context.sliceCaseReducersByType) {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "`context.addCase` cannot be called with two reducers for the same action type: " + type);
                }
                context.sliceCaseReducersByType[type] = reducer2;
                return contextMethods;
            },
            addMatcher (matcher, reducer2) {
                context.sliceMatchers.push({
                    matcher,
                    reducer: reducer2
                });
                return contextMethods;
            },
            exposeAction (name2, actionCreator) {
                context.actionCreators[name2] = actionCreator;
                return contextMethods;
            },
            exposeCaseReducer (name2, reducer2) {
                context.sliceCaseReducersByName[name2] = reducer2;
                return contextMethods;
            }
        };
        reducerNames.forEach((reducerName)=>{
            const reducerDefinition = reducers[reducerName];
            const reducerDetails = {
                reducerName,
                type: getType(name, reducerName),
                createNotation: typeof options.reducers === "function"
            };
            if (isAsyncThunkSliceReducerDefinition(reducerDefinition)) {
                handleThunkCaseReducerDefinition(reducerDetails, reducerDefinition, contextMethods, cAT);
            } else {
                handleNormalReducerDefinition(reducerDetails, reducerDefinition, contextMethods);
            }
        });
        function buildReducer() {
            if ("TURBOPACK compile-time truthy", 1) {
                if (typeof options.extraReducers === "object") {
                    throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "The object notation for `createSlice.extraReducers` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createSlice");
                }
            }
            const [extraReducers = {}, actionMatchers = [], defaultCaseReducer = void 0] = typeof options.extraReducers === "function" ? executeReducerBuilderCallback(options.extraReducers) : [
                options.extraReducers
            ];
            const finalCaseReducers = {
                ...extraReducers,
                ...context.sliceCaseReducersByType
            };
            return createReducer(options.initialState, (builder)=>{
                for(let key in finalCaseReducers){
                    builder.addCase(key, finalCaseReducers[key]);
                }
                for (let sM of context.sliceMatchers){
                    builder.addMatcher(sM.matcher, sM.reducer);
                }
                for (let m of actionMatchers){
                    builder.addMatcher(m.matcher, m.reducer);
                }
                if (defaultCaseReducer) {
                    builder.addDefaultCase(defaultCaseReducer);
                }
            });
        }
        const selectSelf = (state)=>state;
        const injectedSelectorCache = /* @__PURE__ */ new Map();
        const injectedStateCache = /* @__PURE__ */ new WeakMap();
        let _reducer;
        function reducer(state, action) {
            if (!_reducer) _reducer = buildReducer();
            return _reducer(state, action);
        }
        function getInitialState() {
            if (!_reducer) _reducer = buildReducer();
            return _reducer.getInitialState();
        }
        function makeSelectorProps(reducerPath2, injected = false) {
            function selectSlice(state) {
                let sliceState = state[reducerPath2];
                if (typeof sliceState === "undefined") {
                    if (injected) {
                        sliceState = getOrInsertComputed(injectedStateCache, selectSlice, getInitialState);
                    } else if ("TURBOPACK compile-time truthy", 1) {
                        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "selectSlice returned undefined for an uninjected slice reducer");
                    }
                }
                return sliceState;
            }
            function getSelectors(selectState = selectSelf) {
                const selectorCache = getOrInsertComputed(injectedSelectorCache, injected, ()=>/* @__PURE__ */ new WeakMap());
                return getOrInsertComputed(selectorCache, selectState, ()=>{
                    const map = {};
                    for (const [name2, selector] of Object.entries(options.selectors ?? {})){
                        map[name2] = wrapSelector(selector, selectState, ()=>getOrInsertComputed(injectedStateCache, selectState, getInitialState), injected);
                    }
                    return map;
                });
            }
            return {
                reducerPath: reducerPath2,
                getSelectors,
                get selectors () {
                    return getSelectors(selectSlice);
                },
                selectSlice
            };
        }
        const slice = {
            name,
            reducer,
            actions: context.actionCreators,
            caseReducers: context.sliceCaseReducersByName,
            getInitialState,
            ...makeSelectorProps(reducerPath),
            injectInto (injectable, { reducerPath: pathOpt, ...config } = {}) {
                const newReducerPath = pathOpt ?? reducerPath;
                injectable.inject({
                    reducerPath: newReducerPath,
                    reducer
                }, config);
                return {
                    ...slice,
                    ...makeSelectorProps(newReducerPath, true)
                };
            }
        };
        return slice;
    };
}
function wrapSelector(selector, selectState, getInitialState, injected) {
    function wrapper(rootState, ...args) {
        let sliceState = selectState(rootState);
        if (typeof sliceState === "undefined") {
            if (injected) {
                sliceState = getInitialState();
            } else if ("TURBOPACK compile-time truthy", 1) {
                throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "selectState returned undefined for an uninjected slice reducer");
            }
        }
        return selector(sliceState, ...args);
    }
    wrapper.unwrapped = selector;
    return wrapper;
}
var createSlice = /* @__PURE__ */ buildCreateSlice();
function buildReducerCreators() {
    function asyncThunk(payloadCreator, config) {
        return {
            _reducerDefinitionType: "asyncThunk" /* asyncThunk */ ,
            payloadCreator,
            ...config
        };
    }
    asyncThunk.withTypes = ()=>asyncThunk;
    return {
        reducer (caseReducer) {
            return Object.assign({
                // hack so the wrapping function has the same name as the original
                // we need to create a wrapper so the `reducerDefinitionType` is not assigned to the original
                [caseReducer.name] (...args) {
                    return caseReducer(...args);
                }
            }[caseReducer.name], {
                _reducerDefinitionType: "reducer" /* reducer */ 
            });
        },
        preparedReducer (prepare, reducer) {
            return {
                _reducerDefinitionType: "reducerWithPrepare" /* reducerWithPrepare */ ,
                prepare,
                reducer
            };
        },
        asyncThunk
    };
}
function handleNormalReducerDefinition({ type, reducerName, createNotation }, maybeReducerWithPrepare, context) {
    let caseReducer;
    let prepareCallback;
    if ("reducer" in maybeReducerWithPrepare) {
        if (createNotation && !isCaseReducerWithPrepareDefinition(maybeReducerWithPrepare)) {
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Please use the `create.preparedReducer` notation for prepared action creators with the `create` notation.");
        }
        caseReducer = maybeReducerWithPrepare.reducer;
        prepareCallback = maybeReducerWithPrepare.prepare;
    } else {
        caseReducer = maybeReducerWithPrepare;
    }
    context.addCase(type, caseReducer).exposeCaseReducer(reducerName, caseReducer).exposeAction(reducerName, prepareCallback ? createAction(type, prepareCallback) : createAction(type));
}
function isAsyncThunkSliceReducerDefinition(reducerDefinition) {
    return reducerDefinition._reducerDefinitionType === "asyncThunk" /* asyncThunk */ ;
}
function isCaseReducerWithPrepareDefinition(reducerDefinition) {
    return reducerDefinition._reducerDefinitionType === "reducerWithPrepare" /* reducerWithPrepare */ ;
}
function handleThunkCaseReducerDefinition({ type, reducerName }, reducerDefinition, context, cAT) {
    if (!cAT) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Cannot use `create.asyncThunk` in the built-in `createSlice`. Use `buildCreateSlice({ creators: { asyncThunk: asyncThunkCreator } })` to create a customised version of `createSlice`.");
    }
    const { payloadCreator, fulfilled, pending, rejected, settled, options } = reducerDefinition;
    const thunk = cAT(type, payloadCreator, options);
    context.exposeAction(reducerName, thunk);
    if (fulfilled) {
        context.addCase(thunk.fulfilled, fulfilled);
    }
    if (pending) {
        context.addCase(thunk.pending, pending);
    }
    if (rejected) {
        context.addCase(thunk.rejected, rejected);
    }
    if (settled) {
        context.addMatcher(thunk.settled, settled);
    }
    context.exposeCaseReducer(reducerName, {
        fulfilled: fulfilled || noop,
        pending: pending || noop,
        rejected: rejected || noop,
        settled: settled || noop
    });
}
function noop() {}
// src/entities/entity_state.ts
function getInitialEntityState() {
    return {
        ids: [],
        entities: {}
    };
}
function createInitialStateFactory(stateAdapter) {
    function getInitialState(additionalState = {}, entities) {
        const state = Object.assign(getInitialEntityState(), additionalState);
        return entities ? stateAdapter.setAll(state, entities) : state;
    }
    return {
        getInitialState
    };
}
// src/entities/state_selectors.ts
function createSelectorsFactory() {
    function getSelectors(selectState, options = {}) {
        const { createSelector: createSelector2 = createDraftSafeSelector } = options;
        const selectIds = (state)=>state.ids;
        const selectEntities = (state)=>state.entities;
        const selectAll = createSelector2(selectIds, selectEntities, (ids, entities)=>ids.map((id)=>entities[id]));
        const selectId = (_, id)=>id;
        const selectById = (entities, id)=>entities[id];
        const selectTotal = createSelector2(selectIds, (ids)=>ids.length);
        if (!selectState) {
            return {
                selectIds,
                selectEntities,
                selectAll,
                selectTotal,
                selectById: createSelector2(selectEntities, selectId, selectById)
            };
        }
        const selectGlobalizedEntities = createSelector2(selectState, selectEntities);
        return {
            selectIds: createSelector2(selectState, selectIds),
            selectEntities: selectGlobalizedEntities,
            selectAll: createSelector2(selectState, selectAll),
            selectTotal: createSelector2(selectState, selectTotal),
            selectById: createSelector2(selectGlobalizedEntities, selectId, selectById)
        };
    }
    return {
        getSelectors
    };
}
// src/entities/state_adapter.ts
var isDraftTyped = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraft"];
function createSingleArgumentStateOperator(mutator) {
    const operator = createStateOperator((_, state)=>mutator(state));
    return function operation(state) {
        return operator(state, void 0);
    };
}
function createStateOperator(mutator) {
    return function operation(state, arg) {
        function isPayloadActionArgument(arg2) {
            return isFSA(arg2);
        }
        const runMutator = (draft)=>{
            if (isPayloadActionArgument(arg)) {
                mutator(arg.payload, draft);
            } else {
                mutator(arg, draft);
            }
        };
        if (isDraftTyped(state)) {
            runMutator(state);
            return state;
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["produce"])(state, runMutator);
    };
}
// src/entities/utils.ts
function selectIdValue(entity, selectId) {
    const key = selectId(entity);
    if (("TURBOPACK compile-time value", "development") !== "production" && key === void 0) {
        console.warn("The entity passed to the `selectId` implementation returned undefined.", "You should probably provide your own `selectId` implementation.", "The entity that was passed:", entity, "The `selectId` implementation:", selectId.toString());
    }
    return key;
}
function ensureEntitiesArray(entities) {
    if (!Array.isArray(entities)) {
        entities = Object.values(entities);
    }
    return entities;
}
function getCurrent(value) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isDraft"])(value) ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$node_modules$2f$immer$2f$dist$2f$immer$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["current"])(value) : value;
}
function splitAddedUpdatedEntities(newEntities, selectId, state) {
    newEntities = ensureEntitiesArray(newEntities);
    const existingIdsArray = getCurrent(state.ids);
    const existingIds = new Set(existingIdsArray);
    const added = [];
    const addedIds = /* @__PURE__ */ new Set([]);
    const updated = [];
    for (const entity of newEntities){
        const id = selectIdValue(entity, selectId);
        if (existingIds.has(id) || addedIds.has(id)) {
            updated.push({
                id,
                changes: entity
            });
        } else {
            addedIds.add(id);
            added.push(entity);
        }
    }
    return [
        added,
        updated,
        existingIdsArray
    ];
}
// src/entities/unsorted_state_adapter.ts
function createUnsortedStateAdapter(selectId) {
    function addOneMutably(entity, state) {
        const key = selectIdValue(entity, selectId);
        if (key in state.entities) {
            return;
        }
        state.ids.push(key);
        state.entities[key] = entity;
    }
    function addManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        for (const entity of newEntities){
            addOneMutably(entity, state);
        }
    }
    function setOneMutably(entity, state) {
        const key = selectIdValue(entity, selectId);
        if (!(key in state.entities)) {
            state.ids.push(key);
        }
        ;
        state.entities[key] = entity;
    }
    function setManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        for (const entity of newEntities){
            setOneMutably(entity, state);
        }
    }
    function setAllMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        state.ids = [];
        state.entities = {};
        addManyMutably(newEntities, state);
    }
    function removeOneMutably(key, state) {
        return removeManyMutably([
            key
        ], state);
    }
    function removeManyMutably(keys, state) {
        let didMutate = false;
        keys.forEach((key)=>{
            if (key in state.entities) {
                delete state.entities[key];
                didMutate = true;
            }
        });
        if (didMutate) {
            state.ids = state.ids.filter((id)=>id in state.entities);
        }
    }
    function removeAllMutably(state) {
        Object.assign(state, {
            ids: [],
            entities: {}
        });
    }
    function takeNewKey(keys, update, state) {
        const original3 = state.entities[update.id];
        if (original3 === void 0) {
            return false;
        }
        const updated = Object.assign({}, original3, update.changes);
        const newKey = selectIdValue(updated, selectId);
        const hasNewKey = newKey !== update.id;
        if (hasNewKey) {
            keys[update.id] = newKey;
            delete state.entities[update.id];
        }
        ;
        state.entities[newKey] = updated;
        return hasNewKey;
    }
    function updateOneMutably(update, state) {
        return updateManyMutably([
            update
        ], state);
    }
    function updateManyMutably(updates, state) {
        const newKeys = {};
        const updatesPerEntity = {};
        updates.forEach((update)=>{
            if (update.id in state.entities) {
                updatesPerEntity[update.id] = {
                    id: update.id,
                    // Spreads ignore falsy values, so this works even if there isn't
                    // an existing update already at this key
                    changes: {
                        ...updatesPerEntity[update.id]?.changes,
                        ...update.changes
                    }
                };
            }
        });
        updates = Object.values(updatesPerEntity);
        const didMutateEntities = updates.length > 0;
        if (didMutateEntities) {
            const didMutateIds = updates.filter((update)=>takeNewKey(newKeys, update, state)).length > 0;
            if (didMutateIds) {
                state.ids = Object.values(state.entities).map((e)=>selectIdValue(e, selectId));
            }
        }
    }
    function upsertOneMutably(entity, state) {
        return upsertManyMutably([
            entity
        ], state);
    }
    function upsertManyMutably(newEntities, state) {
        const [added, updated] = splitAddedUpdatedEntities(newEntities, selectId, state);
        addManyMutably(added, state);
        updateManyMutably(updated, state);
    }
    return {
        removeAll: createSingleArgumentStateOperator(removeAllMutably),
        addOne: createStateOperator(addOneMutably),
        addMany: createStateOperator(addManyMutably),
        setOne: createStateOperator(setOneMutably),
        setMany: createStateOperator(setManyMutably),
        setAll: createStateOperator(setAllMutably),
        updateOne: createStateOperator(updateOneMutably),
        updateMany: createStateOperator(updateManyMutably),
        upsertOne: createStateOperator(upsertOneMutably),
        upsertMany: createStateOperator(upsertManyMutably),
        removeOne: createStateOperator(removeOneMutably),
        removeMany: createStateOperator(removeManyMutably)
    };
}
// src/entities/sorted_state_adapter.ts
function findInsertIndex(sortedItems, item, comparisonFunction) {
    let lowIndex = 0;
    let highIndex = sortedItems.length;
    while(lowIndex < highIndex){
        let middleIndex = lowIndex + highIndex >>> 1;
        const currentItem = sortedItems[middleIndex];
        const res = comparisonFunction(item, currentItem);
        if (res >= 0) {
            lowIndex = middleIndex + 1;
        } else {
            highIndex = middleIndex;
        }
    }
    return lowIndex;
}
function insert(sortedItems, item, comparisonFunction) {
    const insertAtIndex = findInsertIndex(sortedItems, item, comparisonFunction);
    sortedItems.splice(insertAtIndex, 0, item);
    return sortedItems;
}
function createSortedStateAdapter(selectId, comparer) {
    const { removeOne, removeMany, removeAll } = createUnsortedStateAdapter(selectId);
    function addOneMutably(entity, state) {
        return addManyMutably([
            entity
        ], state);
    }
    function addManyMutably(newEntities, state, existingIds) {
        newEntities = ensureEntitiesArray(newEntities);
        const existingKeys = new Set(existingIds ?? getCurrent(state.ids));
        const addedKeys = /* @__PURE__ */ new Set();
        const models = newEntities.filter((model)=>{
            const modelId = selectIdValue(model, selectId);
            const notAdded = !addedKeys.has(modelId);
            if (notAdded) addedKeys.add(modelId);
            return !existingKeys.has(modelId) && notAdded;
        });
        if (models.length !== 0) {
            mergeFunction(state, models);
        }
    }
    function setOneMutably(entity, state) {
        return setManyMutably([
            entity
        ], state);
    }
    function setManyMutably(newEntities, state) {
        let deduplicatedEntities = {};
        newEntities = ensureEntitiesArray(newEntities);
        if (newEntities.length !== 0) {
            for (const item of newEntities){
                const entityId = selectId(item);
                deduplicatedEntities[entityId] = item;
                delete state.entities[entityId];
            }
            newEntities = ensureEntitiesArray(deduplicatedEntities);
            mergeFunction(state, newEntities);
        }
    }
    function setAllMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        state.entities = {};
        state.ids = [];
        addManyMutably(newEntities, state, []);
    }
    function updateOneMutably(update, state) {
        return updateManyMutably([
            update
        ], state);
    }
    function updateManyMutably(updates, state) {
        let appliedUpdates = false;
        let replacedIds = false;
        for (let update of updates){
            const entity = state.entities[update.id];
            if (!entity) {
                continue;
            }
            appliedUpdates = true;
            Object.assign(entity, update.changes);
            const newId = selectId(entity);
            if (update.id !== newId) {
                replacedIds = true;
                delete state.entities[update.id];
                const oldIndex = state.ids.indexOf(update.id);
                state.ids[oldIndex] = newId;
                state.entities[newId] = entity;
            }
        }
        if (appliedUpdates) {
            mergeFunction(state, [], appliedUpdates, replacedIds);
        }
    }
    function upsertOneMutably(entity, state) {
        return upsertManyMutably([
            entity
        ], state);
    }
    function upsertManyMutably(newEntities, state) {
        const [added, updated, existingIdsArray] = splitAddedUpdatedEntities(newEntities, selectId, state);
        if (added.length) {
            addManyMutably(added, state, existingIdsArray);
        }
        if (updated.length) {
            updateManyMutably(updated, state);
        }
    }
    function areArraysEqual(a, b) {
        if (a.length !== b.length) {
            return false;
        }
        for(let i = 0; i < a.length; i++){
            if (a[i] === b[i]) {
                continue;
            }
            return false;
        }
        return true;
    }
    const mergeFunction = (state, addedItems, appliedUpdates, replacedIds)=>{
        const currentEntities = getCurrent(state.entities);
        const currentIds = getCurrent(state.ids);
        const stateEntities = state.entities;
        let ids = currentIds;
        if (replacedIds) {
            ids = new Set(currentIds);
        }
        let sortedEntities = [];
        for (const id of ids){
            const entity = currentEntities[id];
            if (entity) {
                sortedEntities.push(entity);
            }
        }
        const wasPreviouslyEmpty = sortedEntities.length === 0;
        for (const item of addedItems){
            stateEntities[selectId(item)] = item;
            if (!wasPreviouslyEmpty) {
                insert(sortedEntities, item, comparer);
            }
        }
        if (wasPreviouslyEmpty) {
            sortedEntities = addedItems.slice().sort(comparer);
        } else if (appliedUpdates) {
            sortedEntities.sort(comparer);
        }
        const newSortedIds = sortedEntities.map(selectId);
        if (!areArraysEqual(currentIds, newSortedIds)) {
            state.ids = newSortedIds;
        }
    };
    return {
        removeOne,
        removeMany,
        removeAll,
        addOne: createStateOperator(addOneMutably),
        updateOne: createStateOperator(updateOneMutably),
        upsertOne: createStateOperator(upsertOneMutably),
        setOne: createStateOperator(setOneMutably),
        setMany: createStateOperator(setManyMutably),
        setAll: createStateOperator(setAllMutably),
        addMany: createStateOperator(addManyMutably),
        updateMany: createStateOperator(updateManyMutably),
        upsertMany: createStateOperator(upsertManyMutably)
    };
}
// src/entities/create_adapter.ts
function createEntityAdapter(options = {}) {
    const { selectId, sortComparer } = {
        sortComparer: false,
        selectId: (instance)=>instance.id,
        ...options
    };
    const stateAdapter = sortComparer ? createSortedStateAdapter(selectId, sortComparer) : createUnsortedStateAdapter(selectId);
    const stateFactory = createInitialStateFactory(stateAdapter);
    const selectorsFactory = createSelectorsFactory();
    return {
        selectId,
        sortComparer,
        ...stateFactory,
        ...selectorsFactory,
        ...stateAdapter
    };
}
// src/listenerMiddleware/exceptions.ts
var task = "task";
var listener = "listener";
var completed = "completed";
var cancelled = "cancelled";
var taskCancelled = `task-${cancelled}`;
var taskCompleted = `task-${completed}`;
var listenerCancelled = `${listener}-${cancelled}`;
var listenerCompleted = `${listener}-${completed}`;
var TaskAbortError = class {
    constructor(code){
        this.code = code;
        this.message = `${task} ${cancelled} (reason: ${code})`;
    }
    name = "TaskAbortError";
    message;
};
// src/listenerMiddleware/utils.ts
var assertFunction = (func, expected)=>{
    if (typeof func !== "function") {
        throw new TypeError(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `${expected} is not a function`);
    }
};
var noop2 = ()=>{};
var catchRejection = (promise, onError = noop2)=>{
    promise.catch(onError);
    return promise;
};
var addAbortSignalListener = (abortSignal, callback)=>{
    abortSignal.addEventListener("abort", callback, {
        once: true
    });
    return ()=>abortSignal.removeEventListener("abort", callback);
};
// src/listenerMiddleware/task.ts
var validateActive = (signal)=>{
    if (signal.aborted) {
        throw new TaskAbortError(signal.reason);
    }
};
function raceWithSignal(signal, promise) {
    let cleanup = noop2;
    return new Promise((resolve, reject)=>{
        const notifyRejection = ()=>reject(new TaskAbortError(signal.reason));
        if (signal.aborted) {
            notifyRejection();
            return;
        }
        cleanup = addAbortSignalListener(signal, notifyRejection);
        promise.finally(()=>cleanup()).then(resolve, reject);
    }).finally(()=>{
        cleanup = noop2;
    });
}
var runTask = async (task2, cleanUp)=>{
    try {
        await Promise.resolve();
        const value = await task2();
        return {
            status: "ok",
            value
        };
    } catch (error) {
        return {
            status: error instanceof TaskAbortError ? "cancelled" : "rejected",
            error
        };
    } finally{
        cleanUp?.();
    }
};
var createPause = (signal)=>{
    return (promise)=>{
        return catchRejection(raceWithSignal(signal, promise).then((output)=>{
            validateActive(signal);
            return output;
        }));
    };
};
var createDelay = (signal)=>{
    const pause = createPause(signal);
    return (timeoutMs)=>{
        return pause(new Promise((resolve)=>setTimeout(resolve, timeoutMs)));
    };
};
// src/listenerMiddleware/index.ts
var { assign } = Object;
var INTERNAL_NIL_TOKEN = {};
var alm = "listenerMiddleware";
var createFork = (parentAbortSignal, parentBlockingPromises)=>{
    const linkControllers = (controller)=>addAbortSignalListener(parentAbortSignal, ()=>controller.abort(parentAbortSignal.reason));
    return (taskExecutor, opts)=>{
        assertFunction(taskExecutor, "taskExecutor");
        const childAbortController = new AbortController();
        linkControllers(childAbortController);
        const result = runTask(async ()=>{
            validateActive(parentAbortSignal);
            validateActive(childAbortController.signal);
            const result2 = await taskExecutor({
                pause: createPause(childAbortController.signal),
                delay: createDelay(childAbortController.signal),
                signal: childAbortController.signal
            });
            validateActive(childAbortController.signal);
            return result2;
        }, ()=>childAbortController.abort(taskCompleted));
        if (opts?.autoJoin) {
            parentBlockingPromises.push(result.catch(noop2));
        }
        return {
            result: createPause(parentAbortSignal)(result),
            cancel () {
                childAbortController.abort(taskCancelled);
            }
        };
    };
};
var createTakePattern = (startListening, signal)=>{
    const take = async (predicate, timeout)=>{
        validateActive(signal);
        let unsubscribe = ()=>{};
        const tuplePromise = new Promise((resolve, reject)=>{
            let stopListening = startListening({
                predicate,
                effect: (action, listenerApi)=>{
                    listenerApi.unsubscribe();
                    resolve([
                        action,
                        listenerApi.getState(),
                        listenerApi.getOriginalState()
                    ]);
                }
            });
            unsubscribe = ()=>{
                stopListening();
                reject();
            };
        });
        const promises = [
            tuplePromise
        ];
        if (timeout != null) {
            promises.push(new Promise((resolve)=>setTimeout(resolve, timeout, null)));
        }
        try {
            const output = await raceWithSignal(signal, Promise.race(promises));
            validateActive(signal);
            return output;
        } finally{
            unsubscribe();
        }
    };
    return (predicate, timeout)=>catchRejection(take(predicate, timeout));
};
var getListenerEntryPropsFrom = (options)=>{
    let { type, actionCreator, matcher, predicate, effect } = options;
    if (type) {
        predicate = createAction(type).match;
    } else if (actionCreator) {
        type = actionCreator.type;
        predicate = actionCreator.match;
    } else if (matcher) {
        predicate = matcher;
    } else if (predicate) {} else {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Creating or removing a listener requires one of the known fields for matching an action");
    }
    assertFunction(effect, "options.listener");
    return {
        predicate,
        type,
        effect
    };
};
var createListenerEntry = /* @__PURE__ */ assign((options)=>{
    const { type, predicate, effect } = getListenerEntryPropsFrom(options);
    const entry = {
        id: nanoid(),
        effect,
        type,
        predicate,
        pending: /* @__PURE__ */ new Set(),
        unsubscribe: ()=>{
            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "Unsubscribe not initialized");
        }
    };
    return entry;
}, {
    withTypes: ()=>createListenerEntry
});
var findListenerEntry = (listenerMap, options)=>{
    const { type, effect, predicate } = getListenerEntryPropsFrom(options);
    return Array.from(listenerMap.values()).find((entry)=>{
        const matchPredicateOrType = typeof type === "string" ? entry.type === type : entry.predicate === predicate;
        return matchPredicateOrType && entry.effect === effect;
    });
};
var cancelActiveListeners = (entry)=>{
    entry.pending.forEach((controller)=>{
        controller.abort(listenerCancelled);
    });
};
var createClearListenerMiddleware = (listenerMap, executingListeners)=>{
    return ()=>{
        for (const listener2 of executingListeners.keys()){
            cancelActiveListeners(listener2);
        }
        listenerMap.clear();
    };
};
var safelyNotifyError = (errorHandler, errorToNotify, errorInfo)=>{
    try {
        errorHandler(errorToNotify, errorInfo);
    } catch (errorHandlerError) {
        setTimeout(()=>{
            throw errorHandlerError;
        }, 0);
    }
};
var addListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/add`), {
    withTypes: ()=>addListener
});
var clearAllListeners = /* @__PURE__ */ createAction(`${alm}/removeAll`);
var removeListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/remove`), {
    withTypes: ()=>removeListener
});
var defaultErrorHandler = (...args)=>{
    console.error(`${alm}/error`, ...args);
};
var createListenerMiddleware = (middlewareOptions = {})=>{
    const listenerMap = /* @__PURE__ */ new Map();
    const executingListeners = /* @__PURE__ */ new Map();
    const trackExecutingListener = (entry)=>{
        const count = executingListeners.get(entry) ?? 0;
        executingListeners.set(entry, count + 1);
    };
    const untrackExecutingListener = (entry)=>{
        const count = executingListeners.get(entry) ?? 1;
        if (count === 1) {
            executingListeners.delete(entry);
        } else {
            executingListeners.set(entry, count - 1);
        }
    };
    const { extra, onError = defaultErrorHandler } = middlewareOptions;
    assertFunction(onError, "onError");
    const insertEntry = (entry)=>{
        entry.unsubscribe = ()=>listenerMap.delete(entry.id);
        listenerMap.set(entry.id, entry);
        return (cancelOptions)=>{
            entry.unsubscribe();
            if (cancelOptions?.cancelActive) {
                cancelActiveListeners(entry);
            }
        };
    };
    const startListening = (options)=>{
        const entry = findListenerEntry(listenerMap, options) ?? createListenerEntry(options);
        return insertEntry(entry);
    };
    assign(startListening, {
        withTypes: ()=>startListening
    });
    const stopListening = (options)=>{
        const entry = findListenerEntry(listenerMap, options);
        if (entry) {
            entry.unsubscribe();
            if (options.cancelActive) {
                cancelActiveListeners(entry);
            }
        }
        return !!entry;
    };
    assign(stopListening, {
        withTypes: ()=>stopListening
    });
    const notifyListener = async (entry, action, api, getOriginalState)=>{
        const internalTaskController = new AbortController();
        const take = createTakePattern(startListening, internalTaskController.signal);
        const autoJoinPromises = [];
        try {
            entry.pending.add(internalTaskController);
            trackExecutingListener(entry);
            await Promise.resolve(entry.effect(action, // Use assign() rather than ... to avoid extra helper functions added to bundle
            assign({}, api, {
                getOriginalState,
                condition: (predicate, timeout)=>take(predicate, timeout).then(Boolean),
                take,
                delay: createDelay(internalTaskController.signal),
                pause: createPause(internalTaskController.signal),
                extra,
                signal: internalTaskController.signal,
                fork: createFork(internalTaskController.signal, autoJoinPromises),
                unsubscribe: entry.unsubscribe,
                subscribe: ()=>{
                    listenerMap.set(entry.id, entry);
                },
                cancelActiveListeners: ()=>{
                    entry.pending.forEach((controller, _, set)=>{
                        if (controller !== internalTaskController) {
                            controller.abort(listenerCancelled);
                            set.delete(controller);
                        }
                    });
                },
                cancel: ()=>{
                    internalTaskController.abort(listenerCancelled);
                    entry.pending.delete(internalTaskController);
                },
                throwIfCancelled: ()=>{
                    validateActive(internalTaskController.signal);
                }
            })));
        } catch (listenerError) {
            if (!(listenerError instanceof TaskAbortError)) {
                safelyNotifyError(onError, listenerError, {
                    raisedBy: "effect"
                });
            }
        } finally{
            await Promise.all(autoJoinPromises);
            internalTaskController.abort(listenerCompleted);
            untrackExecutingListener(entry);
            entry.pending.delete(internalTaskController);
        }
    };
    const clearListenerMiddleware = createClearListenerMiddleware(listenerMap, executingListeners);
    const middleware = (api)=>(next)=>(action)=>{
                if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAction"])(action)) {
                    return next(action);
                }
                if (addListener.match(action)) {
                    return startListening(action.payload);
                }
                if (clearAllListeners.match(action)) {
                    clearListenerMiddleware();
                    return;
                }
                if (removeListener.match(action)) {
                    return stopListening(action.payload);
                }
                let originalState = api.getState();
                const getOriginalState = ()=>{
                    if (originalState === INTERNAL_NIL_TOKEN) {
                        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `${alm}: getOriginalState can only be called synchronously`);
                    }
                    return originalState;
                };
                let result;
                try {
                    result = next(action);
                    if (listenerMap.size > 0) {
                        const currentState = api.getState();
                        const listenerEntries = Array.from(listenerMap.values());
                        for (const entry of listenerEntries){
                            let runListener = false;
                            try {
                                runListener = entry.predicate(action, currentState, originalState);
                            } catch (predicateError) {
                                runListener = false;
                                safelyNotifyError(onError, predicateError, {
                                    raisedBy: "predicate"
                                });
                            }
                            if (!runListener) {
                                continue;
                            }
                            notifyListener(entry, action, api, getOriginalState);
                        }
                    }
                } finally{
                    originalState = INTERNAL_NIL_TOKEN;
                }
                return result;
            };
    return {
        middleware,
        startListening,
        stopListening,
        clearListeners: clearListenerMiddleware
    };
};
// src/dynamicMiddleware/index.ts
var createMiddlewareEntry = (middleware)=>({
        middleware,
        applied: /* @__PURE__ */ new Map()
    });
var matchInstance = (instanceId)=>(action)=>action?.meta?.instanceId === instanceId;
var createDynamicMiddleware = ()=>{
    const instanceId = nanoid();
    const middlewareMap = /* @__PURE__ */ new Map();
    const withMiddleware = Object.assign(createAction("dynamicMiddleware/add", (...middlewares)=>({
            payload: middlewares,
            meta: {
                instanceId
            }
        })), {
        withTypes: ()=>withMiddleware
    });
    const addMiddleware = Object.assign(function addMiddleware2(...middlewares) {
        middlewares.forEach((middleware2)=>{
            getOrInsertComputed(middlewareMap, middleware2, createMiddlewareEntry);
        });
    }, {
        withTypes: ()=>addMiddleware
    });
    const getFinalMiddleware = (api)=>{
        const appliedMiddleware = Array.from(middlewareMap.values()).map((entry)=>getOrInsertComputed(entry.applied, api, entry.middleware));
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(...appliedMiddleware);
    };
    const isWithMiddleware = isAllOf(withMiddleware, matchInstance(instanceId));
    const middleware = (api)=>(next)=>(action)=>{
                if (isWithMiddleware(action)) {
                    addMiddleware(...action.payload);
                    return api.dispatch;
                }
                return getFinalMiddleware(api)(next)(action);
            };
    return {
        middleware,
        addMiddleware,
        withMiddleware,
        instanceId
    };
};
;
var isSliceLike = (maybeSliceLike)=>"reducerPath" in maybeSliceLike && typeof maybeSliceLike.reducerPath === "string";
var getReducers = (slices)=>slices.flatMap((sliceOrMap)=>isSliceLike(sliceOrMap) ? [
            [
                sliceOrMap.reducerPath,
                sliceOrMap.reducer
            ]
        ] : Object.entries(sliceOrMap));
var ORIGINAL_STATE = Symbol.for("rtk-state-proxy-original");
var isStateProxy = (value)=>!!value && !!value[ORIGINAL_STATE];
var stateProxyMap = /* @__PURE__ */ new WeakMap();
var createStateProxy = (state, reducerMap, initialStateCache)=>getOrInsertComputed(stateProxyMap, state, ()=>new Proxy(state, {
            get: (target, prop, receiver)=>{
                if (prop === ORIGINAL_STATE) return target;
                const result = Reflect.get(target, prop, receiver);
                if (typeof result === "undefined") {
                    const cached = initialStateCache[prop];
                    if (typeof cached !== "undefined") return cached;
                    const reducer = reducerMap[prop];
                    if (reducer) {
                        const reducerResult = reducer(void 0, {
                            type: nanoid()
                        });
                        if (typeof reducerResult === "undefined") {
                            throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : `The slice reducer for key "${prop.toString()}" returned undefined when called for selector(). If the state passed to the reducer is undefined, you must explicitly return the initial state. The initial state may not be undefined. If you don't want to set a value for this reducer, you can use null instead of undefined.`);
                        }
                        initialStateCache[prop] = reducerResult;
                        return reducerResult;
                    }
                }
                return result;
            }
        }));
var original = (state)=>{
    if (!isStateProxy(state)) {
        throw new Error(("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : "original must be used on state Proxy");
    }
    return state[ORIGINAL_STATE];
};
var emptyObject = {};
var noopReducer = (state = emptyObject)=>state;
function combineSlices(...slices) {
    const reducerMap = Object.fromEntries(getReducers(slices));
    const getReducer = ()=>Object.keys(reducerMap).length ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redux$2f$dist$2f$redux$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["combineReducers"])(reducerMap) : noopReducer;
    let reducer = getReducer();
    function combinedReducer(state, action) {
        return reducer(state, action);
    }
    combinedReducer.withLazyLoadedSlices = ()=>combinedReducer;
    const initialStateCache = {};
    const inject = (slice, config = {})=>{
        const { reducerPath, reducer: reducerToInject } = slice;
        const currentReducer = reducerMap[reducerPath];
        if (!config.overrideExisting && currentReducer && currentReducer !== reducerToInject) {
            if (typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"] !== "undefined" && ("TURBOPACK compile-time value", "development") === "development") {
                console.error(`called \`inject\` to override already-existing reducer ${reducerPath} without specifying \`overrideExisting: true\``);
            }
            return combinedReducer;
        }
        if (config.overrideExisting && currentReducer !== reducerToInject) {
            delete initialStateCache[reducerPath];
        }
        reducerMap[reducerPath] = reducerToInject;
        reducer = getReducer();
        return combinedReducer;
    };
    const selector = Object.assign(function makeSelector(selectorFn, selectState) {
        return function selector2(state, ...args) {
            return selectorFn(createStateProxy(selectState ? selectState(state, ...args) : state, reducerMap, initialStateCache), ...args);
        };
    }, {
        original
    });
    return Object.assign(combinedReducer, {
        inject,
        selector
    });
}
// src/formatProdErrorMessage.ts
function formatProdErrorMessage(code) {
    return `Minified Redux Toolkit error #${code}; visit https://redux-toolkit.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}
;
 //# sourceMappingURL=redux-toolkit.modern.mjs.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createStateManagementMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createStateManagementMixin",
    ()=>createStateManagementMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
;
;
;
/* eslint-disable no-param-reassign */ const createStateManagementMixin = (options)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
        const slice = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["createSlice"])(options);
        const allActions = Object.assign(Object.assign({}, slice.actions), options.asyncActions);
        return class StateManagementMixinClass extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"])(superclass) {
            constructor(...args){
                super(...args);
                const store = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["configureStore"])({
                    reducer: slice.reducer,
                    middleware: (getDefaultMiddleware)=>getDefaultMiddleware({
                            thunk: {
                                extraArgument: this
                            },
                            serializableCheck: false
                        }),
                    // change to true if we want to debug redux
                    devTools: false
                });
                const wrapAction = (action)=>async (...arg)=>{
                        const result = await store.dispatch(action(...arg));
                        // we want to unwrap the result, so in case of an error we can log it
                        try {
                            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["unwrapResult"])(result);
                        } catch (e) {
                            this.logger.error(e.message, result.type, e.stack);
                        }
                        return result;
                    };
                const actions = Object.keys(allActions).reduce((acc, actionName)=>{
                    acc[actionName] = wrapAction(allActions[actionName]);
                    return acc;
                }, {});
                this.actions = actions;
                this.subscribe = (cb, selector = (state)=>state)=>store.subscribe(()=>cb(selector(store.getState())));
            }
        };
    });
;
 //# sourceMappingURL=createStateManagementMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/formMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "formMixin",
    ()=>formMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
;
;
const formMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>class FormMixinClass extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"])(superclass) {
        validateForm(rootEle) {
            return this.getFormInputs(rootEle).every((input)=>{
                var _a, _b;
                (_a = input.reportValidity) === null || _a === void 0 ? void 0 : _a.call(input);
                return (_b = input.checkValidity) === null || _b === void 0 ? void 0 : _b.call(input);
            });
        }
        // eslint-disable-next-line class-methods-use-this
        getFormInputs(rootEle) {
            if (!rootEle) {
                this.logger.debug('cannot get form inputs, no root element was received');
                return [];
            }
            return Array.from(rootEle.querySelectorAll('[name]'));
        }
        getFormData(rootEle) {
            return this.getFormInputs(rootEle).reduce((acc, input)=>Object.assign(acc, {
                    [input.getAttribute('name')]: input.value
                }), {});
        }
        setFormData(rootEle, data) {
            this.getFormInputs(rootEle).forEach((input)=>{
                // eslint-disable-next-line no-prototype-builtins
                if (data.hasOwnProperty(input.getAttribute('name'))) {
                    // eslint-disable-next-line no-param-reassign
                    input.value = data[input.getAttribute('name')];
                }
            });
        }
        resetFormData(rootEle) {
            this.getFormInputs(rootEle).forEach((input)=>{
                // eslint-disable-next-line no-param-reassign
                input.value = '';
                input.checked = false;
            });
        }
        getFormFieldNames(rootEle) {
            return this.getFormInputs(rootEle).map((ele)=>ele.name);
        }
        disableFormField(rootEle, name) {
            var _a;
            (_a = this.getFormInputs(rootEle).find((input)=>input.name === name)) === null || _a === void 0 ? void 0 : _a.setAttribute('disabled', 'true');
        }
        removeFormField(rootEle, name) {
            var _a;
            (_a = this.getFormInputs(rootEle).find((input)=>input.name === name)) === null || _a === void 0 ? void 0 : _a.remove();
        }
    });
;
 //# sourceMappingURL=formMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cookieConfigMixin.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cookieConfigMixin",
    ()=>cookieConfigMixin
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
;
const cookieConfigMixin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((superclass)=>{
    return class CookieConfigMixinClass extends superclass {
        get refreshCookieName() {
            return this.getAttribute('refresh-cookie-name') || '';
        }
    };
});
;
 //# sourceMappingURL=cookieConfigMixin.js.map
}),
"[project]/node_modules/@descope/sdk-mixins/dist/esm/index.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$configMixin$2f$configMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/configMixin/configMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$debuggerMixin$2f$debuggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/debuggerMixin/debuggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$modalMixin$2f$modalMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/modalMixin/modalMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$notificationsMixin$2f$notificationsMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/notificationsMixin/notificationsMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/staticResourcesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$themeMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/themeMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createStateManagementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createStateManagementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$formMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/formMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$projectIdMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/projectIdMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$baseUrlMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/baseUrlMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cookieConfigMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cookieConfigMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectNpmLibMixin$2f$injectNpmLibMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectNpmLibMixin/injectNpmLibMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$injectStyleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/injectStyleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cspNonceMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cspNonceMixin.js [app-client] (ecmascript)"); //# sourceMappingURL=index.js.map
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
}),
"[project]/node_modules/@descope/audit-management-widget/dist/esm/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>z
]);
/**
       * @descope/audit-management-widget v0.5.34
       */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-helpers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$projectIdMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/projectIdMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/observeAttributesMixin/observeAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/loggerMixin/loggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$baseUrlMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/baseUrlMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cookieConfigMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/cookieConfigMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createValidateAttributesMixin/createValidateAttributesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createStateManagementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/createStateManagementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initLifecycleMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/staticResourcesMixin/staticResourcesMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/descopeUiMixin/descopeUiMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/initElementMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$debuggerMixin$2f$debuggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/debuggerMixin/debuggerMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$themeMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-mixins/dist/esm/mixins/themeMixin/themeMixin.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/sdk-component-drivers/dist/index.esm.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/reselect/dist/reselect.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$web$2d$js$2d$sdk$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@descope/web-js-sdk/dist/index.esm.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
const L = (e, t)=>t ? {
        [e]: t
    } : {}, O = (e)=>{
    if (!e) return;
    const t = JSON.parse(e);
    return Array.isArray(t) && 0 === t.length ? void 0 : t;
}, A = (e)=>e.selectedAuditId, D = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSelector"])((e)=>e.auditList.data, (e)=>e.map((e)=>{
        const t = e || {}, { occurred: s, ID: i, type: o } = t, a = t.data, n = void 0 === a ? {
            saml_request: "",
            saml_response: "",
            saml_generated_user: "",
            saml_generated_roles: "",
            oidc_response: "",
            oidc_generated_user: ""
        } : a, { saml_request: d, saml_response: c, saml_generated_user: l, saml_generated_roles: u, oidc_response: h, oidc_generated_user: g } = n, m = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__rest"])(n, [
            "saml_request",
            "saml_response",
            "saml_generated_user",
            "saml_generated_roles",
            "oidc_response",
            "oidc_generated_user"
        ]), p = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__rest"])(t, [
            "occurred",
            "ID",
            "type",
            "data"
        ]);
        var v;
        return ((e)=>Object.fromEntries(Object.entries(e).filter(([e, t])=>!!t)))(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({}, p), {
            data: m
        }), L("type", (v = o) && v[0].toUpperCase() + v.slice(1))), L("saml_request", d)), L("saml_response", c)), L("saml_generated_user", O(l))), L("saml_generated_roles", O(u))), L("oidc_response", O(h))), L("oidc_generated_user", O(g))), {
            occurredFormatted: s ? new Date(Number(s) || 0).toLocaleString() : "N/A"
        }));
    }));
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSelector"])(A, D, (e, t)=>t.find((t)=>e === t.id)), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$reselect$2f$dist$2f$reselect$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSelector"])(A, (e)=>!!e);
const W = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$reduxjs$2f$toolkit$2f$dist$2f$redux$2d$toolkit$2e$modern$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["createAsyncThunk"])("audit/search", (e, { extra: { api: t }, getState: s })=>{
    const i = s().searchParams;
    return t.audit.search(Object.assign(Object.assign({}, i), e));
}), S = ((e)=>(...t)=>(s)=>{
            s.addCase(e.pending, (e, s)=>{
                t.forEach(({ onPending: t })=>{
                    null == t || t(e, s);
                });
            }), s.addCase(e.fulfilled, (e, s)=>{
                t.forEach(({ onFulfilled: t })=>{
                    null == t || t(e, s);
                });
            }), s.addCase(e.rejected, (e, s)=>{
                t.forEach(({ onRejected: t })=>{
                    null == t || t(e, s);
                });
            });
        })(W)({
    onPending: (e, t)=>{
        e.searchParams = Object.assign(Object.assign({}, e.searchParams), t.meta.arg);
    },
    onFulfilled: (e, t)=>{
        e.auditList.data = t.payload;
    }
}, ($ = (e)=>e.auditList, {
    onFulfilled: (e)=>{
        $(e).loading = !1;
    },
    onPending: (e)=>{
        $(e).loading = !0, $(e).error = null;
    },
    onRejected: (e, t)=>{
        $(e).loading = !1, $(e).error = t.error;
    }
}));
var $;
const P = {
    action: W,
    reducer: S
}, M = {
    auditList: {
        data: [],
        loading: !1,
        error: null
    },
    searchParams: {
        text: "",
        sort: []
    },
    selectedAuditId: ""
}, E = {
    search: "/v1/mgmt/audit/search"
}, q = (e)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(void 0, [
        e
    ], void 0, function*({ text: e, sort: t }) {
        return new Promise((s)=>{
            const i = [], o = (new Date).getTime() / 1e3 + 2592e3, r = new Date(o).toLocaleString();
            for(let e = 1; e < 9; e += 1)i.push({
                id: `Audit ${e}`,
                action: `Action ${e}`,
                actorId: `Actor ${e}`,
                type: "info",
                method: `Method ${e}`,
                userId: `User ${e}`,
                device: `Device ${e}`,
                geo: `Geo ${e}`,
                remoteAddress: `127.0.0.${e}`,
                externalIds: [
                    `Login ID ${e}`
                ],
                occurred: o,
                occurredFormatted: r
            });
            t.forEach((e)=>{
                i.sort((t, s)=>{
                    var i, o;
                    return e.desc ? null === (o = s[e.field]) || void 0 === o ? void 0 : o.localeCompare(t[e.field]) : null === (i = t[e.field]) || void 0 === i ? void 0 : i.localeCompare(s[e.field]);
                });
            }), s(i.filter((t)=>t.id.toLowerCase().includes(e.toLowerCase()) || t.action.toLowerCase().includes(e.toLowerCase()) || t.type.toLowerCase().includes(e.toLowerCase()) || t.device.toLowerCase().includes(e.toLowerCase()) || t.method.toLowerCase().includes(e.toLowerCase()) || t.geo.toLowerCase().includes(e.toLowerCase()) || t.remoteAddress.toLowerCase().includes(e.toLowerCase()) || t.externalIds.includes(e.toLowerCase()) || t.userId.toLowerCase().includes(e.toLowerCase())));
        });
    }), T = ({ httpClient: e, tenant: t, mock: s })=>({
        search: (...i)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(void 0, [
                ...i
            ], void 0, function*({ page: i, limit: o = 1e4, text: r, sort: n, from: d } = {}) {
                if (s) return q({
                    page: i,
                    limit: o,
                    text: r,
                    sort: n
                }, t);
                const c = yield e.post(E.search, {
                    limit: o,
                    page: i,
                    text: r,
                    sort: n,
                    from: d
                }, {
                    queryParams: {
                        tenant: t
                    }
                });
                yield ((e)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(void 0, void 0, void 0, function*() {
                        const t = yield e.text(), s = JSON.parse(t);
                        if (!e.ok) {
                            const t = `${s.errorDescription}${s.errorMessage ? `: ${s.errorMessage}` : ""}`;
                            throw Error(t || `${e.status} ${e.statusText}`);
                        }
                        e.json = ()=>Promise.resolve(s), e.text = ()=>Promise.resolve(t);
                    }))(c);
                return (yield c.json()).audits;
            })
    }), N = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    var s, i, o, r;
    const p = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$projectIdMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["projectIdMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$observeAttributesMixin$2f$observeAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["observeAttributesMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$baseUrlMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["baseUrlMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$cookieConfigMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["cookieConfigMixin"], (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"])({
        tenant: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"].missingAttrValidator
    }))(e);
    return r = class extends p {
        constructor(){
            super(...arguments), s.add(this), i.set(this, void 0);
        }
        get tenantId() {
            return this.getAttribute("tenant");
        }
        get widgetId() {
            return this.getAttribute("widget-id");
        }
        get mock() {
            return this.getAttribute("mock");
        }
        get api() {
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f") || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, s, "m", o).call(this), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f");
        }
        init() {
            const e = Object.create(null, {
                init: {
                    get: ()=>super.init
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                var t;
                yield null === (t = e.init) || void 0 === t ? void 0 : t.call(this), this.observeAttributes([
                    "project-id",
                    "base-url",
                    "tenant"
                ], ()=>{
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "f") && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, s, "m", o).call(this);
                });
            });
        }
    }, i = new WeakMap, s = new WeakSet, o = function() {
        this.logger.debug("creating an sdk instance"), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldSet"])(this, i, ((e, t, s, i)=>{
            const o = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$web$2d$js$2d$sdk$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"])(Object.assign(Object.assign({}, e), {
                persistTokens: !0,
                baseHeaders: {
                    "x-descope-widget-type": "audit-management-widget",
                    "x-descope-widget-id": i,
                    "x-descope-widget-version": "0.5.34"
                }
            }));
            return {
                audit: T({
                    httpClient: o.httpClient,
                    tenant: t,
                    mock: s
                })
            };
        })({
            projectId: this.projectId,
            baseUrl: this.baseUrl,
            refreshCookieName: this.refreshCookieName
        }, this.tenantId, "true" === this.mock, this.widgetId), "f");
    }, r;
}), H = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    const s = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createStateManagementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createStateManagementMixin"])({
        name: "widget",
        initialState: M,
        reducers: {
            setSelectedAuditId: (e, { payload: t })=>{
                e.selectedAuditId = t;
            }
        },
        extraReducers: (e)=>{
            P.reducer(e);
        },
        asyncActions: {
            searchAudit: P.action
        }
    }), __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], N)(e);
    return class extends s {
        constructor(...e){
            super(...e), this.state = M, this.subscribe((e)=>{
                this.logger.debug("State update:", e), this.state = e;
            });
        }
    };
}), F = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    const s = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$staticResourcesMixin$2f$staticResourcesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["staticResourcesMixin"], (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"])({
        "widget-id": __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$createValidateAttributesMixin$2f$createValidateAttributesMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createValidateAttributesMixin"].missingAttrValidator
    }))(e);
    return class extends s {
        get widgetId() {
            return this.getAttribute("widget-id");
        }
        fetchWidgetPage(e) {
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                return (yield this.fetchStaticResource(`audit-management-widget/${this.widgetId}/${e}`, "text")).body;
            });
        }
    };
}), U = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    var i, o, r;
    return r = class extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initLifecycleMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initLifecycleMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$descopeUiMixin$2f$descopeUiMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["descopeUiMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$initElementMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["initElementMixin"], F, H)(e) {
        constructor(){
            super(...arguments), i.add(this);
        }
        onWidgetRootReady() {
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {});
        }
        init() {
            const e = Object.create(null, {
                init: {
                    get: ()=>super.init
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                var t;
                yield null === (t = e.init) || void 0 === t ? void 0 : t.call(this), yield Promise.all([
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, i, "m", o).call(this)
                ]), yield this.onWidgetRootReady(), this.dispatchEvent(new CustomEvent("ready"));
            });
        }
    }, i = new WeakSet, o = function() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
            const e = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createTemplate"])((yield this.fetchWidgetPage("root.html")));
            yield this.loadDescopeUiComponents(e), this.contentRootElement.append(e.content.cloneNode(!0));
        });
    }, r;
}), J = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    var s, o, r, d, c;
    return c = class extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], U, H)(e) {
        constructor(){
            super(...arguments), s.add(this), o.set(this, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["debounce"])(()=>{
                const e = this.rangeInput.value, t = new Date;
                let s;
                switch(e){
                    case "minuets15":
                        s = t.setMinutes(t.getMinutes() - 15);
                        break;
                    case "hour":
                        s = t.setHours(t.getHours() - 1);
                        break;
                    case "hour6":
                        s = t.setHours(t.getHours() - 6);
                        break;
                    case "day":
                    default:
                        s = t.setDate(t.getDate() - 1);
                        break;
                    case "day3":
                        s = t.setDate(t.getDate() - 3);
                        break;
                    case "week":
                        s = t.setDate(t.getDate() - 7);
                        break;
                    case "week2":
                        s = t.setDate(t.getDate() - 14);
                        break;
                    case "month":
                        s = t.setMonth(t.getMonth() - 1);
                }
                this.actions.searchAudit({
                    text: this.searchInput.value,
                    from: Math.floor(new Date(s).getTime())
                });
            }));
        }
        onWidgetRootReady() {
            const e = Object.create(null, {
                onWidgetRootReady: {
                    get: ()=>super.onWidgetRootReady
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                var t;
                yield null === (t = e.onWidgetRootReady) || void 0 === t ? void 0 : t.call(this), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, s, "m", r).call(this), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, s, "m", d).call(this);
            });
        }
    }, o = new WeakMap, s = new WeakSet, r = function() {
        var e;
        this.searchInput = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TextFieldDriver"](null === (e = this.shadowRoot) || void 0 === e ? void 0 : e.querySelector('[data-id="search-input"]'), {
            logger: this.logger
        }), this.searchInput.onInput((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, o, "f")), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, o, "f").call(this);
    }, d = function() {
        var e;
        this.rangeInput = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SingleSelectDriver"](null === (e = this.shadowRoot) || void 0 === e ? void 0 : e.querySelector('[data-id="range-input"]'), {
            logger: this.logger
        }), this.rangeInput.onInput((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, o, "f"));
    }, c;
}), V = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>{
    var s, i, r, d;
    return d = class extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(H, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$loggerMixin$2f$loggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["loggerMixin"], U)(e) {
        constructor(){
            super(...arguments), s.add(this), r.set(this, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["withMemCache"])((e)=>{
                this.auditTable.data = e;
            }));
        }
        onWidgetRootReady() {
            const e = Object.create(null, {
                onWidgetRootReady: {
                    get: ()=>super.onWidgetRootReady
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                var t;
                yield null === (t = e.onWidgetRootReady) || void 0 === t ? void 0 : t.call(this), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, s, "m", i).call(this), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, r, "f").call(this, D(this.state)), this.subscribe((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__classPrivateFieldGet"])(this, r, "f").bind(this), D);
            });
        }
    }, r = new WeakMap, s = new WeakSet, i = function() {
        var e;
        this.auditTable = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$component$2d$drivers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["GridDriver"](null === (e = this.shadowRoot) || void 0 === e ? void 0 : e.querySelector('[data-id="audit-table"]'), {
            logger: this.logger
        }), this.auditTable.onSelectedItemsChange((e)=>{
            this.actions.setSelectedAuditId(e.detail.value.map(({ id: e })=>e));
        });
    }, d;
}), G = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSingletonMixin"])((e)=>class extends (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$debuggerMixin$2f$debuggerMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["debuggerMixin"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$mixins$2f$dist$2f$esm$2f$mixins$2f$themeMixin$2f$themeMixin$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["themeMixin"], V, J)(e) {
        init() {
            const e = Object.create(null, {
                init: {
                    get: ()=>super.init
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["__awaiter"])(this, void 0, void 0, function*() {
                var t;
                yield null === (t = e.init) || void 0 === t ? void 0 : t.call(this);
            });
        }
    }), z = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$descope$2f$sdk$2d$helpers$2f$dist$2f$index$2e$esm$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compose"])((e)=>class extends G(e) {
    })(HTMLElement);
customElements.define("descope-audit-management-widget", z);
;
 //# sourceMappingURL=index.js.map
}),
]);

//# sourceMappingURL=node_modules_d7d95bd6._.js.map